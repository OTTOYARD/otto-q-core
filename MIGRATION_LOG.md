# MIGRATION_LOG.md — the human record

One row per change to the OTTO-Q brain. Newest at the top.

This is the record a person reads. `supabase_migrations.schema_migrations` says
*that* something was applied; this file says **what it did and why**, in language
someone can act on six months from now at 2am.

**Every applied migration gets a row here.** Including hotfixes typed live during
a demo — those get their row the same day. See `scripts/APPLYING.md`.

**"Verified" is not "applied without error."** It is the query, count, or run
that proves the behaviour actually changed. If that column says "n/a", the change
is not finished.

---

| Date | File | What changed | Why | Applied by | Verified |
|---|---|---|---|---|---|
| 2026-08-08 | `db/migrations/0021_one_vehicle_one_stall.sql` — **APPLIED, version `20260808170813`.** 1 new routine (`public.ottoq_stall_seat_is_exclusive`), 1 new trigger (`trg_ottoq_stall_seat_is_exclusive`, `BEFORE INSERT OR UPDATE OF current_vehicle_id ON public.stalls`). **Nothing dropped, nothing replaced, no cron touched, no run started or stopped by the file.** | **Found while certifying 0020, not reported by anyone.** Run `0a3c6910` (seed 20260822) froze at tick 5 and never advanced. The metronome kept firing every minute and kept rolling the entire tick back on `duplicate key value violates unique constraint "idx_stalls_one_vehicle_per_stall"` — **20 consecutive times.** `tick_count` sat at 5 while `next_tick_due_at` kept moving, so from the outside the run looked alive. That is the same failure family as every other defect on this project: motion reported but not earned. **Located exactly, not guessed** — `PG_EXCEPTION_CONTEXT` names `twin.ottoq_sim_advance_service_flow` line 351: `UPDATE stalls SET current_vehicle_id = v_rec.id, status='occupied' WHERE id = v_rec.booked_stall AND (current_vehicle_id IS NULL OR current_vehicle_id = v_rec.id)`. **That WHERE clause guards the wrong side of the index.** It stops the statement stealing a stall another vehicle is in, but `idx_stalls_one_vehicle_per_stall` is `UNIQUE (current_vehicle_id) WHERE current_vehicle_id IS NOT NULL` — it says a **VEHICLE** may be in only one stall — and nothing releases the stall the vehicle is already sitting in. Blocked vehicle `b19025e6`: `staged_awaiting_service`, seated in staging `cddbeec9`, holding a live `service_bay` booking on `6ff1e0a5` (window 15:17:39→17:06:39, sim clock 15:30:00) demanded by a `must_do` `fault_repair`. **NOT CAUSED BY 0020, and that was checked rather than assumed:** `b19025e6` is **not rider-flagged** in this run, so neither 0019's in-depot sweep nor 0020's consume-and-place is on its path; its `visit_key` `…:20260808165750` **collides with nothing** (its four historical keys are all distinct), so 0020's run-scoped uniqueness did not make this visit newly visible — pre-0020 the INSERT would simply have succeeded and the same booking would have been made; and `ottoq_sim_advance_service_flow` is untouched by both 0019 and 0020. It is a **latent, seed-dependent deadlock**: any run whose draw puts a bay-requiring `must_do` atom on an already-seated vehicle freezes at that tick and never recovers. `fault_repair` is a ~2% seeded draw, so which vehicle gets it is a property of the seed — three earlier runs on three other seeds missed it. | A frozen run is worse than a failed one: cron reports success, the metronome reports success, `next_tick_due_at` advances, and only `tick_count` and the sim clock tell the truth. **Fixed at the table rather than at line 351 on purpose:** 24 routines in this database write `stalls.current_vehicle_id` or `vehicles.current_stall_id`, so patching the one that happened to fail leaves the other 23 able to fail the same way on a different seed, and would mean replacing a large function to change one statement. The invariant belongs on the table — seating a vehicle releases wherever it was — which is the same shape `public.sync_stall_occupancy` already implements for the vehicle-driven path. **It cannot mask a booking conflict:** it never reads or writes `ottoq_stall_bookings`, so the PROTECT double-booking measurement (pairwise `during && during`) is unaffected in both directions. | **Claude Opus 4.8, branch `p0020-flag-atomic-consume`, based on `origin/main` @ `8f8e464`. Not merged.** Applied via **MCP `apply_migration`** — `SUPABASE_ACCESS_TOKEN` is **not** present in this session (`.claude/settings.local.json` now holds only a `permissions` key) and neither `psql` nor the Supabase CLI is installed, so the byte-exact stream path 0010/0018/0019 used was unavailable. **Applied-text md5 = `2f55734a4d2d81b9c3c68e171e9bb525`**; the committed file is **not** byte-identical because the narrative prose was condensed to fit the apply channel. **What is proven identical is the routine**: `md5(pg_get_functiondef('public.ottoq_stall_seat_is_exclusive'))` = **`aff8d99b068a2265058801832d167b2b`**, 20 lines, equal to the same block extracted from the committed file. Re-checkable at any time. | **APPLIED. A1–A3 passed inside the transaction.** **A3 is behavioural, not structural:** it performs the exact write that froze the run — seat a vehicle into a second stall while it still occupies a first — and proves (i) it now succeeds, (ii) exactly **one** stall claims the vehicle afterwards, (iii) the original stall was released, then restores both stalls and the vehicle's seat and **verifies the restore**. **A2** proves the invariant held at apply time: **0** vehicles claimed by two stalls. **THE REAL VERIFICATION IS THE RUN:** `0a3c6910` had been frozen at tick 5 across **20** consecutive metronome calls; it advanced to tick 6 **on the first metronome call after this landed** and kept going. Drift baseline `public` 343 → 344, **verified by name** (exactly one new routine; `ottoq` stays 55, `twin` stays 71; the trigger itself is not a routine). |
| 2026-08-08 | `db/migrations/0020_rider_flag_consume_and_place_is_atomic.sql` — **APPLIED, version `20260808165323`.** 3 new routines, all trigger functions in `public` (`ottoq_rider_flag_placement_guard`, `ottoq_rider_flag_mark_served`, `ottoq_reanchor_rider_flags_on_clock_rebase`) + their 3 triggers; 2 `CREATE OR REPLACE FUNCTION` (`twin.ottoq_sim_generate_service_manifest`, `ottoq.ottoq_rider_flag_indepot_sweep`), both **md5-guarded against their pre-images** (`dd6ac3a8…`, `79353bd6…`); 1 new column (`ottoq_rider_cleaning_flags.recalled_visit_id uuid` → FK to `ottoq_visit_needs(visit_id)` `ON DELETE CASCADE`) + index; 1 new unique index (`ottoq_visit_needs_vehicle_visit_run_uk`). **One constraint dropped** — `ottoq_visit_needs_vehicle_id_visit_key_key`, the run-blind uniqueness that is the defect — its exact definition captured verbatim to `public.mig0020_prestate` **first**, along with both pre-image function bodies. No routine dropped, no cron touched, `ottoq_events` untouched. | **🔴 THE REPORTED DEFECT IS REAL BUT WAS MIS-READ, AND THE LEAD IS REFUTED.** The brief said 2 of 16 flags were consumed and *"the cleaning VANISHED"* — `recalled_visit_key` matching **no** row, no rider-flagged atom anywhere. **The cleaning did not vanish.** Both keys match a real visit row on the right vehicle, and **both rows carry the rider-flagged atom**, correctly shaped (`must_do:true`, `deferrable:false`, `requires_bay:'wash_bay'`, with the recall `why` string). `f0077a3a`'s wash even reached **`status:'done'` at sim `2026-08-09T04:08:49`**. So: **one of the two was actually washed; the other was correctly placed and had not reached a bay when the run ended.** *Placed is not cleaned* — and neither was lost. **THE LEAD (two clock domains in one key space): CONFIRMED as a phenomenon, REFUTED as the cause.** `public.log_vehicle_state_change` really does `NEW.last_state_change = NOW()` on every state change and really does override an explicit sim-clock assignment in the same UPDATE (both `twin.ottoq_sim_wash_triage` and `ottoq.ottoq_rider_flag_indepot_sweep` are silently overwritten); **5 of 21** surviving flags carry a `recalled_visit_key` decoding to a **real** wall-clock instant equal to a run's `started_at` (`20260808154131` ×3, `20260808160932` ×2). But **both failing keys decode to clean SIM times matching their own recall exactly** — `f0077a3a…:20260808200000` (raised 19:56, recalled 20:00) and `99ddf4ff…:20260809010000` (raised 01:00, recalled 01:00). The lead cannot explain either, and its premise (*"matches no row"*) is false. **THE ACTUAL MECHANISM:** `visit_key = vehicle_id || ':' || to_char(clock,'YYYYMMDDHH24MISS')` **carries no run identifier**, and uniqueness was `UNIQUE (vehicle_id, visit_key)` — run-blind — upserted `ON CONFLICT … DO UPDATE SET atoms, urgency, archetype, meta, status='open'` **and NOT `sim_run_id`**. Runs `a30661fd` and `b857fd29` **both** had `sim_clock_start = 2026-08-08 13:00:00` and the same 30-sim-min grid, so the same vehicle arriving at the same sim minute produced a **byte-identical** key; the later run's INSERT fell through to DO UPDATE and **overwrote the earlier run's row**, keeping the earlier run's `sim_run_id` and `created_at`. **Proof from rows, not reasoning:** 4 visit rows are stamped `sim_run_id = a30661fd` **and** carry `meta.rider_flagged = true`, but run `a30661fd` drew only **two** flags (`aff36072`, `5fe5fe42`); the other two are `f0077a3a` and `99ddf4ff`, whose flags belong to `b857fd29`. That meta can only be written when a flag exists for the vehicle **in `v_run`**, so run C wrote it onto run B's row. **The failure is symmetric and one half was invisible:** (i) the current run's rider work disappears from every run-scoped query — the reported symptom; (ii) **a completed run's ledger is silently rewritten after the fact** — a black-box integrity problem well beyond rider flags that nobody was looking for. **Exterior-only was chance:** the mechanism never reads `flag_kind`. **Prior visit rows are NOT purged at run start** — 19 historical `sim_run_id`s hold visit rows on this instance right now — so this was waiting for any two runs to share a clock. | **(1) Run-scoped uniqueness, done at the INDEX not in the key string.** `visit_key` is also the salt for every `ottoq_sim_seeded_random(v_seed, v_visit‖…)` draw in the manifest, so a run tag in the string would move **every draw in every run** and unpin seed 424242. **This route changes no draw.** **(2) Consume-and-place is one atomic step keyed on `visit_id`.** 0019 wrote `status='recalled'` ~120 lines *before* any visit row existed; the upsert now carries `RETURNING visit_id` and the flag transition happens after it, binding a uuid the database handed back. **(3) Retire-or-place is decided by uuid BEFORE the atom is shaped** — deciding after the upsert would strand a `must_do`, non-deferrable cleaning on the new visit that nothing clears, and ALWAYS HOLD would then hold a **clean** car in the depot indefinitely. **(4) The runtime guard 0019 should have had** — this class of bug is silent, so it must fail loudly at the moment it happens, not at the next audit. **(5) `served_at_sim_clock` is finally stamped** — only the retire branch ever stamped it, and reaching that branch requires the car to **arrive again**, which is the 6-of-16 vs 11-of-16 undercount; fixed with one trigger on `ottoq_visit_needs` rather than by patching both atom-completion writers and missing the third somebody adds later. **(6) THE ANCHORING DECISION — FIXED, not deferred.** `ottoq_start_demo_run` calls `ottoq_sim_run_scenario` (which runs the boot draw and anchors every flag) and **only then** rebases `sim_clock_start`, so a run started from the normal start button anchors flags against a clock that no longer exists. 0019 routed around it by calling the scenario directly — **which is not what the founder presses.** Fixed with a trigger on `ottoq_sim_runs` that shifts every still-pending flag by exactly the delta whenever `sim_clock_start` moves: strictly more total than editing `ottoq_start_demo_run`, since it holds for any future rebaser and a hand-run UPDATE, and the seeded offset inside the window is carried forward exactly so the draw stays deterministic. **DELIBERATELY NOT FIXED, and named:** the 3× live-playback clamp (a playback-rate concern with no bearing on flag correctness — changing tick pacing under a metronome with its own timeout ceiling is not a rider on a data-integrity migration), and the `log_vehicle_state_change` clock clobber (once uniqueness is run-scoped it can no longer *lose work*; what remains is that some keys are ugly, not wrong, and re-sourcing `v_clock` would change every seeded draw). | **Claude Opus 4.8, branch `p0020-flag-atomic-consume`, based on `origin/main` @ `8f8e464`. Not merged.** Applied via **MCP `apply_migration`**: **`SUPABASE_ACCESS_TOKEN` is not present in this session** (`.claude/settings.local.json` now holds only a `permissions` key) and neither `psql` nor the Supabase CLI is installed, so the byte-exact stream path 0010/0018/0019 used was **unavailable**. **⚠️ DISCLOSED RATHER THAN GLOSSED:** to fit the apply channel the narrative comment prose was condensed, so the submitted text is **not** byte-identical to the committed file — file md5 `484c6c2f1c5264aa0c2e0afc6122252a`, applied md5 `33d05b9ffced4740da25be36600cfa31`. Claiming otherwise would be exactly the unearned claim this migration exists to stop. **What IS proven byte-identical is every routine the file creates or replaces**, `md5(pg_get_functiondef(oid))` against the same block extracted from the file — all five matched, comments and line counts included: `twin.ottoq_sim_generate_service_manifest` `f64da76153215943a95a2d34d895adfd` (423 lines), `ottoq.ottoq_rider_flag_indepot_sweep` `434f1b1017269445921cda8cc7f5e81f` (201), `public.ottoq_rider_flag_placement_guard` `03b9ffd7a1853132f12b36c81c017728` (59), `public.ottoq_rider_flag_mark_served` `830ef02d8a4ca3bd66cf1746cd1cf0de` (29), `public.ottoq_reanchor_rider_flags_on_clock_rebase` `a00f73d3d7c53c572fa23bed4c19f19e` (31). **So the brain is the committed file**; the divergence is confined to migration commentary outside every function body. Re-check: compare those five digests against the file at any time. | **APPLIED. Assertions A1–A6 passed inside the transaction** (a single failure would have rolled back every change). **A1** the run-blind constraint is gone and the run-scoped unique index is present. **A2** `recalled_visit_id` and its FK exist. **A3** all three triggers exist and are enabled. **A4** neither replaced routine still compares `recalled_visit_key` as a string, the generator captures `RETURNING visit_id INTO v_visit_id`, and the sweep's catch-all **re-raises** `OQ020` instead of swallowing it. **A5** totality: at **both** depots `exterior_wash` → `wash_bay` and `interior_deep_clean` → `wash_bay` (detail folds onto the wash lane; `detail_bay` is still 0 stalls), and an unrecognised service name returns **NULL = "no bay required"** rather than demanding a stall type that does not exist. **A6** no bound flag violates the invariant. **THE GUARD IS PROVEN NON-VACUOUS** by direct negative test on a throwaway flag row (inserted, tested, deleted): non-pending with no visit bound → **RAISED OQ020** (*"…moved to status 'recalled' with no visit bound to it"*); non-pending bound to another vehicle's visit → **RAISED OQ020** (*"…points at visit …, which belongs to vehicle …"*); row left `pending` → **no raise**, correctly allowed. **BACKFILL BEHAVED AS DESIGNED AND DID NOT LAUNDER:** 19 of 21 surviving flags bound to a real `visit_id`; the **2 cross-run contaminated ones (`f0077a3a`, `99ddf4ff`) were deliberately left NULL** — they *do* have a key match carrying the atom, on a row belonging to the **previous** run, and binding them would have turned the defect into a clean-looking ledger. **Drift baseline `ottoq` 54 → 55, `public` 339 → 343, verified BY NAME not arithmetic** (0019 contributed 2 routines, 0020 contributes 3 trigger functions; `twin` stays 71; the dropped object is a constraint, not a routine). **⚠️ RISK STATED, NOT HIDDEN:** the guard raises, and `twin.ottoq_sim_generate_arrival_manifests` has no handler, so a violation **aborts the tick**. That is deliberate — a stopped run that says why beats a finished run that quietly lost a customer complaint — and the guard is scoped to real transitions so routine UPDATEs and teardown never touch it. **Certification run `0a3c6910` (seed 20260822, exterior share raised to 65% so the sample can discriminate, rate restored immediately): the guard never fired.** See `db/evidence/P0020_root_cause_and_build.md`. |
| 2026-08-08 | `db/migrations/0019_rider_flag_holds_the_vehicle.sql` — **APPLIED, version `20260808153457`.** 2 new routines (`public.ottoq_rider_flag_due`, `ottoq.ottoq_rider_flag_indepot_sweep`), 4 `CREATE OR REPLACE FUNCTION` of existing routines (`ottoq.ottoq_plan_dispatch_tick`, `twin.ottoq_sim_dispatch_vehicle`, `twin.ottoq_sim_auto_dispatch_tick`, `public.ottoq_evaluate_return_need`), 3 COMMENTs. **Nothing dropped; no `VACUUM FULL`; no cron created, altered or disabled — cron 12 is the START engine and was not touched; no simulation run was started by the file.** | **0018 made the recall reach a wash bay. It did not take the car off the road, which is the claim the demo makes.** Measured on 0018's own runs: **9 of 10 flag-linked dispatches were SENT OUT with the flag ALREADY due** (flag age at dispatch 43 → 1,349 min) and recalled one tick later — only **1** was a genuine mid-deployment recall; **2 of 18 flags never fired at all** because they matured while the vehicle was parked (one car arrived 02:04, its flag matured 02:24, and it then sat in `staged_for_departure` for 15 more sim-hours); and **1 flag recalled the same car twice** over 16 sim-hours. **The mechanism, read from source:** `public.ottoq_evaluate_return_need` opens by selecting a dispatch with `status='active'` and returns `should_return=false` (`'no active dispatch in run'`) when there is none, so the rider-flag rung is **structurally unreachable** for a car sitting in the depot — and nothing stopped that same car being dispatched a moment later. **(i)** One predicate, `public.ottoq_rider_flag_due`, now gates both the candidate cursor in `ottoq.ottoq_plan_dispatch_tick` (`deploy_plan`) and the door in `twin.ottoq_sim_dispatch_vehicle`. A flag that is drawn but **not yet due gates nothing** — that car deploys and is recalled mid-deployment, which is the genuine scene. **(ii)** `ottoq.ottoq_rider_flag_indepot_sweep` runs once per dispatch tick **before** the deploy cursor: a parked car needs no recall, it needs the cleaning atom on the visit it is already having (or a visit opened for it) and to be returned to `staged_awaiting_service` so the bay router in `ottoq_decide_tick` (4b) can place it. It never moves a car that is charging, in a bay, at the gate, towed or under an exception, and excludes any car holding an `active`/`returning` dispatch — the exact complement of the recall rung, so every flagged car is owned by exactly one path. **(iii) JUDGEMENT CALL — the flag clears ON CONSUMPTION, not on service.** `ottoq_evaluate_return_need` now reads `ottoq_rider_cleaning_flags.status = 'pending'` instead of `vehicle_need_profile.rider_flag_pending`. Clearing on service would keep the trigger armed while the work sits on the visit and re-recall the car every time it went out — which is exactly the observed double-recall. Clearing on consumption is safe **only because ownership does not lapse**: at that instant the flag has become a `must_do` atom on an open visit, and both ALWAYS HOLD clauses hold the car until it is `done` or `cancelled`. **⚠️ A BRIEFING PREMISE FALSIFIED:** "ALWAYS HOLD already says this / prefer extending `SLA.004.required_services_complete`" — **it does not cover this atom**, and that was checked rather than assumed. Every ALWAYS HOLD clause in this codebase is written against `ottoq_visit_needs.atoms`; a rider flag is not an atom until a visit is generated, and 0018 generates that visit only on **arrival**. A flag maturing on a parked car has no atom and no visit for either clause to hold on. The hold is **extended inside the same cursor**, not duplicated. **Also corrected:** `ottoq_plan_dispatch_tick` is not a third dispatch path — it is the planner `twin.ottoq_sim_auto_dispatch_tick` calls, and the only place a candidate can be excluded. | The demo scene is: a rider flags a car dirty → OTTO-Q pulls it off the road → it routes to a specific bay → you watch it arrive. 0018 delivered the last two. This delivers the middle one. Without it the story a viewer can actually reconstruct from the ledger is "the depot sent out a car it already knew was dirty, then turned it around", and **against `planned_return_at` — the stored generated column that cannot be overwritten — all 10 flag-linked dispatches came home LATE** (median 16 min, worst 354, max −2; not one early), while `prime_inbound` returns a median 14 min early. `scheduled_return_at − returning_started_at` reads exactly 30 minutes for every rider-flag recall and looks crisp, but it is **meaningless**: all **297** dispatches across all **11** trigger types return that same single value, because `scheduled_return_at` is rebased when the return is decided. | **Claude Opus 4.8, branch `p0019-flagged-vehicle-hold`, based on `origin/main`. Not merged. STREAM-APPLIED** — the file was sent byte-exact from disk to the Supabase Management API `POST /v1/projects/{ref}/database/query`, **not** MCP `apply_migration`, for the same reason 0010 and 0018 did it: at 67,933 bytes neither `psql` nor the Supabase CLI exists on this machine and re-emitting 1,248 lines through a model context is a transcription risk on a file whose entire safety argument is that the bytes reviewed are the bytes that ran. **md5 of the bytes that ran = `7c2ce44a7272f56168fb4fd92dfe7e7f`** (67,933 bytes / 67,484 chars — the gap is UTF-8 box-drawing in the comments), byte-identical to the file as committed at `c02de39`. That endpoint writes **no ledger row**, so the `supabase_migrations.schema_migrations` row was written by hand immediately afterwards from the same file through the same endpoint, and **`md5(statements[1])` reads back that same digest** — that equivalence is what licenses the bypass. The version stamp was then written into line 1 of the file, so the file on disk now reads `29934425fdb031ca1e94c598f92d05c4`; **the only difference from the applied bytes is that one comment line.** **Single-transaction proven first** with an `ON COMMIT DROP` temp table still visible three statements later. **Then a full DRY RUN**: the identical file with its final `COMMIT` swapped for `ROLLBACK` — the four md5 guards and all five assertions executed clean, and every pre-image read back **unchanged** afterwards, with neither new routine present. The real apply took **0.42 s**. **Instance re-probed first** (`generate_series(1,1000000)` ×10 inside a plpgsql loop, not a CTE): 203, 197, 241, 198, 199, 195, 200, 198, 192, 192 ms — healthy, no sign of the host CPU starvation that once turned identical work into 17,748 ms. **The md5 pre-image guards 0018 omitted are in this file** (section 1, all four functions): they prove the pre-image this file was written against is the pre-image that was live — they do **not** prove the post-image, which is what the ledger md5 and the drift manifest are for. | **APPLIED. All five assertions passed** — twice, once in the dry run and once in the real apply; they run inside the transaction, so a single failure would have rolled back all six routine changes. **A1** the shared predicate reads true for a due pending flag, false one minute before it was raised, false for every NULL argument, and **both** dispatch gates consult it (the behavioural half used a probe row inserted under a `gen_random_uuid()` run id and deleted before COMMIT; the structural half is labelled structural and does not claim a live run declined a dispatch). **A2** the in-depot sweep exists, is called from the dispatch tick, and `position()` proves it runs **before** `'deploy_plan'` — a sweep running after it would let the car out for one tick. **A3** consuming the flag (`recalled`, then `served`) makes the predicate false, the recall rung reads `ottoq_rider_cleaning_flags`, and the string `p.rider_flag_pending` is **gone** from it. **A4** interior/exterior mapping still TOTAL: at **both** depots `exterior_wash` → `wash_bay` (3 stalls) and `interior_deep_clean` → `wash_bay` (3 stalls) — `detail_bay` is still **0 stalls**, so the two compete for the same three bays — and the sweep keeps an `ELSE` branch so an unrecognised kind still gets cleaned. **A5** every catalogue atom whose lane asks for a space (`exterior_wash`, `interior_deep_clean`, `cosmetic_repair`, `fault_repair`, `mechanical_pm`, `sensor_calibration`) resolves to a stall type that **exists**, at both depots. **Landing read back live:** all four pre-images moved — `ottoq_plan_dispatch_tick` `4ac5af7d`→`59907a11`, `twin.ottoq_sim_dispatch_vehicle` `0cbd66cb`→`1fdcb54c`, `twin.ottoq_sim_auto_dispatch_tick` `1681f011`→`7a8f8993`, `ottoq_evaluate_return_need` `9d9f0f92`→`5693e310`. **Drift: ledger 637 → 638, exactly one new row.** Sections A, B and C clean; B2 still lists only 0017 as written-not-applied. **Section D reads INVESTIGATE at +1 routine in `ottoq` and +1 in `public` — that is exactly the two routines this migration creates**, not an edit outside the migration path (0018 by contrast added none, all three of its changes being `CREATE OR REPLACE`). **PROTECT, re-measured after the apply, no regression:** **0 double-booked stall-pairs** over 373 booking rows (my own pairwise `during && during` on run+stall across `held`/`active`/`done`/`interrupted`, **not** the exclusion constraint); **225 foreign keys in `public`**, key lists generated from `pg_constraint`, **0 orphans**; 0010 geometry **PASS — 0 overlapping pairs and 0 `not_assessed`** on the heading-aware oriented footprint (`relative_x/y` read as FEET, no 1.5699 conversion), **160 stalls per depot = 115 staging / 30 l2 / 10 dcfc / 3 wash_bay / 2 service_bay in both depots**; 0008 laundering **0** — scoped to the latest run so there is no join fan-out (116 pairs, one per vehicle), 12 naive value-equality flags of which **all 12 are 0.000 = 0.000**, and **0 vehicles** under the directional `copied_after_wear` test (equal, non-zero, profile written after the wear row); 0009 credit honesty **0 over-credit** over 81 `twin.service_completed` events on both tests (credited never longer than `bay_capable`, and never naming anything the bay cannot do), 219 false credits prevented, and **`planned_return_at` still raises `428C9`** on UPDATE; cron **10 ON, 11 ON, 12 ON, 13 OFF** — unchanged. **🔴 THE BREACHED PROTECT LINE IS RESOLVED, AND 0018 DID NOT CAUSE IT.** Run A had 7 of 82 seated vehicles with no booking covering the seat. **Two independent lines of evidence, both preserved in `public.p0019_alwayshold_baseline`.** (1) **From the data:** 4 of the 5 `seated_without_booking` offenders have **zero bookings anywhere in the run and zero `ottoq_visit_needs` rows** — they never entered the booking loop at all, which is the signature of `twin.ottoq_sim_seed_fleet` seating the fleet onto stalls at run start; that function **writes no `ottoq_stall_bookings` row** (verified). The 5th (`db9d7b6e`) is `deployed` yet still carries a staging `current_stall_id` — a stale seat left by a dispatch, a different defect. (2) **From the code:** 24 routines in this database write `stalls.current_vehicle_id` or `vehicles.current_stall_id`; **none of the three functions 0018 replaced is among them.** 0018 cannot seat a vehicle. **Verdict: pre-existing, caused by run-start fleet seeding, filed not inherited.** A pre-0018 snapshot of this exact query does not exist (`p0017_clear_leftovers_*` captured vehicles and stalls but no bookings), which is why the argument is made from the code and the booking history rather than from a re-run of the query — that limit is stated rather than papered over. **⚠️ STILL UNPROVEN, AND THIS IS THE REAL GAP:** no simulation run has been executed against 0019, so nothing here shows a flagged car being **declined** a dispatch, a parked flag acquiring its atom, or a flag-linked dispatch returning **early** against `planned_return_at`. The assertions prove the gates are present and the predicate behaves; they do **not** prove the outcome. That needs a fixed-playback run (30 sim-min per tick, 48 ticks) verified to cross 20:00–06:00, and it is the next step, not this one. |
| 2026-08-08 | `db/migrations/0018_rider_flagged_cleaning_recall.sql` — **APPLIED, version `20260808041455`.** 1 new table (`public.ottoq_rider_cleaning_flags`), 3 new columns on `vehicle_need_profile`, 6 policy rows (3 params x 2 depots), 1 RLS policy, 3 `CREATE OR REPLACE FUNCTION`, 7 COMMENTs. **Nothing dropped; no `VACUUM FULL`; cron 12 untouched and still ON; no simulation run was started.** | **Two cleaning variabilities, both drawn per run from the seed.** **PATH 1 (wash rotation) — a real defect, found and fixed in the file.** The founder's rule is that both variabilities are "selected and determined at the start of any given simulation run. Randomized each time." The every-third-night wash gate in `twin.ottoq_sim_generate_service_manifest` is correct and this migration does not touch it. What was wrong is the *group* it reads: **all 216 active autonomous vehicles already carry a `config.wash_group`**, and the fallback is `abs(hashtextextended(vehicle_id::text, 77)) % 3`. **Neither branch involves `random_seed`**, so the wash rotation was byte-identical in every run that has ever been executed. 0018 redraws `wash_group` per run from the run seed inside the existing run-start draw (`public.ottoq_run_boot_draw`, new salt `washgrp:`), writing the same `vehicles.config` key, so all **three** readers (the manifest gate and both `ottoq_evaluate_return_need` sites) inherit it with zero reader edits. **PATH 2 (rider-flagged cleaning recall) — new.** A ridehail rider flags a car mid-deployment; it is recalled immediately rather than at its next natural return. Drawn at run start per vehicle from the seed (salts `rflag:` / `rflagkind:` / `rflagwhen:`, none carrying a clock), stored on `vehicle_need_profile` alongside the other ~24 drawn variables, and materialised as first-class rows in a new `public.ottoq_rider_cleaning_flags` (kind, truthful `why`, trigger, resolved stall type, recall + service timestamps) so an operator surface can display the whole scene. `public.ottoq_evaluate_return_need` gains one rung — `rider_flag_cleaning`, urgency `urgent`, **non-deferrable**, placed below the safety/reserve rungs and **above** the contention gate so a busy depot cannot starve it. `twin.ottoq_sim_generate_service_manifest` puts the matching cleaning atom on the visit as `must_do`. | The founder's investor demo is: a rider flags a car dirty while it is driving → Orchestra pops a notification → OTTO-Q auto-queues it → it routes to a specific bay → you watch it arrive in the 3D twin. **Path 2 is that scene**, and it did not exist. Path 1 existed but was frozen: a demo run that starts in the morning and ends in the evening sees **0 night arrivals**, so the whole rotation was invisible, and even when visible it was the same rotation every time. | **Claude Opus 4.8, branch `p0018-rider-flag-recall`. STREAM-APPLIED — the file was sent byte-exact from disk to the Supabase Management API `POST /v1/projects/{ref}/database/query`, NOT MCP `apply_migration`.** This is the same deviation 0010 made and it is recorded here rather than hidden: at 74,232 bytes the file is far over the re-emit threshold, neither `psql` nor the Supabase CLI exists on this machine, and routing it through a model context would have meant re-typing 1,278 lines — a transcription risk on a file whose whole safety argument is that the bytes reviewed are the bytes that ran. **md5 of the bytes that ran = `3cc30c4e1537ee533b81ac38e2d40334`** (74,232 bytes / 74,230 chars), byte-identical to the file as committed at `c0c404a`, before the version-stamp header was added. That endpoint writes **no ledger row**, so the `supabase_migrations.schema_migrations` row was written by hand immediately afterwards from the same file via the same endpoint, and `md5(statements[1])` reads back as that same digest — the equivalence is what licenses the bypass. **The endpoint runs the payload as ONE transaction**, verified beforehand with an `ON COMMIT DROP` temp table that was still visible three statements later; 0018 needs that, because its author moved the single `COMMIT` to the last line so a failing assertion rolls back the table, the columns, the policy rows and all three function replacements together. **Instance health re-probed first** (`generate_series(1,1000000)` x10 in one transaction): min 191.3 / median 192.4 / max 198.4 ms, 7.1 ms spread — no sign of the host CPU starvation that once turned identical work into 17,748 ms. Apply took 1.4 s. | **APPLIED AND VERIFIED — all seven assertions passed.** They ran inside the transaction, so a single failure would have rolled the whole file back; the apply returned clean, and the numbers below were then re-measured read-only against the live objects. **A1** same seed reproduces the flag set exactly (order-independent signature `6744974d2130` twice), a different seed differs (`ddb5a7feb216`). **A2** fleet 116, band [0,11], seed A drew 3 flags (3 interior), seed B drew 2 — inside the binomial band and under the tenth-of-fleet cap. **A3** `interior_deep_clean` -> `wash_bay` (3 stalls) and `exterior_wash` -> `wash_bay` (3 stalls); **`detail_bay` is still 0 stalls**, so interior and exterior flags compete for the same 3 wash bays, as the file says. **A4a** 0 CHECK constraints pin `return_trigger` to a literal set (the only one touching it is `chk_completed_has_return_trigger`). **A4b** 0 routines drop the new word through a positive `IN`-list. **A5** rider flags project 58.1 bay-min/sim-day against 4,320 available = **1.35%** of the wash lane (ceiling 25%); the nightly rotation's 348 bay-min stays the dominant load on the same bays. **A6** `wash_group` now differs across seeds (`7ba7413a5bbb` vs `bf6f71f1d394`) and splits the fleet 37/36/43, inside the 19–58 band. **A7** 0 of 116 vehicles show a salt collision. **Landing read back live:** all three function md5s moved (`ottoq_run_boot_draw` 79952ed3->ea2cc292, `ottoq_evaluate_return_need` edf36ea9->9d9f0f92, `twin.ottoq_sim_generate_service_manifest` d800e5fb->dd6ac3a8); the new table exists with RLS enabled and policy `rcf_service_role_all` (roles = `service_role`, not `public`); the 3 `rider_flag_*` columns are on `vehicle_need_profile`; 6 `rider_flag_*` rows sit at depot scope. **Drift CLEAN**: ledger 636 -> 637, exactly one new row; Section D routine counts unchanged at ottoq 54 / public 339 / twin 71, which is correct — all three functions are `CREATE OR REPLACE` of existing routines, so 0018 adds no routine. **PROTECT, re-measured after the apply, no regression:** 0 double-booked stall-pairs over 1,453 booking rows (own pairwise `during && during` on run+stall across held/active/done/interrupted, not the exclusion constraint); **225** FK constraints in `public` generated from `pg_constraint`, 0 with orphaned rows; 0010 geometry all PASS — 160 stalls per depot = 115 staging / 30 l2 / 10 dcfc / 3 wash / 2 service in **both** depots, **0 overlapping pairs and 0 not_assessed** on the heading-aware footprint, 0 buried, 0 outside the fence, parcel 452x314 ft (check 7's 32 tight stalls is the documented report-only Canopy B/C pitch, not a failure); `planned_return_at` still raises **428C9** on UPDATE (`column "planned_return_at" can only be updated to DEFAULT`); cron **10 ON** (depot-tick), **11 ON** (retention-nightly), **12 ON** (demo-metronome — the START engine, never touched), **13 OFF** (cert-battery). **STILL UNPROVEN, and this is the real gap:** no simulation run was started, so nothing here shows a rider flag actually firing mid-deployment, a bay being held for it, or the wash rotation differing between two real runs. That needs a run crossing a night window and is the next step, not this one. |
| 2026-08-07 | **NO MIGRATION APPLIED — verification session only.** Nothing created, replaced, dropped, and no `VACUUM FULL`. `db/migrations/0017_stop_is_two_phase_so_a_run_can_always_be_stopped.sql` stays **written but NOT applied**, deliberately: it replaces the START engine and the instance is still freezing episodically, so it cannot be tested. | **Nothing changed in the brain. What changed is what we know.** This session was sent to catch one thing — a `return_signal_prearrival` bay reservation reaching `done` — and to run the four PROTECT checks the previous session could not reach. **All five were reached**, on data already in the database. **No new run was started, and that was a choice:** the instance is still stalling (see the last column), and starting a run purges the prior runs, which would have destroyed the very witness rows that carry the proof. Read-only against the brain except for two small non-`ottoq`-prefixed diagnostic tables, `public.p0018_cpu_probe` and `public.p0018_probe`. | **🎯 THE ASSERTION IS CAUGHT — a 0011 forward hold reaches `done`, end to end, five times.** Read from `ottoq_bay_binding_witness` on `old_source='return_signal_prearrival'` (never from `source`, which the activator rewrites) and corroborated independently in `ottoq_stall_bookings.state`. **Denominator: 8 prearrival holds reached `active`, out of 60 created.** Split by the honest discriminator, `new_source`, and reported separately as required: **bound INSIDE the real forecast window (`bay_reservation_activated`) — 5 activations, 4 reached `done`, 1 `interrupted`.** **Bound EARLY, vehicle-first (`bay_reservation_activated_early`) — 3 activations, 1 reached `done`, 2 `interrupted`.** All 8 had `seated_on_reserved=true` at activation: the car went into the exact stall named for it 30–80 sim-min earlier. **Full lifecycle, sim domain, booking `c48ee44a` (run `eb8e2aaa`, busy_day/424242):** held **14:00** as `return_signal_prearrival` on stall `6ff1e0a5`; forecast window **16:33:24 → 17:12:24** (39 min); activated **17:00** as `bay_reservation_activated`, vehicle seated in that stall; **`done` 18:00**, `release_reason='bay_exit_transition'`. The other four `done` legs: `d8243eda` 13:30→15:00→16:00; `a7d3fbb1` 13:30→18:00→18:00; `7c5261dc` 13:30→18:00→19:00; `faefb0c6` (early) 17:30→20:00→21:00. ⚠️ **THE CAVEAT IS LOAD-BEARING: all five `done` legs come from ONE run, `eb8e2aaa`, which averaged 30 sim-min per tick** (17 ticks, 13:00→21:30). In the finer run `28e758b8` (46 ticks, ~3 sim-min per tick) **all three prearrival activations ended `interrupted` / `bay_exit_before_planned_end`.** At a 30–60 sim-min sampling interval the bay-exit check cannot see a mid-tick departure, and every planned bay duration here (12–51 min) is at or below one tick. **So `done` is observed, but it is not yet proven that the service ran to plan rather than that the coarse clock could not see the car leave.** Item 2 of the brief — did the service take as long as planned — is therefore **NOT ANSWERABLE from this data**: the measurement resolution is larger than the thing measured. Every `done` leg shows bay dwell pinned to the tick grid (60 sim-min) against a 12–51 min plan. **The next run must tick at ≤5 sim-min to settle it.** | **Claude Opus 4.8.** Nothing applied. Branch `p0017-quiet-run`, based on `bay-seat-twin-timer-0016`. **Not merged.** Crons confirmed **10/11/12 ON, 13 OFF**. DB **496 MB**, unchanged — nothing ran, so there is no growth figure and none is claimed. | **THE FOUR CHECKS THAT WERE NEVER RUN — all four run. Three clean, one deliberately not certified.** ✅ **0 double-bookings** — my own pairwise `during && during` scan on the same stall within the same run, NOT the exclusion constraint, over **657 bookings that actually held a stall** (`done` + `interrupted`). This denominator is deliberately historical: the depot is empty (0 `held`, 0 `active`), so a live scan would have been vacuous and is **not** reported as a pass. ✅ **0009 `planned_return_at` raises `428C9` on UPDATE — probed live**, not asserted. `UPDATE public.ottoq_vehicle_dispatches SET planned_return_at = now() WHERE true` returned `SQLSTATE 428C9, "column planned_return_at can only be updated to DEFAULT"`, captured in `public.p0018_probe`. Nothing was written. ✅ **`scripts/check-drift.sql` CLEAN**, run verbatim: verdict `CLEAN`, 636 ledger rows, 15 files, 0 PENDING; Sections A/B/C all `none`; Section D exact on all three schemas — public 339/339, ottoq 54/54, twin 71/71. ⚠️ **ALWAYS HOLD — the gate demonstrably fires, but I am NOT certifying it clean.** `SLA.007.redeployment_readiness` **848 of 848 passed**; `SLA.001.min_soc_at_deployment` **848 of 848 passed**; `SLA.004.required_services_complete` failed **8 of 848**, and every one of those 8 recorded `enforcement='block'`, `enforcement_taken='blocked'`, `pending_must_do=['readiness_check']` — that is the hold working, not breaking. **But 4 of those 8 vehicles have a dispatch row created at or after their block inside the same run**, and I could not prove from the ledger that those dispatches belong to a later, satisfied visit rather than the blocked one. Two of them had ~50 sim-min between block and dispatch (ample time to finish a readiness check); for the other two the dispatch lands in the same tick as the block. **Suspected-not-confirmed, named as an open follow-up rather than dressed as a pass.** Separately, `HW.005.vehicle_one_active_task` failed 6 of 298, all `blocked`, all the **same** vehicle `229f655b` carrying the **same two task ids** across 17 real-minutes and two runs — a persistent stale-state condition, not a race. Open gap. **🔴 0008 LAUNDERING — RESOLVED, AND IT IS NOT LAUNDERING.** The "8 over 555 pairs vs 0 over 207" contradiction is a **join fan-out, not a change in behaviour**. `vehicle_need_profile` has **116 rows, one per vehicle**; `ottoq_vehicle_wear` is keyed per `(vehicle_id, sim_run_id)` and holds **555 rows over those same 116 vehicles** — one per historical run. `JOIN USING (vehicle_id)` therefore compares today's single profile row against that vehicle's wear row from *every run that ever existed*, so the pair count is a function of how many runs are on the box (207 then, 555 now). **Neither number was ever a vehicle count.** The directional read: the 8 flagged pairs are **3 distinct vehicles** (`5ea53696`, `6fdd7f85`, `a1111111-…-005`); **every flagged pair is `0.000 = 0.000`**; and **all three vehicles carry a real `last_wash_at`** (2026-08-06 13:25, 13:28, 13:37) that legitimately explains a zero on both sides. On the current-run row `profile.updated_at` equals `wear.updated_at` exactly — the wash completion writing both, not a copy of a stale sensor — and on the older-run rows the profile was written *weeks after* the wear value, so it cannot have been copied from them. **Verdict: coincidental zero-equals-zero collision on 3 washed vehicles; 0008's invariant holds.** The check as written is defective and should be tightened to the latest run and counted in vehicles, not pairs. **🔴 THE INSTANCE IS STILL SICK, AND IT IS NOT OTTO-Q.** The cockpit tab stayed closed all session. With 0 running runs and ~2 active backends, identical pure-CPU work (`count(*) from generate_series(1,1000000)`) measured **208–1,033 ms across 12 samples** in a good window (`public.p0018_cpu_probe`) — yet this same session's postgres log shows that same statement at **17,748 ms**, checkpoints taking **20–86 s to write 109–517 buffers**, repeated `cron job 10/12 job startup timeout` (the postmaster cannot fork), and `could not accept SSL connection: EOF detected` in bursts. **Roughly half of this session's SQL calls died with "Connection terminated due to connection timeout."** Host-level starvation beneath Postgres, consistent with the prior session. **No tick-cost number can be honestly measured until it clears, and none is quoted here.** **REST OF PROTECT.** ✅ **0010 geometry: 0 overlapping pairs on both depots, 0 `not_assessed`, 160 per depot = 115 staging / 30 l2 / 10 dcfc / 3 wash_bay / 2 service_bay**, using the heading-aware footprint (`heading_degrees IN (0,180)` swaps width/depth), **not** naive `LEAST`/`GREATEST` — 12,720 pairs compared per depot, 320 stalls total. ⚠️ **Orphaned FKs — PARTIALLY measured, and I am saying so rather than inheriting the previous session's pass.** The catalogue reports **225 foreign keys in `public`** (248 across the whole database: public 225, auth 18, storage 5), which confirms the 225 figure and refutes the earlier session's 16. The per-constraint orphan loop **did not complete** — it was killed by the instance three times and rolled back each time, so no 225-wide result exists from this session. A hand-picked probe of the four edges this work depends on returned **0 orphans**: `ottoq_stall_bookings.stall_id → stalls`, `ottoq_stall_bookings.vehicle_id → vehicles`, `ottoq_bay_binding_witness.booking_id → ottoq_stall_bookings`, `ottoq_vehicle_dispatches.vehicle_id → vehicles`. **🔴 ADVERSARIAL — CAUSATION IS REFUTED AT THE STRONG LEVEL, and this is the most important finding after the assertion itself.** The question was whether the reservation *steered* the assignment or merely described it. At the moment each of the five `done` prearrival holds activated, I counted how many sibling bays of the same type in the same depot were occupied by another booking. **The answer is 0, every time.** Nashville has **2 service bays and 3 wash bays**, and at all five activations every sibling bay was free. **So a greedy assigner with no reservation would have found a bay of the right type for all five. The reservation never had to win a contest — there was no contest.** What the hold provably *did* do: it named the exact stall 30–80 sim-min ahead and the car went into that exact stall (`seated_on_reserved=true`, `veh_current_stall = booking.stall_id`), and the stall was not double-booked in the interim. **It determined WHICH bay, not WHETHER.** **The mechanism is proven correct; its value is not yet demonstrated, because demand never got dense enough to force a choice.** No early activation displaced an earlier booking and none starved another vehicle — under zero contention there was nothing to displace. **A NEW DEFECT FOUND WHILE DOING THIS — the `why` string goes stale and can name the wrong bay.** `ottoq_stall_bookings.why` is written at booking time and is **not** refreshed when 0015 rebases `during` or when the stall changes. On booking `7c5261dc` the `why` reads `service 15:16-15:54` against a real window of **17:58–18:36**; on `3f2c6a2b` it reads `14:28-15:06` against a rebased window of **13:48–13:52**; and on `a7d3fbb1` it names **`NASH-WSH-01`** while the row is reserved on **`NASH-WSH-02`**. This is the human-readable audit line disagreeing with the row it sits on — exactly the class of "the row lies" problem 0015 set out to end. Small, contained, and not fixed here because nothing was applied this session. |
| 2026-08-07 | `db/migrations/0016_bay_seat_writes_the_twin_service_timer.sql` (ledger version **20260807015854**, name `bay_seat_writes_the_twin_service_timer`) | **A car seated in a bay by OTTO-Q now carries the twin's service timer, so its work actually takes as long as it was planned to take.** `ottoq.ottoq_activate_due_bay_reservations` exposes `upper(b.during) AS win_end` and, in the same UPDATE that writes `current_state`, now also writes the two `vehicles.config` keys the twin's own admission writes: `svc_step` (`servicing` for a service_bay, `washing` otherwise) and `service_ends_at`, pinned to the booking's own window end (`win_end` when in-window, `p_clock + dur` on an early seat), floored one minute ahead so a seat can never complete on the tick that made it. **Nothing else changed** — same narrowed window gate, same contention order, same rebase, same decline-on-displace, same events. Nothing dropped, no `VACUUM FULL`, no cron touched. | **0015 made the binding real and thereby exposed a latent partial seam.** `twin.ottoq_sim_advance_service_flow` STEP 1 completes an in-bay vehicle when `(config->>'service_ends_at') IS NULL` — a deliberate self-heal for stuck cars. The twin's own admission always writes `svc_step` + `service_ends_at` together, so it never trips it; `ottoq_activate_due_bay_reservations` wrote the bay state and **never** wrote the timer, so the twin completed the service on the very next tick. Measured on run `28e758b8` (busy_day / 424242, pre-fix), grouped by the path that seated the car: `bay_reservation_activated_early` **5 legs, 15.2 planned vs 2.9 actual min, 3 of 5 under a quarter of plan**; twin-driven (null) **3 legs, 17.7 vs 21.0**; `bay_reconcile_twin_admit` **2 legs, 38.2 vs 38.2**. **7 of that run's 18 `twin.service_completed` events carried `self_healed=true`.** The defect predates 0015 but was unreachable — before 0015 this function bound nothing (26 holds, 0 bound). A revert restores '0 bound', the vehicle-first violation 0015 exists to fix, so the fix is forward. `ottoq_activate_present_bookings` verified (pg_proc) not to need it: it never updates `vehicles` at all. | Claude Opus 4.8, MCP `apply_migration` (name `bay_seat_writes_the_twin_service_timer`), which wrote the ledger row. Body re-emitted rather than streamed (no `psql`/CLI on this machine); landing verified live out of `pg_proc`: `writes_timer=true`, `has_win_end=true`, and 0015's gate still present (`still_has_0015_gate=true`). Section D unchanged — 0016 is a pure `CREATE OR REPLACE`, ottoq stays **54**. | **PARTIALLY VERIFIED. The headline the session was sent to prove is NOT proven, and that is stated plainly.** ✅ **FIRST CAUSAL BINDING OF A 0011 RETURN-SIGNAL HOLD** (run `28e758b8`, pre-fix, from the 0014 witness): booking `3f2c6a2b`, vehicle `0d43459d`, purpose `service`, `booked_at_sim` **13:06**, original forecast window **14:28–15:06** (captured before the rebase by side-table instrumentation; lead **82 sim-min**), went `held → active` at sim **13:48** — **40 sim-min AHEAD of its own forecast window** — `old_source='return_signal_prearrival'` → `new_source='bay_reservation_activated_early'`, `seated_on_reserved=true`, and `veh_current_stall = booking.stall_id`. Duration preserved exactly (38 min → 38 min). 0014's earlier binding had `source` NULL; **this is the first for 0011's own holds.** ❌ **It did not reach `done`** — `interrupted` / `bay_exit_before_planned_end` after 4 of 38 min. That is the defect this migration fixes. ⚠️ **The `done` leg for a `return_signal_prearrival` hold is STILL UNOBSERVED.** Post-fix run `12b7ee9c` reached only **9 ticks / 75 sim-min** before the environment degraded: **10 prearrival holds created, 0 bound, 2 superseded, 0 released.** What the post-fix run *does* show, on its own denominators: **0 `self_healed=true` against 5 `self_healed=false`** (pre-fix was 7 of 18), and the witness records `bay_reservation_activated → done` (**n=2**) — the activation path reaching `done` at all for the first time, though on bookings whose `old_source` was NULL, not prearrival-tagged. 🔴 **Why the run is short, and it is not the code:** a **live dashboard is polling this database continuously** — `pg_stat_statements` shows `ottoq_twin_snapshot` at **18,597 calls / 217.5 ms mean (67 min cumulative)**, plus `twin_wear_window` 617.8 ms, `agent_board` 561.9 ms, `twin_events_window` 508.1 ms, `twin_labor_window` 479.8 ms, `twin_offsite_window` 457.2 ms. Tick compute went **773 ms median (run 28e758b8) → 10,019–31,767 ms (run 12b7ee9c)** under that contention, cron 12 was repeatedly cancelled at the 120 s ceiling and by `job startup timeout`, and the SQL endpoint then refused even `select 1` for ~35 minutes. **All tick-cost numbers from run 12b7ee9c are contaminated by that external load and must not be read as tick cost.** **PROTECTED SET — only partly re-measured, denominators stated; the gaps are named rather than assumed clean.** ✅ **0010 geometry: 0 overlapping pairs, 0 `not_assessed`, 320 stalls (160/depot)**, heading-aware footprint (`heading IN (0,180)` swaps width/depth), not naive `LEAST`/`GREATEST`. ✅ **0 orphaned FKs across 225 constraints**, key list generated from `pg_constraint` and probed per-constraint via `query_to_xml` — confirming 225, not the 16 an earlier session reported. ✅ **0009: `planned_return_at` is `attgenerated='s'`.** ✅ Cron **10/11/12 ON, 13 OFF** at run start. ❓ **0008 laundering UNRESOLVED — reported as unresolved, not as a pass and not as a regression.** My directional query returned **8 flagged over 555 pairs**, but the protected figure is over **207 pairs**, so this is a different denominator, not a like-for-like comparison; the follow-up that would have shown whether those 8 are trivial `0.000 = 0.000` collisions timed out and was never completed. ⬜ **NOT MEASURED:** pairwise `during && during` double-booking scan, ALWAYS HOLD defects, the `428C9` UPDATE probe, and `scripts/check-drift.sql` — the endpoint was unavailable. DB **492 MB** at last successful read. |
| 2026-08-07 | `db/migrations/0015_early_activation_and_untagged_event_flood.sql` (ledger version **20260807013120**, name `early_activation_and_untagged_event_flood`) | **A ready car now takes a free bay instead of waiting on a forecast — and the untagged event flood is cut at source.** **(A)** New `ottoq.ottoq_vehicle_bay_ready(run,vehicle,clock)`: TRUE only when the vehicle owes no unfinished charge leg (the identical `planned_end_sim` predicate `ottoq_reconcile_bay_reservations` already calls BLOCKER 1, because charge-leg *status* is not trustworthy). TOTAL — any error returns FALSE, and FALSE only ever means "fall back to today's behaviour". `ottoq.ottoq_activate_due_bay_reservations`: the window gate is **narrowed, not deleted**. `AND lower(during) <= clock` became `AND (lower(during) <= clock OR ottoq_vehicle_bay_ready(...))`; `AND upper(during) > clock` is **untouched**, so a lapsed hold still never seats. The "not before the car physically exists at the depot" guarantee is not lost, because the loop's own `v.home_depot_id = depot AND v.current_state IN (arrived_at_gate, staged_awaiting_service, charge_complete_holding, service_complete_holding)` predicate already asserts exactly that — and that same predicate IS the in-depot reassignment gate, since a car in a bay, charging, deployed or faulted is not in the list. On an early seat the booking's `during` is **rebased to `[now, now + the original duration)`**, duration preserved exactly, which stops the row lying, keeps `ottoq_bay_binding_witness.inside_window` honest, stops the no-show grace running against a car already in the bay, and — the load-bearing part — lets the **three existing exclusion constraints adjudicate displacement**: an `exclusion_violation` means another `held/active/done/interrupted` booking already owns part of that time, so the early seat is **DECLINED** and the stall reservation handed straight back. Nothing is ever pushed or dropped. **Contention:** `ORDER BY (lower(during) <= clock) DESC, lower(during), booking_id` — due-now beats early, then earliest forecast, then the PK, so the order is total and stable; the seat itself is still claimed by the pre-existing `ottoq_reserve_stall` CAS. `ottoq.ottoq_reconcile_bay_reservations` gains a **VEHICLE-FIRST PRE-EMPTION** as its first test (car ready + standing here idle + its booked stall physically free ⇒ leave the booking completely alone, `left_for_early_activation` in the summary event) and **BLOCKER 3 narrowed to `deploy_gate` remedy `need_charge`**. **(B)** `public.ottoq_vehicles_state_change` adds `last_state_change` to its existing pure-clock skip list; `public.ottoq_stalls_state_change` adds `reserved_at`; both, plus `public.ottoq_evaluate_rule_core`, now tag `data_source='twin'` + `sim_run_id` via the new `ottoq.ottoq_active_sim_run_id()` (memoised into the transaction-local GUC `ottoq.sim_run_id`, so 70k trigger firings cost one lookup). `ottoq_evaluate_rule_core` stops emitting the `rule.evaluated_pass` **event** (and its `linked_event_id` stitch UPDATE) for a pass on a non-shadow rule; every fail, every shadow-tier evaluation and every `system.catalog_miss` still emits, and `ottoq_rule_evaluations` still gets one complete row per evaluation either way. `stall.state_changed` added to 0006's `ottoq_write_slimming_policy.chain_event_types`. **(C)** `ottoq_svc_to_stall_type` is **dead code** — see the correction below. **Nothing dropped. No `VACUUM FULL`. Shield, cron 12, cron 10/11, retention purge and the append-only guard all untouched.** | **The brief's own premise was wrong, and the witness table proves it.** 0011's holds did not fail for want of an activation: **21 `return_signal_prearrival` holds went `held → released` with `inside_window = FALSE`**, plus 2 to `superseded`. They died *before their window ever opened*, so activation never got a turn. `ottoq_decide_tick` calls `ottoq_reconcile_bay_reservations` (line 45) **before** `ottoq_release_vacated_spaces → ottoq_activate_due_bay_reservations` (line 47), and the reconcile ledger names the killer: **96 releases as `replanned_no_window`**, plus **52 defers + 10 relocations + 12 releases** attributable to `deploy_gate:need_service`. That last one is a circular deadlock — the twin's deploy gate pins a car in staging *because* must-do work is open, and the reconcile then pushes that car's service-bay reservation away *because* the car is gated. Fixing the window check alone would not have bound a single hold. On the flood: of 69,917 `vehicle.state_changed` rows written with `sim_run_id IS NULL`, **`current_state` appears in the diff only 989 times per 20,000 sampled (4.9%)**, while **56,544 of 69,917 (80.9%) carry a diff of nothing but the three clock keys** `{updated_at, current_soc_updated_at, last_state_change}` — an event asserting a state change in which no state changed. `rule.evaluated_pass` added another 29,178, every one a strict subset of the `ottoq_rule_evaluations` row written microseconds earlier. | Claude Opus 4.8, MCP `apply_migration` (name `early_activation_and_untagged_event_flood`), which wrote the ledger row directly. Neither `psql` nor the Supabase CLI is installed on this machine, so the applied text was re-emitted rather than streamed from the file; **the header comment block differs between the two and the md5s therefore do not match, which is stated here rather than papered over.** What *is* proven is the landing: 11 named markers read back live out of `pg_proc` (both new routines present; `activate_due_bay_reservations` calls `ottoq_vehicle_bay_ready` and stamps `bay_reservation_activated_early`; `reconcile_bay_reservations` carries `left_for_early_activation` and `deploy_gate:need_charge`; the vehicles trigger skips `last_state_change`; the stalls trigger tags via `ottoq_active_sim_run_id`; `evaluate_rule_core` gates the pass event on shadow; policy chain = `{vehicle.state_changed, stall.state_changed}`), plus **5 pre-replace snapshots** under label `0015_early_activation_and_untagged_event_flood_pre`. | **STRUCTURALLY VERIFIED, NOT YET PROVEN AT RUNTIME — said plainly, because the difference matters.** No sim run was started. Starting one purges the prior run, which would have destroyed the `ottoq_bay_binding_witness` and reconcile-ledger rows this entire diagnosis rests on; that trade is Chase's call, not mine. **PROTECTED SET, with the denominator stated for every number.** Genuine, non-vacuous passes: **0 double-bookings** on my own pairwise `during && during` scan (not the constraint) over the **176 bookings in the `held/active/done/interrupted` state set** — the same set the `_v3` exclusion constraint governs. (The all-states scan returns 32 overlaps; those are `released`/`superseded` rows, deliberately outside every constraint predicate, and are not defects.) **0 orphaned FKs across 225 foreign keys**, the key list **generated from `pg_constraint` and executed per-constraint via `query_to_xml`** — never hand-listed. A previous session verified 16 and said so; the real figure is 225. **0010 geometry: 0 overlapping pairs, 0 `not_assessed`, and both depots at exactly 115 staging / 30 l2 / 10 dcfc / 3 wash_bay / 2 service_bay = 160**, using the heading-aware footprint, not naive `LEAST`/`GREATEST`. **0008: 207 pairs, `still_copied` = 0** on the directional `copied_after_wear` test — the laundering signature is gone, not merely small. **0009: `planned_return_at` on `ottoq_vehicle_dispatches` is `attgenerated='s'` and an UPDATE raises `428C9`**, confirmed by probe, not assumed. **Cron 10/11/12 ON, 13 OFF.** Section D re-baselined **ottoq 52 → 54, verified BY NAME**: live list is 54, ADDED = {`ottoq_active_sim_run_id`, `ottoq_vehicle_bay_ready`}, REMOVED = {} — all 52 prior names still present. public 339 and twin 71 unchanged. **VACUOUS, AND MUST NOT BE READ AS PASSES:** `ALWAYS HOLD = 0 defects` over **0 deployed vehicles**, and the live-booking overlap count over **0 `held`/`active` rows** — the depot is empty (0 running runs, 4 occupied stalls, 220 vehicles all idle). Those two are the exact trap of measuring after a stop and are reported as unmeasured, not as clean. **TIMEOUT CEILING RE-CHECKED rather than assumed, and the 162 s puzzle resolves:** `statement_timeout = 120000` comes from `/etc/postgresql-custom/platform-defaults.conf` (`source = configuration file`), and cron runs as `postgres`, which has **no** `pg_db_role_setting` override — so 120 s is genuinely the ceiling. The corroboration is that **cron 12's longest SUCCESSFUL run in 3 days is 119.9 s**, sitting right on it, while every longer duration (max 240.6 s) is a **`failed`** row whose recorded window includes cancellation and unwind. A >120 s `failed` duration is not evidence of a higher ceiling. For scale: over 3 days cron 12 logged **505 failures against 3,790 successes** and cron 10 **258 against 1,896**. DB **455 MB**. |
| 2026-08-07 | `db/migrations/0014_bay_binding_witness.sql` (ledger version **20260807005437**, name `bay_binding_witness`) | **The binding proof was unprovable as a post-hoc query, so this makes it observable.** Adds `public.ottoq_bay_binding_witness` (+2 indexes), `ottoq.ottoq_witness_booking_transition()` and an AFTER UPDATE trigger on `ottoq_stall_bookings` firing only when `state` or `source` actually changed. Each row stamps, at the instant of transition: old/new state, **old/new source**, the vehicle's **actual** `current_stall_id`, the reserved window, and **both clock domains**. Purely additive — nothing dropped or replaced; the trigger body is fully wrapped so an audit write can never abort a booking transition. | Two measured reasons the assertion could not be answered after a run. (1) `vehicles.current_stall_id` is a **point-in-time scalar** — it is overwritten on every move and leaves no history, so "was the car on the reserved stall inside the window" is unanswerable at teardown. (2) 🔴 **The success path renames the evidence.** `ottoq.ottoq_activate_due_bay_reservations` — the ONLY routine that physically seats a car in its reserved bay — ends with `SET state='active', source='bay_reservation_activated'`, overwriting the `return_signal_prearrival` provenance. The literal proof query `source='return_signal_prearrival' AND state='active'` therefore returns **zero rows through the binding path**, not only through the failure path. Every prior session ran that query, got zero, and read it as "nothing ever bound" — the query could never have shown otherwise. The witness's `old_source` survives the rewrite, and `old_source` vs `new_source` also separates **causation** (`activate_due_bay_reservations` MOVED the car) from **correlation** (`activate_present_bookings` merely agreed with where the car already stood). | Claude Opus 4.8, MCP `apply_migration` | Armed mid-run on `6f7518ef` (busy_day/424242, 3×) while the first pre-arrival window was still **0.8 sim-min** away, so no binding could be missed; witness table **0 rows at apply**, **279 rows at stop**. **FIRST OBSERVED CAUSAL BINDING**, booking `555c29da`, `NASH-WSH-02` (wash_bay), need `interior_deep_clean`: reserved at sim **15:07:16** for window **15:59:54–16:24:54** (lead **52.6 sim-min**), went `held → active` at sim **16:01:10** via `bay_reservation_activated` with `seated_on_reserved = true` and `inside_window = true` — i.e. `vehicles.current_stall_id = booking.stall_id` for that same vehicle, inside the window. It **never reached `done`**: the run was stopped 47 real-seconds later (`ended_at` 01:08:39, activation observed 01:07:52), so teardown released it. **Its `source` was NULL, not `return_signal_prearrival`** — so the mechanism is proven, but **not** for 0011's return-signal holds: those are **26 created, 0 bound** in this run. PROTECT re-measured: 0 double-bookings (own pairwise query), 0 orphans/16 FK keys, geometry PASS both depots (115/30/10/3/2, **0** overlapping pairs, **0** not_assessed), 0008 laundering **0/207** directional, `planned_return_at` still raises **428C9**, cron 10/11/12 ON + 13 OFF, drift ottoq 51→52 named to this migration's one routine. |
| 2026-08-07 | `db/migrations/0013_metronome_guard_reads_the_real_timeout.sql` (ledger version **20260807002716**, name `metronome_guard_reads_the_real_timeout`) | **0012's ceiling guard never armed; this makes it real.** 0012 read the CALL's deadline with `NULLIF(current_setting('statement_timeout', true), '')::numeric / 1000.0`. `current_setting` returns the GUC **display** form — measured on this instance it is `'2min'`, while `pg_settings.setting` is `'120000'`. `'2min'::numeric` raises 22P02, 0012's own `EXCEPTION` handler set `v_ceiling_s := 0`, and `IF v_guard_on AND v_ceiling_s > 0` was false on every call. The guard was dead code from the moment it shipped. 0013 reads `pg_settings.setting` (always the raw value in the GUC's base unit, never suffixed) and keeps the stand-down-on-unreadable fallback deliberately. The **only** delta vs the 0012 body is those two lines. **Also corrects 0012's section (3)**, which proposed a cron 12 command that cannot work — see Why. | 0012's guard was written to stop the metronome being guillotined by `statement_timeout`, and it could not, because it never read a non-zero ceiling. Separately, 0012 proposed `SET statement_timeout = '300s'; CALL public.ottoq_demo_metronome(110);` as cron 12's command. Applied, it failed **three times in two minutes** — `ERROR: invalid transaction termination, CONTEXT: PL/pgSQL function ottoq_demo_metronome(integer) line 139 at COMMIT` (cron 12 at 00:19:28 / 00:20:09 / 00:21:32, 22.4 s / 65.5 s / 92.2 s). pg_cron wraps a **multi-statement** command in one implicit transaction, so a procedure that COMMITs must be the ONLY statement in the command — which is exactly why cron 13's `SET …; SELECT …` is legal (a FUNCTION never commits) and this was not. Reverted on sight to the single statement `CALL public.ottoq_demo_metronome(90)`, budget 50 → 90 s to cut the cold-plan-cache tax 0012 (M4) measured. The 120 s ceiling therefore stays, and the repaired guard — not a cron command — is what keeps the CALL clear of it. | Claude Opus 4.8, MCP `apply_migration` | `pg_settings.setting = '120000'` → `v_ceiling_s = 120.0` (vs `current_setting` = `'2min'`, uncastable). Guard arithmetic with budget 90 s against `0.9 × 120 = 108 s`: worst-tick 5 s → reserve 15 s → budget binds at 90 s; worst-tick 26.6 s → reserve 39.9 s → call returns at 68 s, clear of the axe. cron 12 confirmed `active=true`, command `CALL public.ottoq_demo_metronome(90)`; cron 10/11 ON, 13 OFF. |
| 2026-08-07 | `db/migrations/0012_tick_cost_and_metronome_ceiling.sql` (ledger version **20260807001645**, name `tick_cost_and_metronome_ceiling`) | **The calendar read got the index it never had, and the metronome learned to stop before the axe.** (1) `public.ottoq_stall_bookings_live_stall_idx` — a partial btree on `(sim_run_id, stall_id)` over the same state set `ottoq.ottoq_stall_free_between` reads (`held/active/done/interrupted`), so the calendar read's `NOT EXISTS` stops seq-scanning the bookings table once per candidate stall. (2) `public.ottoq_demo_metronome` gained a ceiling guard: it tracks the worst tick **in this call** and refuses to BEGIN a tick when `elapsed + max(1.5 × worst_tick, 15 s) > 0.9 × statement_timeout`, so the CALL returns cleanly instead of being cancelled mid-tick. Kill switch `metronome_ceiling_guard` (default 1). **⚠️ The guard as shipped in 0012 never armed — see migration 0013, which repairs it.** Nothing dropped; cron 12 not disabled, not rescheduled; the shield untouched; 0011's forward-walk untouched. | Measured on run `8368ad72` (seed 424242, busy_day, live 3×, same 13:48 sim start as reference run `897debec`) before writing a line. **The metronome does not overrun because a tick is slow** — cron 12 loops until `elapsed > 50 s`, so ~50 s *is* the design, and pg_cron does not run job 12 concurrently (own pairwise-overlap query over 52 calls: **0 overlapping runs**). **The freeze is a guillotine**: `statement_timeout = 120000` comes from the config file and bounds the whole CALL (internal COMMITs do not re-arm it), while the budget is only checked *between* ticks — so a tick started at t=50 s can run to t=120 s and be cancelled. Reproduced live at 00:12:32 (145.3 s, cancelled inside `ottoq_record_event`) having previously been seen inside `ottoq_record_enacted_booking` — **two different "culprit" functions at the same 120 s boundary, which is the proof the CONTEXT line is where execution happened to be, not a root cause.** Steady-state cost, from a 23-phase stopwatch copy of `ottoq_sim_advance_tick_world` (`track_functions` is **not settable on this instance** — 42501 at both session and database scope — so explicit instrumentation replaced the technique named in the brief): world tick **878.6 ms avg / 2 316.9 ms max**, of which `advance_deployed_telemetry` is **560.2 ms (64 %)**; decide **358.5 ms avg**. A warm beat is ~1.1 s, not 38 s. The per-call tax is the **cold plan cache** — first tick of a metronome call is **2.5–7.4× the rest** in 5 of 8 calls (6 663 vs 896, 9 099 vs 2 557, 3 010 vs 631, 1 934 vs 576, 5 127 vs 2 075 ms). | Claude Opus 4.8, MCP `apply_migration` | **Index, EXPLAIN (ANALYZE, BUFFERS) on the staging calendar read (115 candidate stalls):** before — `Seq Scan on ottoq_stall_bookings (loops=115, Rows Removed by Filter: 206)`, **13.133 ms**, `shared hit=2139`; after — `Index Scan using ottoq_stall_bookings_live_stall_idx (loops=115, Rows Removed: 1)`, **2.373 ms**, `shared hit=331`. **5.5× faster, 6.5× fewer buffers**, and 115 × 206 = 23 690 tuple checks per read eliminated — the origin of the 226 375 534 `seq_tup_read` / 1 652 566 `seq_scan` standing on that 228-row table. A companion `stalls (depot_id, stall_type, …)` index was built and measured in the same session: planner kept `idx_stalls_depot`, 2.373 → 2.466 ms, **no gain — dropped, not shipped**. **0011 EXONERATED with a number:** its 25-step forward walk costs **164.0 ms** (service_bay) / **48.2 ms** (wash_bay) per fully-contended leg, measured by executing that exact walk; observed spikes are 12–14 s, so it cannot produce them. It is also currently walking **zero** steps — **0 of the 49** outstanding unbooked `planned` legs carry a leg_type its CASE maps to a stall type (inspect 38 / interior_tidy 5 / sensor_clean 4 / remote_diagnostics 2). Guard verification is in the 0013 row. |
| 2026-08-06 | `db/migrations/0011_forward_bay_reservation_at_return_signal.sql` (ledger version **20260806231121**, name `forward_bay_reservation_at_return_signal`) | **The return signal now reserves SERVICE and WASH bays, not just a plug.** `ottoq.ottoq_book_appointment` calls the new `ottoq.ottoq_reserve_inbound_bays` immediately after it plans the inbound itinerary and BEFORE its charge-stall search, so a busy tick with no free charger no longer skips the bay hold. The reservation is written to `ottoq_stall_bookings` — the same forward calendar everything else reads, `source='return_signal_prearrival'` — through the existing `ottoq_book_workflow`, which was generalised into `ottoq.ottoq_book_workflow_legs` (leg-type filter, arrival floor, source stamp); the 6-arg `ottoq_book_workflow` is now a thin delegate with identical behaviour for its two existing callers. New `ottoq.ottoq_svc_to_stall_type` resolves the physical requirement TOTALLY from `service_cadence_policy.lane`, falling back to `service_definitions.stall_type_required` only when the lane is unknown AND the type is really seeded. **⚠️ CORRECTION, 2026-08-07 (0015): this sentence overstates what shipped. `ottoq_svc_to_stall_type` is DEAD CODE and always has been — a `pg_proc.prosrc` scan across every function in `ottoq`, `twin` and `public` returns ZERO callers. The lane→stall_type resolution that actually runs is the inline logic already inside `ottoq_book_workflow` and `ottoq_decide_tick` (4b); this function duplicates it and nothing invokes it. It is deliberately NOT dropped (never drop) and it is correct code — it is simply not wired in. Read this row as 'a resolver was written', not 'the resolver is this'.** `public.ottoq_plan_visit_itinerary` gained the new nullable `ottoq_vehicle_itineraries.visit_id` and a SCOPED guard: it still refuses to double-plan the visit an active itinerary already covers, but an active itinerary belonging to a DIFFERENT, superseded visit is now closed instead of blocking forever; its bay dwell also became a `service_cadence_policy.est_min_default` lookup instead of a hardcoded 20 min. `ottoq.ottoq_release_expired_bookings` gained a pre-arrival sweep that releases holds whose visit was superseded or whose window opened `prearrival_no_show_grace_min` (20) ago unused. Three kill switches, all default-on: `prearrival_bay_reservation`, `stale_itinerary_unblocks_replan`, `prearrival_no_show_grace_min`. **Nothing dropped; one nullable column added; no cuOpt, Nemotron, LP, escalation ladder, approval gate or geometry touched.** | Measured on run f86f9fda before writing a line: `ottoq_book_appointment`'s stall search was `('dcfc','l2')` then `'staging'` and NOTHING else, and its only pre-arrival hold was the `stalls.reserved_by` TTL flag — **zero rows on the forward calendar**. Every bay row in that run was written at or after the vehicle was physically in the depot, because the only callers of `ottoq_book_workflow` are `ottoq_decide_tick` and `ottoq_place_unplaced_vehicles`, both in-depot. So bays oversubscribed. | Claude Opus 4.8, via the Supabase **Management API** `POST /v1/projects/{ref}/database/query` (the file is 70 KB, over the ~50 KB re-emit threshold, and neither `psql` nor the Supabase CLI is installed on this machine). That path writes **no ledger row**, so the `supabase_migrations.schema_migrations` row was written by hand immediately afterwards, from the same file, via the same endpoint. `md5(statements[1]) = 8f6249b15921dbb5a365171dad33abc1`, byte-identical to the file at commit `6bbc3a8` — the bytes that actually ran. The committed file's own md5 differs only because the `migration-version:` stamp and the `migration-name:` header are added AFTER the apply, by design. | Applied clean: all pre-guards passed (0 running sim runs; four live md5s matched what the file was written against) and every post-assertion passed, including a totality sweep calling `ottoq_svc_to_stall_type` over all 15 active `service_cadence_policy` rows plus unknown and NULL input, and the `ottoq_book_workflow` delegate still returning the unchanged empty result. **Verification run `68dc660f`** (busy_day, seed 424242, 9 ticks, ~15 real-min, stopped per plan): **30 pre-arrival bay reservations written — against a baseline of 0, because the mechanism did not exist.** On the 26 measurable at the pre-stop read, **26 of 26 were booked 60.0 sim-min BEFORE the vehicle's actual arrival** (min = median = 60.0; the value quantises because the return ETA is a 30-min policy constant and the tick is 30 sim-min — the count is the proof, not the minutes). Of 17 needs cards carrying a `bay_reservation` block, 20 bays were needed and **20 booked, 0 unbookable, 0 hold_required**, anchor `live_eta` on all 17. **All 19 planned bay legs carried a `to_stall_id`**, so no second independent stall search can re-derive them. PROTECTED SET RE-MEASURED AFTER THE STOP: **0 real double-bookings** (full pairwise overlap scan over the run's bookings in the `held/active/done/interrupted` state set), **0 orphaned FKs** (booking→stall, booking→vehicle, leg→itinerary), DB **368 MB**, cron **10/11/12 ON, 13 OFF**, drift Sections **A, B and C all CLEAN**; Section D reports ottoq 48→51 (+3) which is exactly `ottoq_book_workflow_legs`, `ottoq_reserve_inbound_bays`, `ottoq_svc_to_stall_type` — the three routines this file adds and names. **NOT PROVEN, stated plainly:** no pre-arrival hold was observed being HONOURED on arrival (held→active→done) — the leads are 60+ sim-min and the run was deliberately short, so all 23 surviving holds were still `held` at the stop. Three were released early, all three by the PRE-EXISTING reconciler (`replanned_no_window` ×2, `no_show_grace_elapsed` ×1), not by this file's new sweep, which fired 0 times. |
| 2026-08-06 | `db/migrations/0009_honest_completion_and_eta.sql` (ledger version **20260806030248**, name `honest_completion_and_eta`) | **Two records that lied, fixed so the forecasting layer has something true to learn from. Nothing else.** No orchestration, LP, CSR build, cuOpt parse path, Gate B, `verify_jwt`, incident trigger, need-escalation or decision behaviour touched. Zero drops, zero routines added, no `VACUUM FULL`. **(1) `twin.ottoq_sim_advance_service_flow` — completion credit is now intersected instead of unconditional.** Every bay exit used to call `ottoq_wear_mark_serviced` over a **fixed array** chosen by *which bay* the car was leaving and nothing else: wash → 2 services, detail → 3, service bay → **4**. A car that went in for one calibration came out recorded as having had a PM, a calibration, a fault repair and a cosmetic repair. The credit set is now `bay capability ∩ (the atoms still outstanding on that vehicle's own visit ∪ the one service a technician's flag names)`, computed **before** `ottoq_mark_visit_atoms_done` flips those atoms to `done` — order is the whole fix. One `ottoq_wear_mark_serviced` call per real service, the same shape 0005 §3 already uses on the non-bay path and `ottoq_ingest_service_complete` uses on the real-telemetry seam. `ottoq_mark_visit_atoms_done` and all three bay capability arrays are left **byte-for-byte** (that call is already intersected by construction — it only touches atoms that exist). Empty credit set ⇒ `unnest` yields no rows ⇒ **no call at all**: total, cannot raise, cannot abort `decide_tick`. The bay's capability set and what was actually credited now ride on the **existing** `twin.service_completed` event (no new event type, write-rate budget undisturbed), plus one `twin.bay_credit_none` WARNING per call when a bay exit could credit nothing. **(2) `public.ottoq_vehicle_dispatches` + `twin.ottoq_sim_advance_deployed_telemetry` — plan, live estimate and actual are three separate facts.** New **`planned_return_at`**, a `GENERATED ALWAYS … STORED` column over `dispatched_at + planned_duration_min` (two values nothing ever updates), so the dispatch-time plan is **physically unwritable** rather than preserved by convention; new `eta_refreshed_at` (SIM clock) and `eta_source` stamped by **both** writers that move `scheduled_return_at`; and the plan / live-ETA / divergence written into `return_evidence` at the moment of the refresh. **The arithmetic of `scheduled_return_at` is unchanged** — every arrival time and every forecast reader sees the same number as before. | **(1)** The old array was unconditional, so it was wrong by construction on almost every exit. Measured on the whole visit ledger (**234 atoms across 80 visits, 12 distinct codes**): `exterior_wash` and `cosmetic_repair` appear **0 times, ever**, yet were credited on every wash/detail and every service-bay exit respectively; `fault_repair` appears **twice in the entire ledger** yet was credited on **every** service-bay exit. And `fault_repair` is the expensive one — in the 0008 body it sets `open_fault_codes := '{}'` and `worst_fault_severity := 99`. **0008 spent a whole section refusing to let a PM clear faults; the caller had been handing out `fault_repair` unconditionally the entire time.** 0005 registered this as its GAP 5 ("worth its own pass") and 0004 as GAP 4; this is that pass. **(2)** `scheduled_return_at` was one mutable column carrying **three different quantities** over a row's life — dispatch plan, delay-card-adjusted plan, return-handshake estimate — with nothing on the row saying which. Measured on all **435** dispatch rows: **116 of 116** rows that reached the handshake ended at `returning_started_at + exactly 30 min`, `count(DISTINCT return_eta_minutes) = 1` — there has never been a second ETA value. Grading a schedule against an arrival that schedule caused is circular, so there was no training signal. ⚠️ **The brief's "the plan is destroyed" premise is corrected in the file, not inherited:** `planned_duration_min` is written once and never updated (checked every function that mentions it), so the plan always survived *derivably* — 360 of 435 rows had `scheduled_return_at` moved off it. The real defect was narrower and is what got fixed. ⚠️ **ETA part (b) — a "real" ETA — is DECLINED with evidence, not deferred with a wink:** `public.ottoq_return_eta_minutes` is left byte-for-byte untouched (md5 `c59fb82f326a01158a3276c3906d0ec4`, re-verified after). The twin models no position (**0 of 5,523 telemetry packets carry a coordinate**), no route, no remaining distance, and the arrival is *defined* as `returning_started_at + return_eta_minutes` — the ETA **causes** the arrival rather than predicting it, so any "better" estimate would be graded against an outcome it authored. See GAP 6. | Claude, MCP `apply_migration` from the file committed at `71a1ea5` (header `PENDING`). **Byte-fidelity proven, not hoped for: `md5(statements[1])` on ledger row `20260806030248` = `cf8d4d4e936af69b16406c0a5b516b6a` = the md5 of the committed file, 109,799 bytes both** (the convention since 0003). ⚠️ **The FIRST apply attempt FAILED and is recorded rather than buried:** `ERROR 42P17: generation expression is not immutable` — `timestamptz + interval` is `timestamptz_pl_interval`, which is **STABLE**, and a generated column demands IMMUTABLE. The whole transaction rolled back and the database was verified byte-identical afterwards (0 snapshot rows, 0 new columns, both function md5s still the pre-image, 0 ledger rows). The **file** was fixed (never the database alone), re-committed, re-applied. The replacement anchors both conversions at UTC — no DST, no offset — which is exactly "add N minutes of absolute time"; equivalence **proven before committing**: 435 of 435 live rows agree, 0 of 435 intervals carry a day/month part, plus **16,940 synthetic comparisons** (3,388 timestamps × 5 session timezones incl. the half-hour and 30-minute-DST zones, across four 2026 DST transitions, 7 durations each) with **0 mismatches**. All pre-guards passed live before anything changed: `ottoq_sim_advance_service_flow` md5 `8797fe90392487701947ede78b93a768`, `ottoq_sim_advance_deployed_telemetry` md5 `595d96841d68f63714e153faa7dfddf0`, both defect strings present, single signature each, `dispatched_at`/`planned_duration_min` still NOT NULL, **0 runs `running`**. Pre-snapshots under `0009_honest_completion_and_eta_pre` (5 rows, 3 of them context-only), post under `…_post` (2 rows). Cron **10 and 12 paused inside the same transaction as each smoke** — so a failure could not leave the engine switched off — then restored and verified: jobs 2/10/11/12 active, 13 inactive, exactly as found; **job 12 (the start engine) never disabled**. | **Yes — both fixes proven at runtime through the real callers, and every touched row restored.** `scripts/check-drift.sql` run live after applying: **VERDICT CLEAN**, A/B/C/D all OK, ledger 629 rows, 8 files, 0 PENDING, **routine counts public 339 / ottoq 48 / twin 71 all match** (two `CREATE OR REPLACE`s, zero creates, zero drops — exactly the predicted blast radius). **CREDIT — the headline, on two cars with IDENTICAL fault state and opposite work orders, both driven through `twin.ottoq_sim_advance_service_flow` itself.** `Waymo-AV-034`, **one** service due (`mechanical_pm`): old code would have credited `[mechanical_pm, sensor_calibration, fault_repair, cosmetic_repair]`, new code credited **`[mechanical_pm]`** — `false_credits_prevented = 3`; `last_pm_at` **moved** (the control proving real work was recorded), `last_calibration_at` **did not**, and **`open_fault_codes` stayed at 2 with severity 1** — *the old code would have wiped that fault list and set severity 99.* `Tesla-AV-042`, **three** due (`mechanical_pm, sensor_calibration, fault_repair`): credited **all three**, `false_credits_prevented = 1` (`cosmetic_repair`, which was never due); PM moved, calibration moved, and faults went **2 → 0**, severity **1 → 99** — the legitimate clear still fires, so **the fix is not under-crediting**. **ETA — plan, live estimate and actual are now three observable facts.** A dispatch planned for `11:40` (dispatched 08:40 + 180 min) that signals return at sim `12:00`: `planned_return_at` **11:40 before → 11:40 after (survived, and still equals `dispatched_at + planned_duration_min`)**, `scheduled_return_at` moved `11:40 → 12:30` (the live estimate, the flat +30 policy constant), `eta_refreshed_at = 12:00:00` **SIM**, `eta_source = 'policy_constant:return_eta_minutes'`, and `return_evidence` carries `planned_return_at 11:40 / live_return_at 12:30 / plan_minus_live_min -50.0` — matching the independently computed `-50.0`. **V6, the guarantee proven rather than asserted:** `UPDATE … SET planned_return_at = now()` returns **`SQLSTATE 428C9 — column "planned_return_at" can only be updated to DEFAULT`**. All 435 rows have a non-NULL plan. ⚠️ **Deviation from the literal brief, stated plainly:** the brief asked that `scheduled_return_at` itself stop being overwritten. It is still overwritten *on purpose* — **eleven live functions read it as "current best arrival estimate"** (`ottoq_predict_arrivals`, `ottoq_inbound_forecast`, `ottoq_forecast_net_load`, `ottoq_evaluate_return_need`, `ottoq_agent_board`, …), so repointing it would silently change what every arrival forecast sees, which is a decision-behaviour change this brief forbids. The **goal** is delivered by a stronger mechanism: the plan lives in a column Postgres will not let anything write. **RESTORE was verified, not assumed:** both smokes ran inside a single transaction with a whole-schema guard — full-row `::text` comparison across 8 tables (credit) and 16 tables (ETA), **plus a row census over all 357 non-exempt `public` base tables** — and would `RAISE` and roll everything back on any divergence. It did exactly that twice during development (a generic `update_timestamp()` trigger re-stamping `updated_at`; two unlisted append-only log tables), which is how those were found. Final state: **0 rows diverged**, cron restored exactly, and the **only** disabled user trigger in `public` is `trg_ottoq_auto_incident_report` — the one that must stay disabled. **PROTECT re-measured:** DB **338 MB** (was 337), dispatch rows **435**, 0 running runs, `ottoq_return_eta_minutes` md5 unchanged, drift CLEAN. ⚠️ **NOT re-measured, deliberately:** tick seconds and B/event. Both need a live run and there were **0 runs `running`** throughout; idle ticks are not comparable. This file adds no index and no new event type on the happy path, so it is not the first suspect if either moves — but it has not been proven on a live run, and that run is Chase's call. **Residue, declared not hidden:** the smokes left **+15 `ottoq_events` rows** (32,889 → 32,904, of which **2 carry the new `credited` telemetry** — the durable V1/V2 evidence), **6 `public.vehicle_state_log` rows**, and the matching `ottoq_event_state_anchor` rows. All three are append-only by design; `ottoq_events` refuses DELETE, and removing an anchor would break rebuild-on-read for an event that necessarily survives, so they were **exempted deliberately** rather than quietly deleted. |
| 2026-08-06 | `db/migrations/0010_unify_depot_layout.sql` (ledger version **20260806223619**, name `unify_depot_layout`) | **The renderer's depot became the database's depot.** Geometry only — no orchestration, LP, Gate B, cuOpt, Nemotron, decision function, tick behaviour or approval gate touched, and **zero routines created, replaced or dropped** (the drift check's routine counts confirm: public 339 / ottoq 48 / twin 71, all unchanged). Per depot the layout goes **150 -> 160 stalls: staging 100->115, l2 35->30, dcfc 10, wash_bay 3, service_bay 2**, applied to **both** depots (`1111…` and `2222…`). Every stall was re-plotted onto the real 452.13 x 313.98 ft parcel (3.26 ac), replacing the 360 x 220 ft / 1.82 ac frame the database had recorded; `ottoq_site_structures` and the depots' `site_*` fields moved with it. **19 codes leave the layout per depot: 14 staging rows are RE-HOMED (UPDATEd onto surviving positions, keeping their `id` and therefore their whole history) and only 5 provably-unreferenced L2 rows are DELETED.** `absolute_point` was populated for the first time (was NULL on all 300 rows) and the 5 bays got width/depth for the first time (service 14x30, wash 14x35 — **estimates**, flagged as such in the file). `CANOPY-04` is marked decommissioned on paper only; `ottoq_canopy_state` is deliberately untouched, so solar output is byte-for-byte what it was. | The database's depot had never been drawn. When it finally was, it was not buildable: **54 overlapping stall pairs per depot (76 of 150 stalls sitting on top of another stall), 13 staging spaces inside the wash building, 4 inside the battery compound, 20 of 45 charging spaces with no drivable aisle, 2 stalls sharing one identical coordinate, 5 bays with no dimensions at all, and 300 of 300 rows with no map position.** The renderer's lot has 0 overlaps and a 24.8 ft main aisle and a human has reviewed it. **Losing 5 L2 chargers is the correction, not a side effect** — `ottoq_plan_overnight_wave` counts L2 stalls unfiltered, so every capacity figure measured before this migration used a denominator 5 chargers too generous. | Claude, from the file committed at `3a43042` (header `PENDING`). **Applied via the Supabase Management API `/database/query` endpoint with the file streamed byte-exact from disk, followed by an explicit `supabase_migrations.schema_migrations` insert — NOT MCP `apply_migration`.** This is a deliberate deviation and it is recorded here rather than hidden: the file is 99,951 bytes, and routing it through the MCP tool would have required reading it into and re-emitting it from a model context, which is both expensive and a transcription risk across 1,375 lines. Streaming it from disk removes the transcription risk entirely. **The ledger row is byte-identical to the committed file: `md5(statements[1])` = `cd5afc9bd8338259a023001aac6282d3` = the md5 of the file before the version-stamp edit, 99,951 bytes / 99,949 chars.** The endpoint runs the payload as a single implicit transaction — verified beforehand with an `ON COMMIT DROP` temp-table probe, which the migration requires since section 3 creates the seed temp table and sections 4-5 read it. **The apply halted once, correctly, and nothing was written.** Guard 1.4 refused: 8 stalls due to be retired still carried reservations (all on depot `2222…`: `NASH-L2-STALL-21..25`, `NASH-STG-B014`, `-N008`, `-N019`). All 8 were **stale and provably dead** — `status='available'`, `current_vehicle_id` NULL, `reserved_for_mission_id` NULL, and `reservation_expires_at` between 2026-07-27 and 2026-07-28, i.e. **9-10 days expired**, with **0 sim runs `running` or `paused`**. Table-wide, **62 of 62 reservations were expired and 0 were live**. The 8 were snapshotted to `public.preimport_released_reservations_20260806` and released through the sanctioned `ottoq.ottoq_release_stall_reservation(uuid,uuid)` — which clears only `reserved_by` / `reserved_at` / `reservation_expires_at` — then the guard re-read 0 blockers. The other 54 stale reservations were **left alone** as out of scope. | **The booking ledger survived, which was the whole hazard.** `ottoq_stall_bookings` **65 rows before, 65 after, 0 orphans before and after** — `stall_id` is `ON DELETE CASCADE`, so a bad delete would have erased rows silently with no error. `ocpp_sessions` **46 -> 46** and `vehicle_state_log` **2194 -> 2194**, both unchanged. All **28 re-homed rows (14 codes x 2 depots) kept their `id`s** and still resolve; all 28 are still `staging`; all 28 now carry a different `stall_code`, which is the re-home working (id and history preserved, position and code moved). **Counts: 160 = 115/30/10/3/2 on BOTH depots** (320 rows total). **0 NULL `absolute_point`, 0 NULL width/depth including the 5 bays.** Parcel **452.126 x 313.976 ft, 3.26 ac (derived 3.259)** on both depots. **`db/checks/depot_geometry_guard.sql` run live: checks 1-6 all PASS, and each geometry check reports `not_assessed = 0`, so nothing was skipped** — overlapping pairs **54/depot -> 0/depot** across **12,720 pairs compared per depot**; 0 stalls buried in a structure beyond the 5 whitelisted bays; 0 stalls outside the fence; parcel correct on both depots. Check 7 is report-only: 32 tight stalls = **16 codes x 2 depots**, all 10.00 x 15.67 ft, the founder-approved canopy pitch. **The JS guard's findings were tied to the live database rather than assumed:** the live `stalls` rows hash to **`89943752f32f98da4ef80b6baedb3174` on BOTH depots** — the exact seed md5 the generator printed and `checkLayoutGeometry.mjs` measured — so its aisle and lane results describe these rows, not just a file. It reports **all blocking checks passed**, tightest aisle **NASH-STG-E008 at 20.5 ft against a 20 ft one-way bar**, tightest charging aisle **NASH-L2-STALL-30 at 33.1 ft against 24 ft**. **Stall-vs-lane clearance is a WARN, not a PASS, and is reported as such**: the previous **2.41 ft encroachment into 18 E-column stalls is gone (no overlap anywhere)**, but the tightest margin, `NASH-STG-E006` vs the east avenue northbound, is **1.91 ft clear against the west avenue's 6.44 ft** — asymmetric margin, not an encroachment. **Units check — the metres/feet outage is NOT repeating.** Mean pairwise stall distance **144.64 -> 204.31 ft, +41.25%** (12,720 pairs per depot, identical on both), against a predicted ~204 ft / +41% from the parcel genuinely growing. A repeat of the 2026-07 outage would have shown ~3.28x (~474 ft); an unreached seed would have shown no movement. Max pairwise span 478.3 ft, consistent with a 452 x 314 ft lot. ⚠️ **`scripts/check-drift.sql` run live is NOT CLEAN — VERDICT `DRIFT` — and 0010 is not the cause.** The single Section A row is **`20260806030248 honest_completion_and_eta` (migration 0009), applied to this database at 03:02 UTC on 2026-08-06, hours before this apply, from branch `honest-completion-and-eta`, whose file is not on `unify-depot-layout`**. Sections **B (every committed file is applied), C (names agree) and D (routine counts) are all OK**. The drift resolves itself when 0009's PR merges to `main`; importing 0009's file into this branch would duplicate another PR's deliverable and is deliberately not done. |
| 2026-08-05 | `db/migrations/0008_soil_gate_and_retention_walk.sql` (ledger version **20260805230731**, name `soil_gate_and_retention_walk`) | **Two fixes for one disease: a routine that reported success while doing nothing, or the wrong thing.** No orchestration, LP, CSR build, cuOpt parse path, Gate B, `verify_jwt` or decision behaviour touched. Zero drops, zero routines added. **(1) `public.ottoq_wear_mark_serviced` — the soil copy is gated and the PM fault-clear is removed.** 0005 §2 shipped an **unconditional** `exterior_soil_level = COALESCE(round(v_soil,3), …)`. `soil_index` is a **SENSOR**-grime counter; `exterior_soil_level` is the **BODY**-soil grade the needs card bands at 0.45/0.65/0.85. Two different quantities that merely share a 0..1 range, so the copy overwrote a grade with a measurement of something else. Now only `p_service='exterior_wash'` may reset it, to `COALESCE(round(v_soil,3), 0)` — in the sim PART 1 has already zeroed `soil_index`, so the value still comes from the ledger; in production there is no wear row and 0 is the column's own clean floor. **A wash SHOULD clear body soil — the bug was the source and the scope, not the act.** Separately, `open_fault_codes := '{}'` and `worst_fault_severity := 99` narrowed from `IN ('fault_repair','mechanical_pm')` to `= 'fault_repair'` only: a PM is booked on mileage and fixes the PM sheet, not whatever DTC is open, so it must not claim a repair. **PART 1 and the whole of 0005 §3's inspection wiring are byte-for-byte unchanged.** **(2) `public.ottoq_retention_purge_worker` (4-arg) — the walk now follows the clock, not the ID.** 0006's walk was ordered by `event_seq` while its DELETE was predicated on `occurred_at`, so its stopping rule (`IF v_n = 0 AND v_batch_ts >= v_cut THEN EXIT`) was an *inference* that seq order is time order. New `public.ottoq_events_occurred_at_retention_idx` (plain btree on `occurred_at`) backs an oldest-first walk over closed time ranges `[v_from, v_batch_ts]`, advancing `v_from` by 1 µs each iteration. The early exit is gone — the only exit meaning "done" is `v_batch_ts IS NULL`, i.e. genuinely nothing older than the cut. End-of-pass housekeeping moved to that real terminus (0006 could unschedule the backlog job while a backlog remained). The 3-arg overload is untouched. | **(1)** The copy could **only ever ratchet a vehicle toward `ok`** on today's data: measured live pre-fix, `ottoq_vehicle_wear.soil_index` across **207 rows** ran min 0.0000 / max **0.3303** / avg 0.0880 with **0 rows at or above the 0.45 wash floor**, while `vehicle_need_profile.exterior_soil_level` across **116 rows** used the full range (max 1.0000, **48 ≥ 0.45**). **11 of 11 profiles the copy had touched were sitting below 0.45 — every one graded clean, with no wash bay involved.** 0005 §3 had just widened the callers to inspections, tidies, item retrievals and remote diagnostics, so the write went from "a bay exit" to "almost every completion in the depot". ⚠️ The brief's premise that the cap is *structural* is **wrong** — `twin.ottoq_sim_advance_wear_counters` clamps at `LEAST(1.0, …)`, the same range as the destination, so 0.33 is an empirical ceiling from today's soil rate and rainfall. 0005's own smoke test caught the copy going the **other** way (an inspection moving the column 0.199 → 0.480, inventing a wash). The defect is therefore *worse* than "a one-way ratchet": the direction of the error depends on the weather. **(2)** `event_seq` order is measurably **not** `occurred_at` order — **18 inversions in 23,697 adjacent pairs** — because `occurred_at` is the **SIM** clock and runs start at a random time of day. The early exit needs only ONE recent-looking row in a batch that deleted nothing to stop the whole pass. Demonstrated end-to-end pre-fix: with a 30-day-old row present and the real 7-day policy, the old worker processed exactly one window, deleted 0, exited, and left `count(*) WHERE occurred_at < cut` at 1. | Claude, MCP `apply_migration` from the file committed at `00307b2` (header `PENDING`). **Byte-fidelity proven, not hoped for: `md5(statements[1])` on ledger row `20260805230731` = `48c35f15cec95f7893afa5cb7e1df263` = the md5 of the committed file, 72,275 bytes both** (same convention as 0003–0006). All pre-guards passed live before anything changed: `ottoq_wear_mark_serviced` md5 `d10f848b66b204b539805869034cb456` (the 0005 body), the 4-arg worker md5 `815c91faf7b57fc7cd7ed906b8cfca3a` (the 0006 §7 body), both defect strings present, `ottoq_events.occurred_at` NOT NULL, all five target columns present. Pre-snapshots under `0008_soil_gate_and_retention_walk_pre` (3 rows), post under `…_post` (3 rows). Cron **10 and 12 paused for the smoke window, restored and verified** — all 5 jobs `restored_exactly = true`, job 12 active, job 13 left inactive as found, and every cron run in the window `succeeded` (19 on job 10, 37 on job 12, **zero failures**). | **Yes — both fixes proven at runtime, and every touched row reverted.** `scripts/check-drift.sql` run live: **VERDICT CLEAN**, A/B/C/D all OK, ledger 628 rows, 7 files, 0 PENDING, **routine counts public 339 / ottoq 48 / twin 71 all match** (two `CREATE OR REPLACE`s, zero creates, zero drops — the blast radius predicted exactly this). **RETENTION — the exact case that failed before now passes.** A probe row was planted 30 days old at `event_seq` **8697443**, which was simultaneously the **table's maximum** `event_seq` — the seq/time inversion in its purest form. Pre-call `count(*) WHERE occurred_at < cut` = **1**; after `CALL ottoq_retention_purge_worker(30, 2000, '7 days', ARRAY[…])` = **0**. Total events **23,699 → 23,698**, i.e. **exactly one row deleted and zero collateral**; `ottoq_sim_runs` 7 and `ottoq_run_archives` 46 both intact, and the 6 protected `production_live` runs untouched. A second call on the now-drained table deleted 0 and reset `pass_deleted` to 0 — the **drained** branch, not a budget timeout, which is the "terminates instead of guessing" property. **SOIL — all four behaviours proven on live rows, then reverted.** On vehicle `f0077a3a…` (body soil **0.857**, in the `overdue` band; 2 open faults, severity 1; wear `soil_index` **0.025**): a **`mechanical_pm` service-bay exit left `exterior_soil_level` at 0.857, `open_fault_codes` at 2 and `worst_fault_severity` at 1**, while `last_pm_at` moved 2026-06-01 → 2026-08-05 12:34:56 — the control that proves the call did real work rather than no-oping. *The old code would have written 0.857 → 0.025 (grading it `ok`), emptied the fault list and set severity 99.* A genuine **`exterior_wash` then reset the same column 0.857 → 0.000** and moved `last_wash_at`, while still **not** clearing the 2 faults. On vehicle `03726c33…` (soil 0.694, 1 fault, severity 3, wear `soil_index` 0.220) a **`fault_repair` correctly cleared faults 1 → 0 and severity 3 → 99 while leaving `exterior_soil_level` at 0.694** — the legitimate clear still works and does not touch soil. **All 5 touched rows (2 `vehicle_need_profile`, 3 `ottoq_vehicle_wear`) restored from `proof0008_*_prestate`, verified at 0 rows still diverged on a full-row `::text` comparison.** **INDEX COST, measured and NOT rounded away — it came in far CHEAPER than the file predicted:** 184 kB over 23,698 rows = **7.95 bytes/row against a forecast of ~24**, because `occurred_at` is the sim clock and the table holds only **182 distinct timestamps**, so btree deduplication collapses the duplicate keys. `ottoq_events` is now at 11 indexes as intended, and the DB is unchanged at **330 MB**. ⚠️ **NOT re-measured, deliberately: the ~3.7 s/tick and ~960 B/event protect numbers.** Both need a live run and there were **0 runs `running`** throughout; idle ticks are not comparable. §3 adds an index to the hottest table, so the tick is the one number this file could plausibly move — it is the first suspect if it regresses, and the index is droppable without breaking correctness (the exactness lives in the `ORDER BY`, not the index). That run is Chase's call. |
| 2026-08-05 | `db/migrations/0007_add_site_energy_snapshots_created_at_idx.sql` (ledger version **20260805032907**, name `add_site_energy_snapshots_created_at_idx`) | **Nothing — this is a retroactive record, not a change.** One btree, `idx_site_energy_snapshots_created_at ON public.site_energy_snapshots (created_at DESC)`, was applied to the live database at **03:29:07 UTC on 2026-08-05** by something that was **not this repo**. It wrote a ledger row and left no committed file. The file added here is copied byte-for-byte from that ledger row's `statements[1]` and is `IF NOT EXISTS`, so it is a no-op against the live database. **File number 0007 is LATER than 0006 while its ledger version is EARLIER** — 0006 was already committed and applied under that number when the orphan was found. File numbers order the repo; the `migration-version` header orders reality. | `scripts/check-drift.sql` was reporting it as **Section A CRITICAL drift** — "IN DATABASE, NOT IN REPO" — which is the one alarm this repo exists to raise. Writing the file is the drift check's own prescribed remedy. ⚠️ **The index is harmless; the fact is not.** Something applied DDL to the brain outside this repo about eleven hours before 0006. Worth finding out which agent or session, because the next one might not be a one-line index. | Claude. **Not applied by anything** — the index already existed. The file is documentation of a change someone else made. | **Yes, by construction.** `scripts/check-drift.sql` run live after committing it: **VERDICT CLEAN**, Section A now `none — nothing has been applied past the baseline without a file`. Before this file existed, the same script reported version `20260805032907` under A/CRITICAL. |
| 2026-08-05 | `db/migrations/0006_slim_writes_and_arm_retention.sql` (ledger version **20260805142711**, name `slim_writes_and_arm_retention`) | **Stopped the database re-writing the same fact, and armed a retention purge that had never deleted anything.** Storage and retention only — no orchestration logic, no LP, no CSR build, no parse path, no Gate B, no `verify_jwt`, no decision anywhere. Zero drops. **(A)** New `public.ottoq_events_slim_new_state()` + `BEFORE INSERT` trigger `ottoq_events_slim_new_state_bi` on `ottoq_events`: CUT 1 drops `new_state` when it is byte-equal to `payload->'new'` (the `.created` duplicate); CUT 2 keeps one full snapshot per (run, event_type, entity, UTC day) as an **anchor** in the new `public.ottoq_event_state_anchor` and drops `new_state` on every later `vehicle.state_changed`, which the new `public.ottoq_event_new_state(uuid)` rebuilds by folding `payload->'diff'->k->'to'` forward. The whole trigger body is inside one exception block — its failure mode is "we save nothing", never "the tick rolls back". **The column is NOT dropped** and pre-0006 rows are returned as-is, so the black box needs no cutover. **(B)** `ottoq_purge_prior_runs` now `PERFORM set_config('ottoq.retention','on',true)` — the one line that turns the silent no-op into a real purge — plus a widened never-delete list (`ottoq_run_archives`, `ottoq_schema_snapshots`, the retention cursor and both new policy tables, plus pattern protection for `ottoq_phase%`/`ottoq_cert%`/`%_evidence`/`ottoq_proof%`/…), a `status <> 'running'` guard on the per-table loop as well as the ledger delete, and honest reporting (`failed_tables` is now returned instead of swallowed). `ottoq_events_block_mutation` forgives **UPDATE** while — and only while — the retention flag is on. The 4-arg `ottoq_retention_purge_worker` replaces the quadratic `ctid` block-cursor with a bounded, index-ordered walk on `event_seq`. Two policy knobs seeded: **`ottoq_retention_policy` = 7 days, day-aligned** (chosen over "last 10–20 runs" because a time window bounds BYTES and a run-count window does not; per-run history is preserved forever by `ottoq_run_archives` anyway) and `ottoq_write_slimming_policy` (chain cut deliberately narrow: `vehicle.state_changed` only). **(§8)** autovacuum tightened to 2% + insert-driven on the six churn tables, `lz4` set on the three `ottoq_events` jsonb columns. | The database hit **14 GB** and stopped accepting connections; ticks ran 48–65 s against a normal ~1 s. Two causes. **(A)** `new_state` stored a complete re-photograph of the vehicle row on every state change — **measured live at 2,095 B of a 4,578 B row** — at ~2,400 event rows/minute (9,560 events in the 4 real minutes of run `83a12664`), i.e. roughly 400 MB per hour of running. **(B)** `ottoq_events` is append-only by trigger, with an escape hatch that only opens when the transaction-local GUC `ottoq.retention` is `'on'` — and `ottoq_purge_prior_runs` never set it, then swallowed the refusal into a `RAISE WARNING` nobody reads and returned `{"ok": true}`. **Retention has therefore never deleted a single event in the system's history.** | Claude, MCP `execute_sql` from a **byte-verified staging copy** — ⚠️ **this differs from 0002–0005 and the difference is deliberate; read it.** `apply_migration` takes its payload as retyped text, and a 72 KB file cannot be proven byte-identical that way *before* it executes. So the file was transferred in 10 chunks into `public.mig0006_source`, each verified by comparing the cumulative prefix md5 against the local file after every chunk, and only then executed — `EXECUTE` of the assembled text inside a guard that re-checks `md5(v_sql) = '4183de7939088fd252c5f05b19ad7893'` and aborts otherwise. The ledger row was written from that same verified text, so **`md5(statements[1])` = `4183de7939088fd252c5f05b19ad7893` = the md5 of the file as committed at `043f9aa` (header `PENDING`, trailing newline included)** — the identical convention as 0003/0004/0005, and here it is *proven* rather than hoped for. All content guards passed live before anything changed (purge without `ottoq.retention`, with `c_keep_tables`; exactly one 4-arg worker containing `cursor_block`; the append-only guard; the `event_seq` UNIQUE btree present). Pre-snapshots under `0006_slim_writes_and_arm_retention_pre` (4 rows), post under `…_post` (6 rows). | **Yes — both halves proven at runtime, and one new defect found. Cron 10 and 12 paused for the smoke window and restored + verified (all 5 jobs `restored_exactly = true`, job 12 active, job 13 left inactive as found; every cron run before and after the migration `succeeded`, zero failures).** `scripts/check-drift.sql` run live: **VERDICT CLEAN**, A/B/C/D all OK, ledger 627 rows, 6 files, 0 PENDING. **Routine counts public 337 → 339, verified BY NAME not arithmetic:** live 335 distinct names vs 332 in `db/baseline/functions_public.sql`, ADDED = {`ottoq_decide_indepot_approvals` (0002), `ottoq_event_new_state`, `ottoq_events_slim_new_state`}, **REMOVED = {}**. **(A) THE WRITE IS SLIMMER, measured A/B on byte-identical content** (same vehicle row, same payload, policy toggled between the two writes): `new_state` **2,095 B → NULL**, whole row **4,578 B → 2,479 B = −45.8%**; on the chain cut, anchor **2,550 B** vs the next event **453 B = −82.2%**. **Lossless both ways:** `ottoq_event_new_state()` on the slimmed `.created` row returned a value **jsonb-equal to the fat original**, and on the chain row folded the diff to exactly the expected state (`current_state` → `offline`). **Tamper-evidence intact: `payload_hash = ottoq_compute_event_hash(payload)` on all 9,561 rows, 0 mismatches** — §4 touches only `new_state`, which nothing hashes or signs. **(B) RETENTION DELETED ROWS FOR THE FIRST TIME EVER: `rows_purged: 6`, `retention_armed: true`, of which 5 were `ottoq_events`.** Bounded to zero collateral by construction — 6 of the 7 real runs are `run_by='production_live'` (protected by name) and the 7th was passed as `p_keep_run`, so the only purgeable run was a throwaway `smoke0006_retention` run created for the test. Verified after: kept run's events **522 → 522**, NULL-`sim_run_id` production events **9,039 → 9,039**, **`ottoq_run_archives` 45 → 45**, 6 production_live runs all still present. **§5.2 is LOAD-BEARING, not inert — the file's own GAP 2 is now closed with evidence:** `trg_ottoq_events_no_update` fires on **both** DELETE and UPDATE (`pg_trigger` read live), and purging an old parent event drove `ottoq_events_parent_event_id_fkey` (ON DELETE SET NULL) to **UPDATE a surviving child in a different run** — the child survived and its `parent_event_id` was nulled. Without the added `TG_OP='UPDATE'` branch that UPDATE would have raised and rolled the whole purge back. **§8 confirmed live:** all 8 autovacuum reloptions set on `ottoq_events`, `payload` compression = `l` (lz4). ⚠️ **NOT verified, deliberately: the ~4.3 s/tick number.** Idle ticks measure 0.006–0.031 s, which is not comparable — re-certifying 4.3 s needs a live run, and **starting one would now actually delete the last run's 9,561 events** (see the warning below). That call is Chase's, not mine. |
| 2026-08-05 | `db/migrations/0005_inspection_and_condition_resets.sql` (ledger version **20260805020029**, name `inspection_and_condition_resets`) | **Credited the work that actually happens, and reset the conditions the card grades on.** Three `CREATE OR REPLACE`, zero created, zero dropped. (§2) `public.ottoq_wear_mark_serviced` — PART 1 (`ottoq_vehicle_wear`) reproduced byte-for-byte; PART 2 keeps every 0004 line and gains the **condition columns**: `exterior_soil_level` := `round(soil_index,3)` read **after** PART 1 (a projection of the truthful ledger, not a per-service rule, so it is correct for every present and future service), `cabin_condition` := `'clean'` on `interior_deep_clean` and — only when it currently reads `'light_litter'` — on `interior_tidy`, `open_fault_codes`/`worst_fault_severity` := `'{}'`/**99** on `fault_repair`+`mechanical_pm` (99 is PART 1's own sentinel and lands on the card's `ELSE 0.00` = `ok`; **0 would land on `<= 3` and grade a repaired vehicle overdue forever**), `software_version` := `sw_target_version`, `item_retrieval_pending` := `false`. (§3) `twin.ottoq_sim_advance_visit_atoms` — **the missing sim caller**, one guarded `PERFORM public.ottoq_wear_mark_serviced(…, v_a->>'svc', COALESCE((v_a->>'ends_at')::timestamptz, p_clock))` inserted after the existing `ottoq_close_atom_leg` in the atom-done branch, in **its own `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING`** because this function has no top-level handler and an unguarded raise here unwinds the whole tick. The `readiness_check` and triage-`clear` branches are deliberately **not** credited (a check restores nothing; a cancellation is not a completion). (§4) `public.ottoq_run_boot_draw` — anchors `wear_km_applied`/`wear_km_applied_run` from `ottoq_vehicle_wear.drive_km_total` right after the seeder call, inside its own handler, and reports `odometer_watermarks_anchored` in the boot manifest. **Escalation still OFF (0 callers, dry-run). Not one interval, threshold or `must_do_at` retuned.** | 0004 moved 14 profiles, all completion-driven — and fleet health still got **worse across run 909: 75.9% overdue-or-critical at boot → 82.8% at the end, 0 vehicles ever `ok`**. Two measured causes, both the same two-ledger disease. **(A)** 0004's reset hangs off a **bay exit**, and most completed work never sees a bay: run 909 finished `interior_inspection` 90, `interior_tidy` 26, `item_retrieval` 5, `sensor_clean` 5, `remote_diagnostics` 5 — **all crediting nothing** (booking side: `inspect` 190 done vs wash+service+detail = 27). `interior_tidy` and `sensor_clean` are the sharpest: the correct reset **already existed** in PART 1 and was simply unreachable outside a bay. **(B)** The card grades cleanliness on a date **and** a condition, and 0004 moved only the date — so `99ddf4ff` left the detail bay with `deep_clean_ratio 0.000` yet still read `cabin_condition='soiled'` ⇒ `cabin_urgency='overdue'`. Fleet-wide, **116 of 116** had `exterior_soil_level <> soil_index` (mean 0.462 vs 0.217): **the read ledger claimed the fleet was 2.1× dirtier than the truthful one.** | Claude, MCP `apply_migration` (name `inspection_and_condition_resets`). **The ledger's stored statement is the committed file byte-for-byte** — 80,087 chars / 82,059 bytes / 1,278 newlines / md5 **`7aa8333336589a301d72c3d902dd70f7`**, equal to the md5 of the file as committed at `cfbf6bc` (header `PENDING`, trailing newline included). Same convention as 0003/0004. All **three** md5 guards matched live **before** apply, compared as `md5(rtrim(pg_get_functiondef(oid), E' \n\r\t'))`: `ottoq_wear_mark_serviced` `7fd07989…`, `ottoq_sim_advance_visit_atoms` `cb864b7b…`, `ottoq_run_boot_draw` `8981e6d3…`. Pre-snapshots under `0005_inspection_and_condition_resets_pre` (3 rows), post under `…_post` (3 rows); **pre ≠ post on all three**, confirming each was genuinely replaced. No chunked transfer needed. | **Yes for the wiring, end-to-end through the real sim caller.** `scripts/check-drift.sql` run live after apply: **VERDICT CLEAN**, A/B/C/D all OK, ledger 625 rows, 4 files, 0 PENDING. **Routine counts unchanged and verified live: public 337, ottoq 48, twin 71** — three `CREATE OR REPLACE` add no routine, so Section D needed no edit and **no name diff against `db/baseline/functions_public.sql` was owed**. **V1:** callers of `ottoq_wear_mark_serviced` went **3 → 4**, the new one being `twin.ottoq_sim_advance_visit_atoms`. **SMOKE (cron 12 paused for the window, then restored and verified `active=true`; job 13 left inactive as found).** Run 909's full pre-state preserved first into `public.smoke0005_*_prestate` (248 visit_needs, 116 profiles). **(A) INSPECTION → LEDGER, end-to-end:** an `interior_inspection` atom completed through `twin.ottoq_sim_advance_visit_atoms` (returned `1`) advanced vehicle `bfbaf0b2`'s profile **`exterior_soil_level` 0.199 → 0.480, exactly equal to `wear.soil_index` 0.480** — the two ledgers agree for the first time — with `updated_at` 2026-08-04 23:33:46 → 2026-08-05 02:03:39. Note it moved **upward**: this is a genuine *observation*, not a cleaning side effect, which is the whole of CORRECTION A. `done_at` = **2026-08-04T15:00:00**, the atom's own `ends_at` and not the tick clock, confirming §3's clock choice. **(B) CLEANED VEHICLE, urgency actually improves:** an `interior_deep_clean` completed the same way for `99ddf4ff` → `exterior_soil_level` **0.595 → 0.000**, `cabin_condition` **`'soiled'` → `'clean'`**, and on `ottoq_vehicle_needs_card` **`wash_urgency` `due_soon` → `ok`** and **`cabin_urgency` `overdue` → `ok`**. `overall_urgency` stays `critical` — honestly, on `tire_urgency`+`fault_urgency`, i.e. **exactly the dimensions GAP 2 names as deliberately unrestored**, not a failure of this file. **TOTALITY (the leg_type regression test):** `perimeter_walkaround` and `a_service_that_does_not_exist` both returned without error or warning. **IDEMPOTENCY:** after 3 further replays `exterior_soil_level` 0.000, `cabin_condition` `'clean'`, `odometer_km` 3653 — all unmoved. **All smoke writes then reverted from the prestate tables: 0 visit_needs and 0 profiles still diverged.** **NOT yet verified — needs the bounded run (≥139 sim-min, captured after STOP, SIM domain, per arriving vehicle):** V5 (profiles moved ≫ 0004's 14), V6 (`soil_diverged` collapses; `litter_zero_but_dirty_label` will **not** reach 0 — GAP 3, predicted in advance), V7 convergence **against the new run's own boot baseline, not 909's 75.9% and never 908's 84.5%**, V8 (`odometer_watermarks_anchored` = 116 at boot, `moved_off_seed` > 0 — §4's fix for 0004's inert odometer), and V9 the protect list re-measured. |
| 2026-08-04 | `db/migrations/0004_close_ledger_loop.sql` (ledger version **20260804232058**, name `close_ledger_loop`) | **Closed the ledger loop: a service completion now moves the table the decisions are read from.** Replaced `public.ottoq_wear_mark_serviced` — the existing `ottoq_vehicle_wear` UPDATE is reproduced byte-for-byte and its `FOUND` result captured into `v_wear_found` immediately after it (so the boolean contract is bit-identical for both callers), then a **second, independent UPDATE against `public.vehicle_need_profile`** was added: `last_wash_at` / `last_deep_clean_at` / `last_calibration_at` / `last_pm_at` per service code, `km_at_last_pm` := the **post-advance lifetime** `odometer_km` (NOT `wear.km_at_last_pm`, which is run-scoped and would read as ~0 lifetime km), and `odometer_km` advanced by the un-credited part of `wear.drive_km_total`. Wrapped in its own `BEGIN … EXCEPTION WHEN OTHERS` that `RAISE WARNING`s by name and lets PART 1 stand — it is reachable from the twin service flow inside a tick and must never abort one. Replaced `public.ottoq_ingest_service_complete` — **two changes only**: the `v_run.sim_run_id IS NOT NULL AND` half of the reset precondition was removed (it made the whole reset unreachable in real production, where no `ottoq_sim_runs` row is `running`), and `EXCEPTION WHEN OTHERS THEN NULL` on the reset loop became a named `RAISE WARNING`. Added 2 nullable bookkeeping columns to `vehicle_need_profile` (`wear_km_applied`, `wear_km_applied_run`) — the run-scoped watermark that makes the odometer advance idempotent, catalogue-only `ADD COLUMN`, no table rewrite. §5 issued a scoped data repair for the `GREATEST(0,…)`-clamped PM marks. **Escalation deliberately left OFF; not one interval, threshold or `must_do_at` value retuned.** | `public.vehicle_need_profile` is the table every due-date, urgency rung and `must_do` decision is computed from, and it was **written once at run boot from a seeded hash of the vehicle's UUID and never updated again**: 116 rows, **0 with `updated_at > drawn_at`**, one writer (`ottoq_seed_vehicle_need_profiles`, boot-only), zero UPDATE statements against it anywhere in `public`/`ottoq`/`twin`, zero triggers. Due dates could therefore only ever travel further into the past, so **overdue was the resting state, not an event** — 98 of 116 (84.5%) graded overdue-or-critical and **0 of 116 graded `ok`**, exactly inverting the founder's doctrine *"overdue is a failure state, not a trigger."* The correct reset code already existed and already fired on every completion from both seams — but wrote only to `public.ottoq_vehicle_wear`, a different table `ottoq_vehicle_needs_card` never joins (confirmed: the view's 24,588 chars match `vehicle_need_profile` and never match `ottoq_vehicle_wear`). Two ledgers; the truthful one was unread. | Claude, MCP `apply_migration` (name `close_ledger_loop`). **The ledger's stored statement is the committed file byte-for-byte** — 50,468 chars / **51,432 bytes** / md5 `207b385958331554fb239f3286829800`, equal to the md5 of the file as committed at `07e93f0` (header `PENDING`, trailing newline included). Same convention as 0003, not 0002 — see 0003's method note. Both md5 guards matched live **before** apply (`ottoq_wear_mark_serviced` = `6a34100c…`, `ottoq_ingest_service_complete` = `5b11e1b3…`, compared as `md5(rtrim(pg_get_functiondef(oid), E' \n\r\t'))`); pre-snapshots under label `0004_close_ledger_loop_pre` (2 rows), post under `0004_close_ledger_loop_post` (2 rows). File small enough for one payload — **no chunked transfer was needed**, unlike 0003. | **Yes for the wiring — and §5 is REFUTED, see below.** `scripts/check-drift.sql` run live after apply: **VERDICT CLEAN**, Sections A/B/C/D all OK, ledger 624 rows, 3 migration files, 0 PENDING. **Routine counts unchanged and verified live: public 337, ottoq 48, twin 71** — this migration adds ZERO routines (two `CREATE OR REPLACE`, one `ALTER TABLE`), so Section D needed no edit and no name diff was owed. **SMOKE — the ledger moved for the first time in the system's history.** Cron 10 and 12 paused for the window and **restored and verified `active=true` afterwards**. Vehicle `4b179b26-a48b-4f65-aad1-74ce3179a85d`, **with 0 sim runs `running`** — i.e. the exact production condition where the pre-0004 `v_run.sim_run_id IS NOT NULL` gate skipped the reset entirely. One call to `ottoq_ingest_service_complete(…, ARRAY['mechanical_pm'])` returned `ok=true, atoms_matched=1`, and the profile row moved: **`last_pm_at` 2026-07-12 13:00:00 → 2026-08-04 23:24:51.886608**, **`updated_at` 18:52:30.723751 → 23:24:51.886608 = 16,341.163 s past its own `drawn_at`, the first time any row in this table has ever had `updated_at > drawn_at` from a service completion.** `km_at_last_pm` 63,683 → **65,812 = exactly `odometer_km`** (lifetime scale, the §3 trap avoided). `odometer_km` correctly **unchanged** (no wear row in production ⇒ credit 0, no error, no guess) and `wear_km_applied` re-anchored to 0 with a NULL run. `last_wash_at` / `last_calibration_at` / `last_deep_clean_at` all **untouched** — the service→column map is selective and unmapped codes fall through. **Idempotency proven, not asserted:** 3 further replays moved `odometer_km` and `km_at_last_pm` by **exactly 0**. **CORRECTION A guard rail held:** `ottoq_vehicle_wear` negative `km_at_last_pm` **111 → 111**, `km_since_pm_negative` **0**, n=207 — nothing in this migration or its smoke wrote that table. Both replaced functions still md5-identical to the `0004_close_ledger_loop_post` snapshot, so the smoke changed no code. Evidence preserved in `public.mig0004_smoke_receipt` (3 phases, non-`ottoq` prefix so the next run's purge cannot eat it). **NOT yet verified — needs the bounded run (≥139 sim-min, captured after STOP, SIM domain, per arriving vehicle):** V4 convergence (does the 84.5% actually drain), and V6 (the protect list re-measured). |

### ⚠️ 0004 §5 IS A PROVEN NO-OP, AND THE FILE'S STATED PREMISE FOR IT IS FALSE

Found while verifying, reported rather than quietly patched. **The applied SQL is not being edited** —
the file must stay byte-identical to what the ledger holds. This is the correction of record.

- **§5 changed exactly zero values.** `laundered_rows_remaining` went **26 → 26**; `worst_pm_ratio`
  **1.41 → 1.41**; `pm_mark_above_odometer` **0 → 0**. The UPDATE matched its 26 rows and wrote each
  one the value it already had.
- **Why it is a no-op, and it is arithmetic, not luck.** All 26 rows have
  `odometer_km < pm_interval_km` (measured: **26 of 26**; odometers 805 – 8,713 km against intervals
  6,653 – 9,465 km). So `MOD(odometer_km, pm_interval_km) = odometer_km`, and
  `km_at_last_pm := odometer_km − MOD(odometer_km, pm_interval_km) := 0`. The repair is an identity
  on this population by construction.
- **The file's justification does not exist in this database.** §5 and CORRECTION A claim a laundered
  row reaching "169,957 km against a 7,816 km mean interval, a pm_ratio of 21x." **Measured
  `max(pm_ratio)` across all 116 rows was 1.41 BEFORE the migration ran**, and the largest odometer
  among the 26 clamped rows is 8,713 km. **There is no 21x row. Do not quote that figure.**
- **And there was nothing to fix.** These 26 are young vehicles that have not yet reached their first
  PM interval. For them `km_at_last_pm = 0` is *truthful*, not laundered — their pm_ratios are
  **0.10 – 0.96, every one under 1.0, i.e. not PM-overdue at all**. §5 was written against a defect
  that this data does not contain.
- **⚠️ IT CONTAMINATED V3's INSTRUMENT, so read V3 carefully.** §5 touched `updated_at` on all 26 rows
  without changing a value, so `count(*) FILTER (WHERE updated_at > drawn_at)` jumped **0 → 26 at
  migration time, before any service completed.** **V3's baseline is now 26, not 0.** The smoke test
  above is therefore reported as **26 → 27**, and the honest predicate for "a real completion moved
  this row" is the narrower `updated_at > drawn_at AND last_pm_at > drawn_at`, which reads **1** —
  exactly the smoke vehicle. Use that form in the certification run, not the raw count.
- **Blast radius: none.** Zero values changed, so no due date, urgency rung or `must_do` decision moved.
  The cost is a contaminated counter, documented here, not a behaviour change.
- **Still open (was GAP 3):** the `GREATEST(0, …)` clamp in `ottoq_seed_vehicle_need_profiles` is
  untouched and will redraw this population at the next boot. Since the clamped rows are provably
  *correct* for sub-first-interval vehicles, the honest next step is to **drop the §5 repair concept
  entirely** rather than carry it forward — it was solving a phantom.

| 2026-08-04 | `db/migrations/0003_bay_work_recovery.sql` (ledger version **20260804183836**, name `bay_work_recovery`) | **Gave bay work the second witness charging already had.** Replaced `public.ottoq_vehicle_needs_card` (new `owed` CTE reads the outstanding-work LEDGER, not the cadence clock; three new appended columns `owed_bay_svcs` / `owed_bay_min` / `rebook_owed`; `must_do_now` becomes cadence-due UNION ledger-owed, `deferrable_now` narrowed so the two can never contradict). Replaced `public.ottoq_decide_tick` (section 4b only: charge firewall narrowed by one three-clause exception, resumed work floored at urgency `due`, per-lane per-tick resumption budget `bay_resume_share_max`, and `is_resume` / `eff_urgency_rank` / `fresh_waiting_lane` / `resume_cap_lane` / `resumed_bay_work` stamped so recovery is countable straight off `ottoq_decisions`). Replaced `public.ottoq_indepot_reassignment_guard` (P1a/P1b: all **three** born-approved fast paths now stamp `decided_at` in the SIM domain and carry the same `payload.decision` audit object the auto-gate writes). Added view `public.ottoq_indepot_gate_latency` (P1b: the one canonical, never-mixed-domain latency measurement). One new policy dial catalogued (`bay_resume_share_max`, default 0.5). P1(c) eviction re-baseline recorded in-file as §7. | **Bay work recovered 0 of 5 interrupted jobs** (0 of 4 excluding the one genuine no-space refusal) while charging recovered 7 of 12. Root cause: `ottoq_decide_tick` (4b) picks bay candidates only from `must_do_now`, which was built from the CADENCE PROFILE alone — and the twin resets that profile when it credits a job "completed", in one measured case crediting a 35-minute service for 27 seconds of work. Charging recovers because `vehicles.current_soc` is a physical fact the engine re-reads every tick; bay work had no equivalent second witness, so the outstanding-work ledger said the work was owed while the cadence clock said the car was fine. Vehicle 5cee8fb3 logged 33 consecutive refusals to redeploy over 30 sim-min and was never once considered for a bay. | Claude, MCP `apply_migration` (name `bay_work_recovery`). **The ledger's stored statement is the committed file byte-for-byte** — 165,517 chars / 166,702 bytes / md5 `994a98708a25ad1c6a00e907f1c85ad6`, equal to the md5 of the file as committed at `a06cf4f` (header `PENDING`, trailing newline included). ⚠️ **METHOD NOTE, and it differs from 0002.** The file is 166 KB, too large for one MCP tool-call payload, so it was transferred in 10 run-length-encoded ASCII-safe chunks into `public.mig0003_transfer`, each chunk md5-verified on arrival, then decoded into `public.mig0003_decoded` and verified against the file's own md5 **before anything was executed**. `apply_migration` then ran a small guarded executor that re-checks that md5 and replays the file's 11 top-level statements (boundaries from a dollar-quote/string/comment-aware tokenizer, round-trip verified). The ledger row's `statements` was then set to the file's exact bytes, because that is the SQL that was actually applied to the brain; the executor shim is preserved verbatim in `public.mig0003_decoded` id=2. ⚠️ Also note **0002's ledger md5 is the file-with-`PENDING` *minus its trailing newline*** (100,846 chars, `b2f860…`) — confirmed by reconstruction. 0003's is the full file including the newline. The two are not computed the same way; do not compare them as if they were. | **Partly — V1/V2/V3 pass now; V4–V7 need a bounded run and are NOT yet done.** All guards matched live before apply: `ottoq_decide_tick` = `d279aa48…`, view `ottoq_vehicle_needs_card` = `4f42015a…` (both from `db/baseline/`), and `ottoq_indepot_reassignment_guard` against 0002's own post-snapshot — pre-snapshots recorded under label `0003_bay_work_recovery_pre`, post under `0003_bay_work_recovery_post`. **V1** ledger detector live on 116 vehicles, `charge` leaked into `owed_bay_svcs` **0 times** (0 owed at rest — no run active, so the detector is proven sane, not yet proven to fire). **V2** `must_do_now` ∩ `deferrable_now` contradictions **0**. **V3** gate latency now measurable in ONE domain: sim **46 rows, min 3.51 / avg 8.19 / max 8.80 sim-min** (matches the dial `indepot_reassign_auto_decide_min=8`); real 58 legacy rows; `unmeasurable` **2** — exactly the two pre-0003 born-approved rows the migration predicted would land there. The bogus "min 0.01 / max 22.18" mixed-domain range is gone by construction. **Routine counts unchanged and verified live: public 337, ottoq 48, twin 71** — this migration adds ZERO routines (two `CREATE OR REPLACE` functions, one replaced view, one new view), so Section D needed no edit. `scripts/check-drift.sql` run live after apply: **VERDICT CLEAN**, Sections A/B/C/D all OK. **NOT yet verified — needs the bounded run (≥139 sim-min, captured after STOP, SIM domain, per arriving vehicle):** V4 (the P0 number — bay work actually re-booked into a bay it holds), V5 (fresh arrivals not starved), V6 (charge path flat), V7 (0002 protect list intact). V8 (the decider's approve branch / still-barred readmit branch) remains an open coverage gap. |
| 2026-08-04 | `db/migrations/0002_approval_gate_decider.sql` (ledger version **20260804140958**, name `approval_gate_decider`) | Made the in-depot reassignment gate **answer** instead of absorb. Added `public.ottoq_decide_indepot_approvals` (OTTO-Q's own policy decider: 8 sim-min latency, 30 sim-min hard expiry backstop that runs even when the kill switch is off). Replaced 5 existing functions: `public.ottoq_indepot_reassignment_guard` (stamps a sim-domain `requested_at_sim`/`decide_after_sim`/`expires_at_sim` trio + `clock_domain` tag into the payload, real columns untouched), `twin.ottoq_opportunistic_scan` (type-filtered to the twin's own approvals so a doctrine gate is never settled by a seeded coin flip), `ottoq.ottoq_readmit_reopened_needs` and `ottoq.ottoq_readmit_resumed_visits` (approval exclusion **narrowed**, not removed — the gate still blocks genuine changes of plan and is ignored only for the vehicle's own fault deferral; emitted doctrine string corrected to match the code), and `twin.ottoq_sim_vehicle_exception_handler` (technician flip now resolves the ledger row in the same transaction; `auto_staged` path resolved; **tow-retrieval clock domain fixed**; named top-level safety net added). 3 new policy dials catalogued. | `ottoq_ops_approvals` was an **absorbing state** for `indepot_reassign`: 55 pending rows, none ever decided, none able to expire. Root cause was a clock-domain mismatch — the guard stamped `decide_after`/`expires_at` with `now()` (REAL) while the only decider compared them to the SIM clock, 325 min apart in the measured run, so the rows were neither decidable nor expirable. Those rows then barred **27 of the 29 vehicles (93.1%)** holding cut-short work from readmission, which is why cut-short work was never re-booked: 0 of 38, then 0 of 52, three phases running. Paired defect: tow retrieval compared REAL `last_state_change` to the SIM clock and fired **zero** times, leaving broken vehicles unable to leave. | Claude, MCP `apply_migration` (name `approval_gate_decider`). **Applied byte-for-byte from the committed file** — the ledger's stored statement md5 `b2f860822b7df8037967962def5ed03d` equals the md5 of the file's exact contents, so the database and the repo hold identical SQL. | **Yes — the absorbing state is gone, measured, not asserted.** All 5 md5 guards matched live before apply. One call to the new decider resolved **55 of 55** stranded rows (`rows_resolved=55`); every one carries an audited `payload.decision`. **V1**: `indepot_reassign` now 55 `declined` + 3 `approved`, **0 pending**, 55/55 with a decider stamp and an audited decision, all with reason `atomic_visit_protected_bounded_defer_budget_governs` (the deferral verdict is DECLINED by design, so the eviction machinery and the phase-11 baseline are untouched). **V2** stuck-beyond-ceiling = **0**. **V3** technician-approved contradictions = **0**. Nemotron doctrine held: `copilot_recommendation='unavailable'`, `copilot_binding=false` on all 55 — it was unreachable and the system still decided, conservatively. **Readmission reachability on the real population** (denominator: the 29 vehicles holding an open reopened need with ≥1 atom not done/cancelled), OLD rule vs NEW rule evaluated against the preserved pre-fix approval states: barred **27 → 0**, reachable **2 → 29** (reproduces the diagnosed 93.1% exactly). **Tow clock fix**, evaluated with the real clocks of run `73f53ed5` (real ran **325.3 min ahead** of sim): old predicate `false` (never fires — matches the observed zero retrievals), new predicate `true` in both the sim and the legacy-real branch. `scripts/check-drift.sql` run live after apply: **VERDICT CLEAN**, Sections A/B/C/D all OK. Cron 10 and 12 left **active throughout** (no pause was needed) — 4/4 and 7/7 runs succeeded post-migration, zero failures. **NOT yet verified — needs the bounded run:** V4 (cut-short work actually re-booked into a space), V5 (tow retrieval firing end-to-end), V6 (phase-11 baseline re-cert). Those need ≥139 sim-min captured after the run is stopped, per the file's own §10. |
| 2026-08-04 | `db/baseline/` (snapshot, not a migration) | Captured the whole live brain into files for the first time: 336 `public` + 48 `ottoq` + 71 `twin` user-defined routines (455 total), tables, RLS policies, cron jobs, and the 27 deployed edge functions. Established `db/migrations/`, `scripts/check-drift.sql`, `scripts/gen-drift-sql.sh`, `scripts/APPLYING.md`, and this log. | The brain existed only inside Supabase with no source repo. The live ledger held **621 applied migrations**; the founder's working folder held 80 migration files and **none of the 80 appeared in the live ledger**. There was nothing to review, revert, or diff. Baseline = the line from which "if it isn't a committed file, it didn't happen" starts being true. | Claude (read-only session) | **Yes.** Counts re-verified live against `pg_proc`/`pg_depend` on 2026-08-04: public 336, ottoq 48, twin 71, ledger 621 rows, 27 edge functions all ACTIVE — all five match the export exactly, zero drift. `scripts/check-drift.sql` run live the same day: **VERDICT CLEAN**, Sections A/B/C/D all OK. Alarm proven to fire via a negative test (moved baseline cut + wrong name + phantom file → 2 CRITICAL drift rows, 1 CRITICAL name mismatch, 1 WARN orphan, 1 INVESTIGATE count diff). |

---

## ⚠️ THREE THINGS 0006 LEAVES OPEN — read before the next run start

Found while verifying 0006, reported rather than quietly patched. None of these were introduced by
0006's own edits; two are pre-existing and one is a real limitation of 0006's new retention walk.

### 1. ⚠️ THE NEXT RUN START WILL, FOR THE FIRST TIME, ACTUALLY DELETE THE PRIOR RUN'S EVENTS

`ottoq_start_demo_run` calls `ottoq_purge_prior_runs`, and until 0006 that call **silently deleted
nothing from `ottoq_events`** — the append-only trigger refused it and the handler swallowed the
refusal. It is now armed and it works: measured `rows_purged: 6` on a bounded test. **So the
long-standing habit of "just start a run" now has a consequence it never had before.** As of this
writing that means the **9,561 events of run `83a12664`** (the post-recovery verification run) are
deleted the moment anyone starts a new run.

**Preserve anything you still want into a NON-`ottoq`-prefixed table first.** This is not a defect —
it is the fix working — but it is a sharp edge that did not exist yesterday.

### 2. THE NEW RETENTION WALK CAN STOP EARLY AND LEAVE GENUINELY OLD ROWS BEHIND

§7 replaced the quadratic `ctid` cursor with an ordered walk on `event_seq`, which is correct and is
a real improvement. But its early-exit —

```
IF v_n = 0 AND v_batch_ts >= v_cut THEN … EXIT; END IF;
```

— assumes **`event_seq` order is `occurred_at` order**. That assumption is already false in this
database, and it was measured, not theorised:

- **11 seq/time inversions** inside run `83a12664` alone (9,559 adjacent pairs compared).
- **Demonstrated end to end:** with a 30-day-old row present and the real 7-day policy, one call to
  `ottoq_retention_purge_worker(30, 2000, '7 days', …)` processed exactly **one** window
  (watermark advanced `8673216 → 8675216`), deleted **0**, and exited — while
  `count(*) WHERE occurred_at < cut` stayed at **1**. The armed worker reported success and deleted
  nothing, which is the *same shape* of failure 0006 set out to end, for a new reason.

**Why it matters beyond the test:** `occurred_at` is written from the **SIM clock**, and per the
arrival/tick doctrine a run starts at a **random time of day**. A run whose sim clock sits behind the
previous run's writes old-timestamped rows at high `event_seq`, and the walk exits before reaching
them. The ordinary case (old runs at low seq) works fine.

**Not fixed here — out of 0006's scope, and the fix is a judgement call:** either drop the early exit
and let `p_time_budget_s` bound the pass, or drive the walk from an index on `occurred_at` (which does
not exist — see the file's own §1 (3): the only `occurred_at` indexes are one partial and one BRIN).

### 3. THE PURGE LOOP TRIES TO DELETE FROM VIEWS, AND NOW SAYS SO OUT LOUD

`ottoq_purge_prior_runs` picks its targets from `information_schema.columns`, which lists **views** as
well as tables. Three of them have a `sim_run_id` column, so every purge has always issued a `DELETE`
against them and always failed:

`ottoq_active_sim_runs`, `ottoq_approach_band`, `ottoq_booking_why_v1` — all confirmed `relkind='v'`.

This is **pre-existing, not a 0006 regression**. What changed is that 0006 stopped swallowing the
failures, so they now surface as `"ok": false` with a named `failed_tables` array. **Read `ok: false`
here as "the honest reporting works", not as "the purge broke"** — the row counts confirm the real
tables were purged correctly. The one-line fix (restrict the loop to `information_schema.tables` /
`relkind='r'`) belongs in its own migration.

---

## Smoke test for 0003 — a cut-short BAY job took a bay, for the first time

Targeted, bounded, and **not** a substitute for the V4–V7 certification run (still owed).
Run `0003bee5-0000-4000-8000-000000000003`, depot `1111…1111`, sim clock 16:00:00.
Cron 10 and 12 were paused for the window and **restored and verified afterwards**
(both `active=true`; 16 subsequent runs of job 12 and 8 of job 10, **all `succeeded`,
zero failures**). Evidence preserved in `public.mig0003_smoke_receipt_decisions` /
`…_bookings` — the `ottoq_*` originals are purged when the next run starts.

**The scenario reproduces the exact deadlock, not a convenient one.** Vehicle
`00cc3d0a` was given a detail-bay job that had been booked, started at 15:40 and cut
short at 15:41:25 (`cut_short_at` + `reopened_at` on the atom, `meta->'reopen'` on the
row) — and its cadence profile was then reset to *spotless*, which is what the twin
does when it credits an interrupted job as complete. Battery was set to 99% so the
charge path was never involved: this tests the bay lane and nothing else.

| Check | Result |
|---|---|
| **The card, pre-0003 behaviour** | `cabin_urgency = 'ok'`, `overall_urgency = 'ok'` — the cadence clock says the car is clean. Before this migration `must_do_now` would have been empty and the bay loop would never have looked at it again. |
| **The card, with the ledger detector** | `must_do_now = {interior_deep_clean}`, `owed_bay_svcs = {interior_deep_clean}`, `owed_bay_min = 25`, `rebook_owed = true`, `must_do_legs = {detail}`. **The second witness fires while the cadence clock still says "ok" — that is the whole fix, observed directly.** |
| **The decision** | one `decide_tick` call: 2 built, 2 enacted, **0 errored**. The bay row: `resolved_action_context='stall_assignment'`, `outcome='enacted'`, `source='needs_card'`, `need='interior_deep_clean'`, **`resumed_bay_work=true`**, `resumed_need='interior_deep_clean'`, `bay_booked=true`, `is_resume=true`, **`eff_urgency_rank=3`** (the `due` floor doing its job, against a cadence urgency of `ok`), `fresh_waiting_lane=0`, `resume_cap_lane=1` (budget correctly did not bite with no fresh competition). |
| **The bay it HELD** | a real `wash_bay` stall, `purpose='detail'`, `state='held'`, window **16:00 → 16:25 = 25.00 min** (matching the 25 owed minutes), `booked_at_sim` in the SIM domain, `source='needs_card'`, `need_code='interior_deep_clean'`. Not a paper booking: `stalls.current_vehicle_id` = the vehicle, `vehicles.current_stall_id` = the stall, `current_state='in_detail_bay'`. |
| **Cleanup** | vehicle released through the engine's own `ottoq_release_vacated_spaces`; 0 bookings left open, 0 bays left occupied, 0 runs left running, synthetic ledger row deleted. All four replaced objects still md5-identical to the `0003_bay_work_recovery_post` snapshot, so the smoke test changed no code. |

**What this does and does not prove.** It proves the mechanism end to end — ledger
detector → card → `decide_tick` (4b) → enacted assignment → a booking that holds a real
bay — on the precise failure shape that measured 0-of-5. It does **not** yet give the
P0 rate under load, nor V5 (fresh arrivals not starved), V6 (charge path flat) or V7
(0002 protect list intact). Those still need the ≥139 sim-min bounded run.

---

## Certification run for 0002 — V4/V5/V6 closed, and the falsifier resolved

Run `093c20f4-cf2d-4a6c-ab1c-693b98e51c0c` (seq 906), `busy_day`, seed 424242, live/3.0x,
depot `1111…1111`. **179.781 sim-min** (13:00:00 → 15:59:46.847), 733 ticks, auto-stopped by
the cron-12 metronome at its 60 real-min ceiling. Protocol floor of 139 sim-min cleared by
40.8 min. Run started 14:21:57 UTC, **after** the migration was applied at 14:09:58 UTC.
Evidence preserved in `public.phase13_*_424242`. All figures below were **re-derived
independently from live rows** in an adversarial pass, not copied from the capture harness.

| Test | Result |
|---|---|
| **V4 — cut-short work re-booked into a space it HELD** | **PASS, first time in four phases. 7 of 17 = 41.2%** (baseline 0 of 38, and 0 of 52 the phase before). Charging subset **7 of 12 = 58.3%** (baseline 0 of 33). Measured in the SIM domain (`b2.booked_at_sim > interrupted.released_at`). Every one of the 7 genuinely occupied its stall: held durations **12.2 / 15.0 / 27.5 / 36.0 / 52.5 / 72.0 / 79.3 sim-min** (median 36.0), clipped to the run window — 3 ended `done` / `window_elapsed_occupied`. None is a paper booking. The phase-11 failure mode "attempt fires, then finds no window" went **14 of 38 → 0 of 17**. |
| **V5 — tow retrieval fires end-to-end** | **PASS.** `vehicle.tow_retrieved_staged` fired **11 times**; it had never once fired. The paired sim-vs-real clock fix is proven at runtime. |
| **Approvals — absorbing state gone** | **PASS, in-run.** 48 `indepot_reassign` created, **48 decided, 0 pending, 0 expired** (baseline 7 created, 0 decided ever). Table-wide across all types and all runs: **0 pending**. No vehicle ends the run waiting on an unanswered question. |
| **Gate latency is non-zero (not cosmetic)** | **PASS with a caveat.** 44 of 48 went through the auto-gate with real latency: min 3.51, **median 8.28**, max 8.80 sim-min, computed purely in-domain (`payload.decision.decided_at_sim − payload.requested_at_sim`). Median lands on the dial `indepot_reassign_auto_decide_min = 8`. **Caveat:** 2 of 48 are *born-approved* at zero latency by the pre-existing `vehicle_fault_critical_immobilizing` safety carve-out — a deliberate safety path, not the decider. See the defect note below. |
| **Nemotron doctrine** | **HELD.** `copilot_seen=false`, `copilot_binding=false`, no `payload.copilot` on all 48. Nemotron was unreachable and the system still resolved every row, failing **closed** (decline = protect the atomic visit). Nemotron decided nothing. |
| **Re-routes still gated** | **HELD.** The readmit exclusion was narrowed, not removed: a pending `tech_greenlight`, or a pending `indepot_reassign` that is *not* a fault deferral, still bars readmission. Only the vehicle's own fault deferral is ignored. This run contained **0 genuine re-routes** (all 48 were `deferred_awaiting_tech` or `critical_immobilizing`), so nothing slipped through — but see the coverage gap below. |
| **V6 — protect list** | See below. Both hard-fail triggers pass. |

### Protect list, re-measured independently

**0 double-bookings** over 361 in-scope bookings (`held/active/done/interrupted`, self-joined
on `stall_id` with `&&` on `during`) — reproduced with my own query, not the harness's.
**0 starvation**: 7 of 112 arrivals returned under 85% SoC (min 79.9) and **none** ended the
run without a charge booking. Emission invariant **1.000** (17 interrupted bookings ↔ 17
`booking_interrupted` events). Phantoms **0 of 254**, reverse coverage **100%**,
inspection-zone parking **0.0%**, bay no-show **1 of 49** with grace verified still at 15.
Arrivals 112 vs baseline 117 (−4.3%) — a passed workload gate, not a collapse.

### The migration's own falsifier — RESOLVED, it did not trip

§10 V6 of `0002` said: if `gate_mode='deferred_awaiting_tech'` evictions start destroying
minutes, "the assumption is wrong and this migration is wrong." They went **0.00 → 245.37
minutes**. That looks damning and it is **not** a regression:

- **Phase 11 stamped a hardcoded zero.** In `phase11_eviction_evidence_424242`, all **45 of 45**
  deferred evictions carry `interrupted_with_min_remaining = 0.00` — while **38 of them** are
  `was_mid_service = true`. Forty-five mid-service evictions each destroying *exactly* zero
  minutes is not a measurement; it is a constant. §0002 line 1396 documents the constant and
  removes it.
- **The workload did not shift.** Mid-service deferred evictions: **38 in phase 11, 38 in this
  run.** Identical. What changed is that the number is now computed from the booking's real
  remaining window instead of being stamped `0`.
- **`declined` and `pending` really are indistinguishable to the eviction path.** The only
  status test in the resume branch is `a.status = 'approved'`; both `pending` and `declined`
  fail it identically and fall through to the same budget check. The migration's stated
  assumption **holds** on code reading.

**Therefore the two "regressed" protect figures are not comparable to their baselines.**
"Evictions cutting live work 20.3% → 31.8%" and "gate protection 90.0% → 84.4%" both have
numerators that depend on `interrupted_with_min_remaining > 0`, which phase 11 forced to zero
on the deferred path. Phase 11's true figures were worse, not better — reconstructed at
~1,359.78 destroyed minutes against **276.78** now. The honest read is a **−56.8% improvement
in minutes destroyed**, and the first trustworthy version of the rate. These two figures should
be **re-baselined off this run**, not treated as a regression.

### Open items — not blockers, but do not let them rot

1. **Zero-latency safety carve-out (2 of 48).** `vehicle_fault_critical_immobilizing` inserts
   its approval already `approved`, `decided_at = now()`. Defensible — a vehicle that cannot
   safely continue in place should not wait 8 minutes — but it stamps `decided_at` in the
   **REAL** domain while `payload.requested_at_sim` is **SIM**, and it writes no
   `payload.decision` object. Consequence: any latency computed as
   `decided_at − requested_at_sim` on those rows is meaningless. That is exactly where the
   capture harness's reported "min 0.01 / max 22.18 sim-min" came from — **both are
   clock-domain artifacts and should not be quoted.** Fix: stamp `p_sim_clock` and add the
   audit object, same shape as the auto-gate.
2. **Untested branches.** The decider's *approve* path (`zone_c_reopener_*`) and the narrowed
   readmit gate's *still-barred* path were both **unexercised** — this run produced no genuine
   re-route. Needs a scenario that raises an `indepot_reassign` for optimisation or congestion.
3. **cuOpt share is not certifiable from this run.** The harness's own supply-honesty invariant
   reads `cuopt_free_stall_undercount = 36 of 78` against a target of 0. Unrelated to this
   migration; do not quote 36.6%.

---

## Certification run for 0003 — the P0 headline is REFUTED, the two P1 fixes are PROVEN

Run `fc1ad933-c5c2-44e1-9cd1-e724a81d3d3e` (seq 908), `busy_day`, seed 424242, playback `live`,
`speed_x` 3.0, `run_by` operator_demo, depot `1111…1111`. Sim window 13:48:00 → 16:48:10.352 =
**180.173 sim-min** (protocol floor 139 cleared by 41.2), 781 ticks, 60.01 real-min, **109 arrivals**
(baseline 112, −2.7%). Auto-stopped by the cron-12 metronome at its 60-real-minute ceiling; cron 12
and 10 were never paused. **All numbers captured after `status='completed'`, SIM domain only.**
Evidence preserved out of the `ottoq_*` namespace: `phase14_bookings_424242` (736),
`phase14_decisions_424242` (10,738), `phase14_dispatches_424242` (112), `phase14_visit_needs_424242`
(110), `phase14_approvals_424242` (63), `phase14_gate_latency_424242` (51), `phase14_runrow_424242`,
and the full narrative in `phase14_cert_424242` (20 rows).

### V4 — ⭐ THE P0 NUMBER: **BAY WORK RECOVERED 0 OF 2. THE HEADLINE DOES NOT HOLD.**

2 bay bookings reached `state='interrupted'` (detail ×2); **0** received a replacement — unchanged
from the baseline's 0 of 4. The migration's own instrument is unambiguous: across 10,738 decisions
and 781 ticks, **`resume_candidates_seen = 0` and `bay_work_resumed = 0`. The new code path never
executed once.**

**Root cause, isolated to two independent gates — both measured, not assumed:**

1. **The `must_do` clause in the §3 ledger CTE.** Of 107 open/in-progress `ottoq_visit_needs` rows,
   **32** carry interruption evidence, **14** of those have a bay-lane atom, **13** still have that
   atom open — but only **1** has `must_do = true`. The owed bay atoms are `mechanical_pm` ×8,
   `interior_deep_clean` ×3, `sensor_calibration` ×2, `cosmetic_repair` ×1 — all `must_do = false`;
   the lone `fault_repair` is the only one that qualifies. `AND COALESCE((a.value->>'must_do')::boolean,
   false)` collapses the detector **14 → 1, a 93% loss.** The file states this as doctrine
   ("deferrable work does not get to jump a scarce bay"); the measured consequence is that the P0 fix
   is inert for 12 of 13 real cases.
2. **The `staged_awaiting_service` gate in `ottoq_decide_tick` (4b).** The one surviving candidate
   (`96b10b07`, `fault_repair`, `must_do=true`, `owed_bay_min` 114) spent the rest of the run in
   `charging_l2` and never entered `staged_awaiting_service`, so the bay cursor never saw it. The
   migration's own charge carve-out could not rescue it either — that exception requires **no free
   charger**, and this vehicle was *on* one.

**Honest denominator:** of the 2 bay interruptions, `b2222222` released as
`vehicle_fault_eviction_deferred_resumed` and has **zero** open needs rows — its work resumed in
place, nothing was owed. The genuinely-lost bay denominator is therefore **1**, and recovery is 0 of 1.

**The numbers were not flattered by shrinking the denominator.** Bay jobs *started* = 46 bookings /
109 arrivals = **0.422 per arrival** vs the baseline's 49 / 112 = **0.4375**. Flat. Bay work was
attempted at the same rate; the smaller interruption count (2 vs 4) is workload variance.

### V5 / V6 — fresh work not starved; charge path not moved by this migration

`fresh_displaced_by_resumed = 0`, but **trivially** — no resumed row was ever enacted, so the
anti-starvation budget (`bay_resume_share_max`, resolved live at 0.5) is **untested at runtime.**
13 fresh bay admissions enacted, 5 refused on genuine scarcity.

Charging recovery is **not re-certified and not a regression**: 7 of 16 same-purpose = 43.8% (baseline
7 of 12 = 58.3%); on a same-class basis (dcfc↔l2 both count) 10 of 16 = 62.5% (baseline 8 of 12 =
66.7%). The **numerator is identical (7)**; the denominator grew. Not attributable to 0003 — a
line-level diff of the pre/post `ottoq_decide_tick` snapshots shows **all 22 removed lines lie inside
section 4b**, with nothing removed from the charge path, cuOpt, or Gate B, and
`ottoq.ottoq_enact_space_assignment` still hard-refuses `dcfc`/`l2`. All 7 replacements genuinely
**held** their stall: 2.21 / 9.91 / 24.33 / 34.54 / 35.79 / 49.40 / 56.87 sim-min, median 34.54.
**There was not one bay replacement to trace.**

### P1(a) and P1(b) — both CONFIRMED FIXED at runtime

- **P1(a)** 4 born-approved fast-path rows, **all** `clock_domain='sim'`, **all**
  `has_audit_object=true`, reason `zone_c_reopener_vehicle_fault_service_cannot_continue_in_place`,
  `decided_at_sim == requested_at_sim`, latency 0.00. Baseline had 2 such rows with no audit object
  and a REAL `decided_at`.
- **P1(b)** all **49 decided** gate rows are `clock_domain='sim'`. Declines (41) min 8.00 / **median
  8.21** / max 9.03 sim-min, landing on the dial `indepot_reassign_auto_decide_min = 8`. The bogus
  "0.01 – 22.18" range is impossible by construction. The 2 `unmeasurable` rows are `pending`, aged
  **7.18 and 5.13 sim-min** at the stop — *below* the 8-min dial, i.e. in flight at truncation, not
  stranded.

### P1(c) — §7's arithmetic verified, and a claimed correction to it REFUTED

Independent reconstruction from `phase11_eviction_evidence_424242`: 45 deferred resumes, **24** closed
a booking, 24 live at close, raw 1,631.48, **net 1,359.77** — matching §7's recorded 1,359.78 to the
decimal. A figure of ~815.77 does **not** reproduce. Verified live: the two twin handlers carry 4
emission sites and **zero** hardcoded `interrupted_with_min_remaining = 0`.

### Protect list

**HELD** — emission invariant 19/19/19 = 1.000 · real double-bookings **0 of 325** in scope (partly
structural: `ottoq_stall_bookings_no_overlap_v3` enforces exactly this scope) · inspection-zone
parking 0 · starvation 0 (on a denominator of **one** low-SoC arrival) · churn 5 (baseline 19).

**MOVED AGAINST, recorded rather than buried, and not attributable to a migration that never fired** —
bay no-show 5 of 46 = 10.9% (baseline 1 of 49 = 2.0%; grace verified unchanged at 15; all 5 are
planner reservations with `source IS NULL`, not enacted admissions) · evictions cutting live work
19 of 43 = 44.2% (baseline 31.8%) · minutes destroyed 560.60 (baseline 276.78) · gate protection
71 of 90 = 78.9% (baseline 84.4%) · enacted ex-inspect per arrival 1.183 (baseline 1.295) · work done
per arrival 2.807 (baseline 3.071).

### Verdict — **do not revert, do not advertise the P0 as fixed**

No hard-fail occurred: no starvation, no double-booking, no fresh arrival displaced by resumed work.
The change is additive and **inert**: it removed nothing from the charge path, left all four guarded
objects md5-identical to the `0003_bay_work_recovery_post` snapshot, raised no engine warning
(`decide_tick` never failed; the only log warnings are the pre-existing
`ottoq_world_advance: no running production run`), and `check-drift.sql` re-run live after the run
reports **CLEAN** with routine counts public 337 / ottoq 48 / twin 71 all matching. Reverting would
discard two runtime-proven fixes and buy nothing. **Migration 0004 should relax exactly the two gates
named in V4 and re-certify.**

---

## Objects created outside a migration — declared, not hidden

The drift check counts routines, so a plain table created by hand is invisible to
it. Anything created outside the migration path gets declared here instead.

| Date | Object | Why it exists | Safe to drop? |
| 2026-08-07 | `public.ottoq_bay_binding_witness` (279 rows), `public.p0014_prestop_bookings_424242` (319), `public.p0014_prestop_vehicles_424242` (220), `public.p0014_prestop_stalls_424242` (320) | Evidence for the 0014 binding session on run `6f7518ef`. The witness is the **only** record that a forward bay reservation has ever bound causally — booking `555c29da`, `held → active` via `bay_reservation_activated`, seated on the reserved stall inside its window, 52.6 sim-min after it was reserved. The three `p0014_prestop_*` tables are the **pre-stop** capture (STOP empties the depot, so occupancy measured after it is vacuous): wash bays **0/3 occupied** while 6 `detail` pre-arrival holds sat unbound on them, service bays 2/2, l2 25/30, dcfc 5/10, staging 20/115; and **21 vehicles in `charge_complete_holding`** — the exact state activation requires — **none** of which held an open-window reservation. That mismatch, not run length, is why 0011's holds do not bind. Deliberately **not** `ottoq`-prefixed, so starting a run cannot purge them. | Not until a `source='return_signal_prearrival'` hold has been observed reaching `active` **and** `done`. These rows are the only record of how close the mechanism came and what blocked it. |
|---|---|---|---|
| 2026-08-07 | `public.proof0012_binding_blocker` (1 row) | **The sharpest negative result of the 0012 session.** Booking `49d1c436` is the ONLY `source='return_signal_prearrival'` hold in run `6f7518ef` whose window opened while its vehicle was actually inside the depot — reserved `NASH-SVC-01` (service_bay) for 14:48–15:25, sim clock 14:52 inside the window. **It did not bind.** It was released at 14:52:23 with `release_reason = 'replanned_no_window'` while the vehicle sat at `NASH-L2-STALL-29` in state `charging_l2` — i.e. legitimately upstream in its charge leg, exactly where the full-service-visit doctrine says it should be. So binding is **not** blocked by run length or by the tick cost: the replanner gives the bay up instead of pushing the hold later. Final census for that run: **18 held, 1 released, 0 active, 0 done**. Deliberately **not** `ottoq`-prefixed. | Not until a hold has been observed reaching `state='active'` with `vehicles.current_stall_id = booking.stall_id`. This row is the only record of *why* that has never happened. |
| 2026-08-07 | `public.proof0012_ticks_before` (143 rows), `public.proof0012_cron_before` (30 rows), `public.proof0012_window` (1 row), `public.proof0012_prearrival_census` | The before/after evidence for migrations 0012 and 0013, captured before `ottoq_start_demo_run` purged the prior run. `proof0012_cron_before` holds the **reproduced guillotine**: cron 12 at 00:12:32, **145.3 s**, `canceling statement due to statement timeout` inside `ottoq_record_event` — the same 120 s boundary that had previously been blamed on `ottoq_record_enacted_booking`, which is the proof that the CONTEXT function is where execution happened to be, not a root cause. Deliberately **not** `ottoq`-prefixed. | Yes, once the numbers are quoted durably — they are, in the 0012 and 0013 rows above. |
| 2026-08-07 | `public.proof0012_fk_all` (87 rows), `public.proof0012_fk` (13 rows), `public.proof0012_428c9` (1 row), `public.prof0012_cron_prestate` (5 rows) | PROTECT-list receipts for the 0012/0013 session: **87 foreign keys checked, 0 orphans** (a superset of the 17 named in the brief); `planned_return_at` still raises **`428C9 :: column "planned_return_at" can only be updated to DEFAULT`**; and the pre-change snapshot of all five cron jobs, which is the restore reference for the cron 12 command edit. Deliberately **not** `ottoq`-prefixed. | `prof0012_cron_prestate` not until cron 12's command is settled; the rest yes, once quoted. |
| 2026-08-07 | `public.prof0012_tick` (~300 rows), `public.prof0012_tabstat`, `public.prof0012_act`, `public.prof0012_pgss` | The 23-phase stopwatch attribution behind 0012's (M3): `11_advance_deployed_telemetry` **560.2 ms avg = 64 %** of an 878.6 ms world tick, and the `MICRO_*` rows that priced 0011's forward walk at **164.0 ms / 48.2 ms** per fully-contended leg. Built because **`track_functions` is not settable on this instance at any scope** (42501), so the technique named in the brief was unavailable. `prof0012_act` is **empty** — `pg_stat_activity` is filtered for the `postgres` role here, which is why wait-event attribution of the 12–84 s spikes was not possible. Deliberately **not** `ottoq`-prefixed. | Yes. Scratch instrumentation; the numbers are quoted in the 0012 row. |
| 2026-08-06 | `public.proof0009_credit` (2 rows) | Migration 0009's completion-credit proof, captured from inside `twin.ottoq_sim_advance_service_flow` itself. Two vehicles with **identical** fault state (2 open faults, severity 1) and different work orders: `Waymo-AV-034` (one service due) credited **1 of 4** with `false_credits_prevented = 3` and its fault list **intact**; `Tesla-AV-042` (three due) credited **3 of 4** and its faults correctly cleared 2 → 0. Each row carries `old_code_would_have_credited` (= the bay capability array, i.e. exactly what the pre-0009 code did) next to `credited_now`, so the before/after is on one line. Deliberately **not** `ottoq`-prefixed, so starting a run cannot purge it. | Yes, once the numbers are quoted somewhere durable — they already are, in the 0009 row above, and 2 `twin.service_completed` events in `ottoq_events` carry the same payload. |
| 2026-08-06 | `public.proof0009_eta` (1 row) | Migration 0009's ETA proof: one dispatch driven through the real return handshake in `twin.ottoq_sim_advance_deployed_telemetry`, showing `planned_return_at` **11:40 → 11:40 (survived)** while `scheduled_return_at` moved **11:40 → 12:30**, with `eta_refreshed_at` (SIM), `eta_source`, and the plan/live/`-50.0 min` divergence recorded in `return_evidence` at the moment of the refresh. Also carries the **V6** result — `SQLSTATE 428C9, "column planned_return_at can only be updated to DEFAULT"` — which is the proof that the plan is physically unwritable rather than merely un-overwritten. Deliberately **not** `ottoq`-prefixed. | Yes, once quoted durably — it already is, in the 0009 row above. |
| 2026-08-05 | `public.proof0008_profile_prestate` (2 rows), `public.proof0008_wear_prestate` (3 rows) | Full pre-state of the two vehicles migration 0008's soil smoke test drove through `ottoq_wear_mark_serviced` (`f0077a3a…` and `03726c33…`), captured **immediately before** the first call so the test could run against real graded rows and then be **completely reverted** — which it was, verified at **0 rows still diverged** on a full-row `::text` comparison of both tables. Deliberately **not** `ottoq`-prefixed, so starting a run cannot purge them. | Yes, once the soil fix has been re-observed on a live run (V7/V9 in the migration file). They hold no brain logic — they are a restore point. |
| 2026-08-05 | `public.proof0008_soil_smoke` (5 rows) | The before/after ledger of 0008's soil smoke: one `0_before` row per vehicle, then `1_after_mechanical_pm`, `2_after_exterior_wash` and `3_after_fault_repair`. This is the surviving evidence that a service-bay exit no longer launders body soil (0.857 held) or wipes a fault list (2 faults held), that a genuine wash still resets it (0.857 → 0.000), and that `fault_repair` still clears faults without touching soil. Deliberately **not** `ottoq`-prefixed. | Yes, once the numbers are quoted somewhere durable — they already are, in the 0008 row above. |
| 2026-08-05 | `public.proof0008_cron_prestate` (5 rows) | Pre-pause snapshot of all 5 cron jobs, used to prove jobs 10 and 12 were restored **exactly** after 0008's smoke window (`restored_exactly = true` on all five, job 12 active, job 13 left inactive as found). | Not until the next run start — it is the restore reference if a cron is touched again. |
| 2026-08-05 | `public.mig0006_source` (10 rows) | The **byte-verified transfer copy of `0006_slim_writes_and_arm_retention.sql`**, assembled in 10 chunks and proven identical to the committed file (`md5 = 4183de7939088fd252c5f05b19ad7893`) *before* a single statement executed. This is the artifact that lets anyone re-check, from inside the database, that what ran is what was committed: `SELECT md5(string_agg(body,'' ORDER BY part)) FROM public.mig0006_source`. Deliberately **not** `ottoq`-prefixed, so starting a run cannot purge it. | Yes, once someone has independently confirmed `md5(statements[1])` on ledger row `20260805142711`. It duplicates the ledger, which is the durable record. |
| 2026-08-05 | `public.mig0006_drift_src` (2 rows), `public.mig0006_drift_out` (8 rows) | Same technique applied to `scripts/check-drift.sql` (`md5 = 4187204b558557a265bd8b9629f42589`, verified before execution) and the verbatim CLEAN verdict it produced, so the drift result quoted for 0006 is reproducible rather than transcribed. | Yes. Re-runnable at any time; the script is read-only. |
| 2026-08-05 | `public.mig0006_smoke` (8 rows), `public.mig0006_runrows` (per-table row census), `public.mig0006_cron_prestate` (5 rows) | 0006's runtime evidence. `mig0006_smoke` holds the event ids behind the A/B write-size proof (fat 4,578 B vs slim 2,479 B on identical content), the anchor/fold pair, and the parent/child pair that proved the FK-cascade UPDATE now survives a purge. `mig0006_runrows` is the census that proved the purge test had **zero** collateral (6 of 7 runs are `production_live`). `mig0006_cron_prestate` is the pre-pause snapshot of all 5 cron jobs, used to prove they were restored exactly. Deliberately **not** `ottoq`-prefixed. | Not until the ~4.3 s/tick re-measurement is done on a live run — `mig0006_cron_prestate` is the restore reference if a cron is touched again, and the rest is the only record of the first successful retention delete. |
| 2026-08-05 | `public.smoke0005_visit_needs_prestate` (248 rows), `public.smoke0005_profile_prestate` (116 rows) | Full pre-state of run 909's `ottoq_visit_needs` (`visit_id`, `status`, `atoms`) and of every `vehicle_need_profile` row, captured **immediately before** migration 0005's smoke test so the test could be run end-to-end through the real sim caller (`twin.ottoq_sim_advance_visit_atoms`) and then **completely reverted** — which it was, verified at 0 rows still diverged on both tables. Also the surviving record of the pre-0005 divergence (116 of 116 rows with `exterior_soil_level <> soil_index`). Deliberately **not** `ottoq`-prefixed, so starting a run cannot purge them. | Yes, once the ≥139 sim-min certification run for V5–V9 is done and its results are logged. They hold no brain logic — they are evidence and a restore point. |
| 2026-08-04 | `public.pre0002_approvals_evidence` (58 rows) | Snapshot of `ottoq_ops_approvals` for `approval_type='indepot_reassign'` taken **immediately before** migration 0002's decider was first run, so the pre-fix state of the 55 stranded rows survives. It is what the 27→0 readmission-reachability before/after was measured against. Deliberately **not** `ottoq`-prefixed, so starting a run cannot purge it. | Yes, once the ≥139 sim-min certification run for V4–V6 is done and its results are logged. It holds no brain logic — it is evidence. |
| 2026-08-04 | `public.phase14_*_424242` — `bookings_424242` (736), `decisions_424242` (10,738), `dispatches_424242` (112), `visit_needs_424242` (110), `approvals_424242` (63), `gate_latency_424242` (51), `runrow_424242` (1), `cert_424242` (20) | Raw evidence for the 0003 certification run `fc1ad933` (180.173 sim-min, 109 arrivals), preserved **immediately after** `status='completed'` and before any subsequent run start, since `ottoq_start_demo_run` purges the prior run. `phase14_cert_424242` carries the full narrative including the two-gate root cause of the 0-of-2 bay recovery. Deliberately **not** `ottoq`-prefixed. | Not until migration 0004 has re-certified bay recovery — this is the only surviving record of *why* 0003 never fired. |

---

## How to add a row

Copy the row shape above. Fill in all six columns.

- **Date** — the day it was applied to the live database, not the day you wrote it.
- **File** — `db/migrations/NNNN_short_name.sql`.
- **What changed** — the objects touched and the behaviour that moved. Not "fix".
- **Why** — the symptom that started it. A number is worth ten adjectives.
- **Applied by** — a person or agent, and the path used (MCP `apply_migration`,
  Supabase CLI, or dashboard SQL editor). If it was the SQL editor, say so —
  that path writes no ledger row and the drift check cannot see it.
- **Verified** — the proof. Query, count, or run result.

Then re-run `scripts/check-drift.sql` and confirm **CLEAN**.
