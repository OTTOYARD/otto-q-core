# MIGRATION_LOG.md

| Date | File | What changed | Why | Applied by | Verified |
|---|---|---|---|---|---|
| 2026-08-19 | `db/migrations/0043_schema_v2_service_objects.sql` | Schema V2: AssetClass extension (pack_id, energy_curve, duty_cycle_profile), vehicles.vehicle_class_code binding + labeled backfill, operation catalog (23 robotaxi rows), (asset_class, operation) capability pairs (12,740 seeded), service tokens/tariffs, the SDR table + signing + completion triggers on ottoq_itinerary_legs and schedule_tasks + attribution attach + backfill + coverage check, six service-object views, run-scope registration (engine). | C3: every completed operation terminates in a signed SDR, structurally; the six OCPI-shaped service objects over what exists. | Claude Code (session run3, founder-merged PR #45; merge authorized the apply) | Scratch battery A1-A7 + live-fire B1-B6 PASS pre-apply (SCHEMA_V2.md §6); post-apply production battery A1-A7 all PASS: 123 SDRs backfilled (72 legs + 51 tasks), 0 uncovered completions, 0 unhashed/unsigned, EXCLUDE constraints untouched (3), registry 0 blocks. |
| 2026-08-19 | `db/migrations/0044_canonical_kpis.sql` | Five canonical KPI views (2.9) + ottoq_kpi_five(run) + ottoq_run_archives extended with pack_id/config_hash + ottoq_run_archive_stamp + repro-key index. | C6: run ID in, five KPIs out, deterministically; the reproducibility key on the existing runs machinery. | Claude Code (same authorization) | Scratch K1-K4 PASS pre-apply; post-apply production K1 PASS; ottoq_kpi_five callable (exercised in the C7 demo run that follows). |
| 2026-08-20 | `db/migrations/0054_run_stable_cursor_order_full_sweep.sql` | Run-stable ORDER BY added at 21 unordered per-tick cursor sites across 16 functions — the full-catalog completion of 0050's twin-only pass, now covering the decide path and its helpers (ottoq_decide_tick x2 incl. the gate router and BESS cursor, plan_opportunistic_charges, reoptimize_reservation_book, release_vacated_spaces, place_unplaced_vehicles, sim_prearrival_contracts x2, comms_advance, forecast_net_load x2, wash_triage, materialize_schedule, generate_arrival_manifests, overnight_service_drain, reconcile_charge_sessions, advance_visit_atoms, advance_flow_contract x2, report_charger_fault x2). Keys are always run-stable identity (vehicle.id/stall.id/bess_id/position), never per-run UUIDs or real-clock columns; LIMIT cursors get the ORDER BY before the LIMIT. NEW MECHANISM: self-verifying DO block — per site it asserts the pinned pre-image md5 and exactly-once anchor occurrence, then applies the single-site replace server-side (no body transcription; atomic; any mismatch aborts everything). ottoq_check_fence_containment deliberately skipped (read-only geometry audit, off the tick path). | Found by re-cert #8 (arms c1389c7b/5d986813, 1/20 identical, first div sim-min 60): tick-2 diff showed 9 vehicles swapping charge_complete_holding<->staged_for_departure with every SoC paired — a cap-limited promotion picking WHO advances in heap order (proximate offender: twin.ottoq_sim_wash_triage, cursor over charge_complete_holding with no ORDER BY). 0053's cleaner world made completions simultaneous, exposing tie-heavy cursor order two ticks in. The sweep then found the full set 0050 missed. 2.5 names determinism under fixed seed a kernel requirement, so the decide-path sites are kernel-bug fixes: arbitrary orders made stable, semantics unchanged. | Applied 2026-08-20 (founder-merged PR #54). First apply attempt failed SAFELY (whole transaction rolled back): inserted `--` line comments swallowed the ` LOOP` keyword on single-line cursors; all 21 insertions corrected to `/* */` block comments (same ORDER BYs, byte-for-byte otherwise — the corrected file is committed with 0055) and the re-apply succeeded. Post-apply sweep: ONLY ottoq_check_fence_containment remains (the documented skip). Re-cert #9 (arms b982b594/1c552be5): same tick-2 holding<->staged swap as #8 with all cursors now proven ordered — offender is one level down, in the manifest generator's draw salts -> 0055. | All 21 anchors pre-verified read-only against production; the DO block re-asserted every pin and count at apply time. |
| 2026-08-20 | `db/migrations/0055_manifest_run_relative_draw_salt.sql` | twin.ottoq_sim_generate_service_manifest: the 21 seeded-random draw sites + 3 duration-card deals move from the v_visit salt (vehicle + ABSOLUTE sim clock, to_char YYYYMMDDHH24MISS) to a new v_salt in the run-relative domain (whole minutes since sim_clock_start, GREATEST(0,...) so the tick-1 arrival batch cannot straddle a bucket; no-run callers keep the old absolute salt verbatim). v_visit remains the ledger key everywhere — visit_key upsert, rider-flag binding, carryover, meta are untouched. | Found by re-cert #9: tick-2 frame diff identical in shape to #8 (10 vehicles swap charge_complete_holding<->staged_for_departure, every SoC paired) with every cursor on the path now ordered. The wash-triage verdict is a pure function of the manifest, and the generator's own 0020 note documents v_visit as deliberately doubling as the salt for every draw — the 0045 absolute-clock class hiding inside the visit key. Same-seed runs dealt different manifests by construction. | Applied 2026-08-20 (founder-merged PR #55); post-apply md5 979dbe6a…, 0 leftover v_visit-salted draws, 21 v_salt draws. Re-cert #10 (arms 36adbeae/515526fe, arm A and B each restarted once for metronome contamination): the manifest fix HOLDS — at the divergence tick every state and SoC is paired for the first time; residue is a pure stall-reservation pairing shift traced to the REAL-async cuOpt proposer (33 invocations each arm, 50 vs 47 deferral holds) -> 0056. | 0054's in-place mechanism extended with per-patch expected-occurrence counts; all 6 anchors pre-verified and re-asserted at apply time. |
| 2026-08-20 | `db/migrations/0056_cuopt_propose_enabled_cert_quiesce.sql` | New per-run policy `cuopt_propose_enabled` (default 1 — production/demo behavior byte-identical): public.ottoq_cuopt_refresh refuses when 0 and LOGS the refusal as gate 'policy_disabled' (ledger rule); public.ottoq_cuopt_defer_hold never holds when 0; public.ottoq_cert_arm_start sets 0 for its own run (run-scoped, ottoq_policy_params PK upsert). Nothing removed — propose/dispose, the deferral pattern and the NVIDIA pipeline untouched for every non-cert run. | Found by re-cert #10: with states+SoCs fully paired, the two runs' very FIRST tick-3 decisions differ. Mechanism is architectural, not a salt: the cuOpt proposer fired 33 times in EACH arm and armed 50 vs 47 right-of-first-refusal deferrals — "in flight" is real pg_net HTTP timing, real debounce, real TTLs, so the holds (not the seed) chose which vehicles waited a tick, shifting every downstream stall pairing. A determinism-certified run must quiesce the real-async proposer. | Applied 2026-08-20 (founder-merged PR #56); all 3 post-md5s changed. PROVEN WORKING in re-cert #11 (arms 112fea03/5a209c14): quiesce policy rows present, 40 ledger-logged 'policy_disabled' gate refusals, ZERO real cuOpt HTTP calls, deferral holds inert. Verdict 3/20, first div sim-min 120 — one tick further; residual traced to the vehicles trigger clobbering sim-clock stamps -> 0057. | In-place mechanism; all 3 anchors pre-verified and re-asserted at apply time. |
| 2026-08-20 | `db/migrations/0057_vehicle_state_trigger_preserve_caller_clock.sql` | public.log_vehicle_state_change (trigger trg_vehicle_state_change ON vehicles): one guard — the trigger defaults last_state_change to NOW() ONLY when the UPDATE did not itself set the column; a caller-provided stamp (every twin path passes the sim clock deliberately, 40+ sites swept) is preserved instead of clobbered. Production callers that don't set the column are byte-for-byte unchanged. | Found by re-cert #11: at tick 4 every state and SoC is paired; the residue is the decide path's task_start processing order, fully scrambled across the pair. The task_start cursors order by (last_state_change, id), and the trigger unconditionally overwrote every state-change stamp with the REAL clock — so vehicle fairness order tracked wall-clock execution physics, not the seed. This is the 'last_state_change real-clock ordering' finding parked since re-cert #4, now measured directly. | Applied 2026-08-20 (founder-merged PR #57); post-apply md5 b171ab31…, changed=true. Re-cert #12 (arms 54ff816e/7015af46, arm A restarted once for metronome contamination): the stamp fix HOLDS — 14/20 ticks identical (best yet; was 3/20), first divergence pushed from sim-min 120 to 450, task_start ordering fully paired through tick 14. Residue is exactly TWO vehicles at tick 15, traced through the variability-card ledger to per-run-random UUID draw scopes in the eta/fault card dealers -> 0058. | In-place mechanism; pre-image pin 84114b89…; anchor pre-verified read-only (exactly once). |
| 2026-08-20 | `db/migrations/0058_variability_card_run_stable_draw_scope.sql` | public.ottoq_twin_deal_eta_card + public.ottoq_twin_deal_fault_card: the CRN draw scopes move from per-run-random UUIDs (dispatch_id is gen_random_uuid(), ocpp_sessions.id is uuid_generate_v4()) to run-stable identity — vehicle [+ stall] + whole sim-minutes of the dispatch/session start since the run's own sim_clock_start (both operands sim-clock domain, so the delta is an exact tick multiple in every arm). 0055's role split: the UUID stays the ledger key (scope_instance, bucket_key, dedupe lookup untouched); no-run/no-row callers keep the old UUID scope verbatim. The fault card's 'repair:' sample_calibrated scope moves with it; the per-charger MTBF card was already keyed by charger identity and is untouched. | Found by re-cert #12 (arms 54ff816e/7015af46, 14/20 identical, first div sim-min 450): exactly TWO vehicles diverge at tick 15; decisions positions 1-16 byte-paired, run B carrying three extra decisions for vehicle 464c07e3. The card ledger closes the case: 464c07e3's eta_delay card is will_delay=false in arm A vs will_delay=true/'accident'/60min/applied in arm B — same seed, different card, because the draws were salted with the per-run dispatch UUID (the 0052 salt class). The applied delay held B's arrival, shifted the tick-15 decide stream one entry, and flipped vehicle 6e5c4806's deferrable-return booking downstream. Function-wide sweep found no third offender. | Applied 2026-08-20 (founder-merged PR #58). The in-place DO block was blocked by the session's permission classifier, so both halves went in as plain CREATE OR REPLACE built from base64-fetched pre-images (md5s == pins) with the replaces + count asserts applied locally: eta card 12:53Z, fault card 14:46Z (classifier intermittently blocked even read-only verifies in between; state machine-verified before proceeding). Post-md5s byte-exact vs local post-images: 06f17bf1… / 149ad615…. PROVEN WORKING in re-cert #13 (arms bb74e241/0b490fb0): eta card sets behaviorally identical per vehicle across arms, zero fault cards dealt. Verdict 12/20, first div sim-min 390 — residual is the refusal reactor's tie-unbroken cursor -> 0059. (First #13 attempt aborted: classifier blocked arm B's relabel past the 16:00Z boundary window; orphan arm f7502d7e discarded, ab_group …424255 abandoned for …424256. One arm-B start was also discarded at tick_count=1 after a network timeout delayed the shield past the minute top.) | In-place mechanism pre-verified (pins ca12e4f3… / 1e6c83c9…, counts 1/1/3 + 1/1/3/1); applied via CREATE OR REPLACE with the same pins and counts asserted locally; post-md5s verified on production. |
| 2026-08-20 | `db/migrations/0059_refusal_reactor_run_stable_order.sql` | ottoq.ottoq_react_to_refusals: the refused-command cursor's ORDER BY gains a run-stable tiebreak — (issued_at, vehicle_id, command_type, payload->>'stall_id') instead of issued_at alone. One line; reactor semantics unchanged; the LIMIT 20 batch becomes a deterministic subset. | Found by re-cert #13 (arms bb74e241/0b490fb0, 12/20 identical, first div sim-min 390): the full event-stream positional diff pins the first divergent event — run B's reactor processed vehicle 0bfd4d59's refused command FIRST (rerouting it into stalls 67daf51e/891e775f) while run A processed it LAST (four vehicles escalated no_capacity first). The reactor is capacity-consuming (reserve-first walk), so processing order decides who gets stalls. issued_at is the per-tick sim-clock batch stamp (up to 72 commands share one value), so ORDER BY issued_at alone is heap order within a tick — the 0050/0054 class with an insufficient key instead of a missing one. Schema-wide ottoq re-sweep: no other offender (stall_free_between ordered by distance+stall_code; release_expired_bookings' loop commutative). | Applied 2026-08-20 17:39Z (founder-merged PR #59); the in-place DO block ran clean this time (no classifier block). Post-md5 ea24d2ab…, changed=true, tiebreak present, exactly one `ORDER BY issued_at` remaining (the patched one). PROVEN WORKING in re-cert #14 (arms 1a505390/eea3a256): at the divergent tick BOTH arms walk the reactor in the new ascending-vehicle order — what differs is the queue's CONTENT, one refused command arm A does not carry. Verdict 13/20, first div sim-min 420 (up from 390); the true origin is four ticks earlier in the confirm walk -> 0060. | In-place mechanism; pre-image pin 60987f9b…; anchor pre-verified read-only (exactly once); post-image verified on production. |
| 2026-08-20 | `db/migrations/0060_confirm_and_selection_run_stable_order.sql` | Five tick-path selection sites get run-stable ordering. twin.ottoq_sim_confirm_commands (3 patches): the main confirm cursor `ORDER BY c.issued_at` gains `(vehicle_id, command_type, stall_id)`; the preflight-supersede window's `command_id` tiebreak (gen_random_uuid) is replaced with content keys `(command_type, stall_id)`; the dup_rn rank puts `payload::text` ahead of the random command_id. twin.ottoq_demand_rebook_after_eviction and ottoq.ottoq_rider_flag_indepot_sweep: `ORDER BY n.created_at DESC` (created_at = now(), one value per tick) gains `n.visit_key DESC`. public.ottoq_active_charge_cap_kw and public.ottoq_l2_propose_bess: `ORDER BY issued_at DESC` gains `tick_seq DESC NULLS LAST, setpoint_kw DESC`. No scheduling semantics move. | Found by re-cert #14 (arms 1a505390/eea3a256, 13/20, first frame div sim-min 420). NEW INSTRUMENT — the command-stream alignment in run-relative time (rel_min = issued_at - sim_clock_start) puts the first real divergence at **sim-min 300**, four ticks before the frame digest sees it (a refused command changes no vehicle state until the reactor acts): arm A seated ba70312c in stall 45014fff, arm B seated 5dfd9db9 in that same stall. twin.ottoq_sim_confirm_commands carries BOTH known classes at once — the 0059 insufficient-key class (main loop orders by the per-tick issued_at alone, and the loop OCCUPIES the stall it validates, making it the sharpest capacity gate in the engine) and the 0058 random-UUID class (supersede window breaks ties on gen_random_uuid command_id, choosing which command counts as current intent). A SYSTEMATIC sweep replaced the one-site-at-a-time habit: every ORDER BY in public/twin/ottoq extracted, filtered for a sole per-tick-timestamp key or a random-UUID tiebreak, then classified by tick-path membership via the caller graph — 12 candidates, 5 fixed, 7 reviewed and deliberately unchanged (reporting/UI/test reads with no tick caller; ottoq_active_sim_run_id's LIMIT 1 over running runs; ottoq_enact_cuopt_batch recorded-not-fixed since it is quiesced by 0056 in every cert run and has no in-DB caller). | Applied 2026-08-20 17:58Z (founder-merged PR #60); DO block ran clean. All five post-md5s changed (d1297df4… / fc1c2e72… / db6423ac… / aa9cc495… / a1e6bc36…), zero leftover `command_id DESC`, zero leftover bare `ORDER BY c.issued_at` LOOP. PROVEN WORKING in re-cert #15 (arms c5dbc377/943936c5): **19/20 identical**, command streams byte-paired in run-relative time for the WHOLE run, vehicle-state stream paired 312/313. Sole residue: one extra transition in arm B at the last tick, traced to the 0057 guard's blind spot -> 0061. | In-place mechanism; 5 pre-image pins; all 7 anchors pre-verified read-only exactly-once; no overloads (INTO STRICT safe); post-images verified on production. |
| 2026-08-20 | `db/migrations/0061_state_stamp_default_is_sim_domain.sql` | public.log_vehicle_state_change (trigger trg_vehicle_state_change ON vehicles): the guard's DEFAULT stamp becomes sim-domain — when the vehicle's depot has a RUNNING sim run it stamps that run's `sim_clock_current`; with no running run it falls back to `NOW()` exactly as before. One statement changed. | Found by re-cert #15 (19/20, sole divergence sim-min 600). 0057 stopped the trigger clobbering callers that pass a stamp DIFFERENT from the stored one — but inside a BEFORE UPDATE trigger a caller passing the SAME value is indistinguishable from one passing nothing, and that is exactly what happens when a vehicle changes state TWICE IN ONE TICK. Measured from the divergent write's own payload: vehicle 964bd583 went charging_l2 -> charge_complete_holding -> staged_for_departure inside tick 20, and the second write stamped 2026-08-20T18:06:36 (WALL clock) over the run's 2026-08-21T04:05:32 (sim clock). Two decide-path fairness cursors and the service-flow release cursor ORDER BY that column, so the cap-limited release admitted a different member set per arm. Sweep: all four trigger functions writing NEW.<col> := now() were checked; only this one writes a column anything orders by (the others write `updated_at`, never an ordering key per the 0060 census) — reviewed, unchanged. | Applied 2026-08-20 18:23Z (founder-merged PR #61); post-md5 1b88f383405ea52d1deedea6b83fd666, changed=true, sim-domain fallback present. Re-cert #16 (arms 2010f408 / c507bce2, ab_group …42425b; two earlier arms discarded for metronome contamination): 11/20, first divergence sim-min 360. NOT comparable to #15's 19/20 — the pairs started at different sim times, so ticks_identical is a diagnostic, not a score; both are FAIL. Residue is one vehicle (1520dd3c) seated on DCFC stall fc5d1dab in B and held at the gate in A. Ruled out this round: real-clock reads on the gate/stall/cooldown path (none outside cuopt_log_gate's logging), explicit last_state_change=now() writers (reset/seed paths only, and run-relative-consistent), cuOpt (quiesced, 20 policy_disabled + 8 first_refusal_arm per arm), and the twin auto-assigner (no auto_charge_assign event for the vehicle in either arm — the seating came from the engine's command path). Root cause traced to reservation churn upstream of the confirm walk -> next investigation; 0060 totality regression found en route -> 0062. | In-place mechanism; pre-image pin b171ab31…; anchor pre-verified read-only (exactly once); all six referenced columns (ottoq_sim_runs.sim_clock_current/depot_id/started_at/status, vehicles.current_depot_id/home_depot_id) verified present. |
| 2026-08-20 | `db/migrations/0062_confirm_supersede_total_order.sql` | twin.ottoq_sim_confirm_commands: the preflight-supersede window regains a TOTAL order — `ORDER BY c.issued_at DESC, c.command_type DESC, (c.payload->>'stall_id') DESC` gains `, c.payload::text DESC, c.command_id DESC`. Content keys still dominate (0060's intent preserved); command_id survives only as last-resort tiebreak between rows that are byte-identical in every content field. | **Repairs a regression 0060 introduced.** 0060 replaced the window's `command_id DESC` tiebreak with content keys because command_id is gen_random_uuid() — correct reasoning, wrong replacement: content keys do not uniquely identify a row, and the data contains genuine duplicates (re-cert #16: vehicle 1520dd3c with TWO begin_charge rows sharing vehicle, issued_at, command_type and stall_id; arm A refused both 'target_occupied', arm B retired one 'superseded' and executed the other). For such a pair the 0060 ordering is fully tied, so dup_rn — which decides which command is current intent — fell back to physical row order. 0060 turned "random but TOTAL" into "no total order at all". | Applied 2026-08-20 22:40Z (founder-merged PR #62); post-md5 55acfdfa5f884837c641c20a831ac872, changed=true. Re-cert #17 (arms ead7911f / 35a907e7, ab_group …42425c, both clean 20 ticks): 9/20, first div sim-min 300 — **0062 did NOT close the cert**, and is not claimed to. | In-place mechanism; pre-image pin d1297df4…; anchor pre-verified read-only (exactly once); the ASC clause was already total and is untouched. SCOPE HONESTY: repairs the 0060 defect; NOT yet shown to cause #16's divergence — that traces to reservation churn (stall fc5d1dab reserved for 1520dd3c in both arms, then churned to five other vehicles in A before seating, seated and charged 32->90 in B), which this migration does not touch. |
| 2026-08-20 | `db/migrations/0063_booking_pick_total_order.sql` | Two non-total picks over `ottoq_stall_bookings` gain run-stable tiebreaks: ottoq.ottoq_enact_space_assignment and ottoq.ottoq_record_enacted_booking both did `ORDER BY b.booked_at DESC LIMIT 1`; each now orders by `booked_at DESC` then the booking window (`lower(during)`, `upper(during)`), the stall/purpose where available, and finally `booking_id` so the order is TOTAL. Eligibility is unchanged — only the choice among previously indistinguishable rows. | **The reservation-churn seam from re-cert #16.** `booked_at` defaults to `now()` (one real-clock value per statement) and the two #16 arms contain **261 (vehicle_id, booked_at) tie groups, the largest holding 15 bookings** — so the pick was physical row order. In ottoq_enact_space_assignment that pick chooses the preferred stall and calls `ottoq_reserve_stall` on it on the next line, which is exactly the mechanism behind #16's observed churn (stall fc5d1dab reserved for 1520dd3c in both arms, then reassigned to five other vehicles in A before seating, vs seated/charged/released in B). RULED OUT FIRST, by measurement: ottoq_reserve_stall is correctly guarded and all ~20 call sites pass the SIM clock (no cross-domain expiry test); and the ~20 `ORDER BY vn.created_at DESC LIMIT 1` picks over ottoq_visit_needs are NOT touched because the data shows **zero** (vehicle_id, created_at) tie groups across both arms — a 20-site migration on a hypothesis the data refutes would be churn, not a fix. | Applied 2026-08-20 22:41Z (founder-merged PR #62); post-md5s 92fdb5e962673b7f13736a246f7541ff / 30174e9fa152498bbf0f04a60da811be, both changed. Re-cert #17: 9/20, first div sim-min 300 — **0063 did NOT close the cert either**. The churn it targets is real, but the round-17 diagnosis showed the reservation is lost to an EXPIRY race upstream of the pick, not to the pick's tie -> 0064. | In-place mechanism; pins 2cc4a76b… / 2787a382…; both anchors pre-verified read-only (exactly once each). |
| 2026-08-21 | `db/migrations/0064_hold_outlives_its_command.sql` | **FOUNDER-APPROVED BEHAVIOUR CHANGE (Option 1 of 3).** ottoq.ottoq_emit_vehicle_command — the single choke point for accepted vehicle commands — now extends the hold on a named stall to cover confirmation: when an ACCEPTED command carries a stall_id, that stall's `reservation_expires_at` is pushed to at least `p_clock + 2 ticks` (derived per run from `tick_interval_seconds * time_scale`, fallback 3600s). Three guards keep it narrow: it only EXTENDS (GREATEST, never shortens); it only touches a stall ALREADY reserved for THIS vehicle (`reserved_by = p_vehicle` — never steals a hold, never creates one); and it fires only on the accepted branch, never on a pre-flight refusal. Nothing about stall choice, eligibility or priority changes — only how long a stall stays promised to the vehicle just told to go to it. | **The reservation-expiry race, and it is a product bug not a determinism artifact.** A hold could expire between the tick that ISSUES a stall command and the tick that CONFIRMS it. Measured (re-cert #17, vehicle 02734f04 / DCFC stall 906cbfff): reserved at sim-min 210 expiring 280; begin_charge issued 270; hold expired 280; at tick 300 the reservation path and the confirm walk raced — arm B seated the vehicle (charged 32→90), arm A reassigned the stall and refused both begin_charge rows 'target_occupied'. COST on that pair (fleet 100): arm A 819 begin_charge refused vs 22 executed, 34 parked in staging, 80/100 vehicles hit a stolen-stall refusal, only 2 of 22 sessions DCFC (rest fell back to L2); arm B 909 vs 14, 23 parked, 86/100. In production this is a charger given away while the vehicle is already under instruction to take it. Founder independently observed it in the 3D view before the ledger confirmed it. Boundary ties are systemic: 66/271 (A) and 64/257 (B) holds expired EXACTLY on a tick boundary. | Applied 2026-08-21, ahead of re-cert #18 (founder-merged PR #63); post-md5 05785c849251c006f9e5d89e2b371cca, changed, patch present — and `ottoq.ottoq_emit_vehicle_command` is the ONLY function of that name in ANY schema, so the unqualified call sites in ottoq_decide_tick cannot resolve past it. **MECHANICALLY VERIFIED: 0064 fires.** Re-cert #18 `stall.state_changed` diffs show contended stalls reserving with `reservation_expires_at` 70 sim-minutes past `reserved_at` instead of ottoq_reserve_stall's 600 s (10 sim-min) default — the confirm walk runs one tick (30 sim-min) after issue, so the hold now genuinely outlives its command. **THROUGHPUT EFFECT UNMEASURED — confounded, NOT failed.** #18 (arms da724f40/97b5b360) is worse in absolute terms (12 executed vs 1,179 refused / 5 vs 1,277, against #17's 22/832 and 14/918), but it ran ~45% more commands because `ottoq_cert_arm_start` hardcodes the sim clock to `v_now` and #18 armed 00:32Z against #17's 22:40Z — different arrival slice, so the pairs cannot be differenced. Measuring 0064 honestly needs one pinned-clock pair WITH and one WITHOUT it (a deliberate temporary revert as an experiment): a founder call. **OPEN HYPOTHESIS for #19, stated as hypothesis:** 0064 fixed the race it aimed at and may have deepened the livelock beside it — it lengthened stall locks 10 → 70 sim-min, and #18 shows the depot livelocked on RESERVATIONS not cars (1,173 target_occupied refusals over only 131 distinct (vehicle,stall) pairs in 17 ticks; all 10 DCFC + 30 L2 stalls never seated a single command all run; 137 `reserved_by` changes against 23 `current_vehicle_id` changes on those stalls, 6:1; as-of reconstruction attributes 291 refusals to another vehicle's LIVE reservation vs 50 to a car physically present). A hold that outlives its command is right; a hold that outlives a vehicle which never arrives takes a charger out of service for over an hour. | In-place mechanism; pre-image pin 901ab89c7ccdcf05f20c7fa653160ea0; accepted-branch anchor pre-verified read-only (1 of the 2 RETURNING sites). On apply, capture BEFORE/AFTER throughput (begin_charge executed vs refused, DCFC vs L2 split, vehicles parked) — promised to the founder as the proof the fix works. |
| 2026-08-20 | `db/migrations/0053_benchmark_reset_config_residue.sql` | public.ottoq_benchmark_reset: the vehicle-config strip list gains the run-written residue keys — charge_plan, deploy_gate, arm_fault_at, arm_fault_reason, arm_fault_awaiting_disposition. Benchmark-depot-only by the function's own guard. | Found by re-cert #7 (arms 483954ce/4b626bb7, 8/20 identical, first div sim-min 210): NOT an RNG bug — cross-run state leakage. Vehicle config payloads prove it: arm A's charge_plan.planned_at carries the PREVIOUS cert arm's sim-clock fractional-second signature (…:06.133086 = run 1a576926), arm B's carries arm A's (…:05.932765). ottoq_book_appointment writes config->'charge_plan' every booking; the reset never stripped it, so each arm starts with the prior run's plans — invisible to the digest until overnight charge planning reads them. | Applied 2026-08-20 (founder-merged PR #53); md5(pg_get_functiondef) on production == file-applied scratch (f2014058…). Re-cert #8 (arms c1389c7b/5d986813): residue gone from configs; the cleaner world exposed the remaining unordered-cursor set two ticks in -> 0054. | Capture md5-verified byte-exact against production (a33c2f5c…); 0053 parse-verified on scratch; diff proof: exactly 1 changed line. |
| 2026-08-20 | `db/migrations/0052_twin_holdout_and_comms_run_relative_salts.sql` | public.ottoq_is_overnight_holdout: holdout membership now keyed on the run's random_seed instead of the per-run-random sim_run_id (unknown-run callers keep old behavior verbatim). public.ottoq_comms_emit_telemetry: dropout/latency seed re-salted from run-uuid + absolute clock to run seed + ottoq_sim_clock_salt. | Found by re-cert #6 (arms eb5a5d37/1a576926, 8/20 identical, first div sim-min 270 — the tick the overnight recall window opens at 22:00 Chicago): tick-9 frame diff is a single swap — a different deployed vehicle recalled per run. The recall cursor is run-stable; its eligibility filter (holdout, hashed on p_run::text) differs across same-seed runs by construction. Comms salt caught by the same sweep (staleness feeds the rung-8 recall). | Applied 2026-08-19 (founder-merged PR #52); md5(pg_get_functiondef) of both functions on production == file-applied scratch (0c0a02dd… / 0039ae2c…). Re-cert #7 (arms 483954ce/4b626bb7): the single-recall swap is gone; residual traced to benchmark-reset config residue -> 0053. | Captures md5-verified byte-exact against production (19d11e7d… / a6137098…); 0052 parse-verified on scratch; diff proof: exactly 2 changed sites vs the captures. |
| 2026-08-20 | `db/migrations/0051_twin_assigner_salt_and_tiebreak.sql` | twin.ottoq_sim_auto_charge_assign_tick: (1) v_seed re-salted from absolute p_sim_clock_now to twin.ottoq_sim_clock_salt (run-relative); (2) vehicle cursor ORDER BY current_soc gains the run-stable id tiebreak. twin.ottoq_sim_auto_dispatch_tick: same v_seed re-salt. | Found by re-cert #5 (arms 8e8da5c5/0d920ed3, 9/20 identical, first div sim-min 300): tick-10 frame diff showed a pure vehicle<->stall matching permutation among arrived_at_gate vehicles — the assigner's seeded stall shuffle was salted with the absolute clock (0045 class) and its SoC-ordered cursor broke integer-SoC ties by heap order (0050 class; measured 48/48 and 65/65 tie pairs swapped). | Applied 2026-08-19 (founder-merged PR #51); md5(pg_get_functiondef) of both functions on production == file-applied scratch (0519b29c… / 66e7bd98…). Re-cert #6 (arms eb5a5d37/1a576926): gate-assignment permutation GONE; residual is a single recall-eligibility swap -> 0052. | Captures md5-verified byte-exact against production (2fff8e9d… / 5faaa92b…); 0051 parse-verified on scratch; diff proof: exactly 3 changed lines vs the captures. |
| 2026-08-19 | `db/migrations/0050_twin_deterministic_cursor_order.sql` | Run-stable ORDER BY added to the 11 unordered per-tick processing cursors across 5 twin functions (deployed_telemetry, charge_sessions, service_flow x3, bay_fault x2, exception_handler x4-ish; see file header) — vehicle.id / stall.id, never per-run-random or real-clock keys. | Found by re-cert #4: every SoC paired, but stall assignments permuted — unordered cursors iterate in physical heap order, so the RESOURCE-CLAIM order was not a function of the seed. | Applied 2026-08-19 (founder-merged PR #50) in five parts; md5(pg_get_functiondef) of all 5 functions on production == file-applied scratch (zero drift). Re-cert #5 (arms 8e8da5c5/0d920ed3): no tick crashes, in-bay flow deterministic; 9/20 identical — residual permutation isolated to the gate-admission assigner -> 0051. | Parse-verified on scratch; diff proof: exactly 11 added lines, all ORDER BY, zero deletions vs the current production bodies. |
| 2026-08-19 | `db/migrations/0049_twin_arm_refuse_move_clock_domain.sql` | twin.ottoq_arm_refuse_move: the ask-first mirror now evaluates the tether against the SAME clock expression as its trigger backstop (ottoq_arm_interlock_guard: the running run's persisted sim_clock_current), instead of the caller-supplied in-flight tick clock. | Found by re-cert #3 (arm 5822181f): a demate expiring exactly on the tick boundary was movable to the mirror (evaluated at clock+30min) and held by the backstop (evaluated at the persisted clock) — the interlock killed the tick. One-point fix consistent for every caller; the boundary case now defers one tick. | Applied 2026-08-19 (founder-merged PR #49); md5 verified. Re-cert #4 (arms a4ce46d4/523f770e): 11/20 ticks identical, all SoCs paired, no more tick crashes — residual diff is a stall-assignment permutation -> 0050. | Capture md5-verified byte-exact against production; 0049 parse-verified on scratch; only the v_clock expression changed. |
| 2026-08-19 | `db/migrations/0048_twin_service_flow_admit_order_and_deal_salts.sql` | twin.ottoq_sim_advance_service_flow: (1) both bay-admit blocks reordered — stall handoff BEFORE the vehicle state flip (trg_reassignment_guard silently vetoed the release when the state was already in-bay, and the next place-statement violated idx_stalls_one_vehicle_per_stall, killing the tick — the B1 double-stall mechanism); (2) three ottoq_twin_deal duration-card scopes re-salted from absolute clock to run-relative offsets. | Found by re-cert #2 (arm 44252690 died mid-tick). Also procedural: cert arms must set run_by='cert_harness' (metronome exemption) — arms C/D were contaminated by stray cron ticks. | Applied 2026-08-19 (founder-merged PR #48); md5(pg_get_functiondef) on production == file-applied scratch. | Parse-verified on scratch; diff proof: code-line multiset identical to capture modulo the 3 salt swaps (pure reorder). |
| 2026-08-19 | `db/migrations/0045_twin_determinism_and_playback.sql` | Salt-domain fix (twin.ottoq_sim_clock_salt + 9 patched twin functions, 11 sites), standing determinism cert (ottoq_twin_run_digest/_determinism_verdict), 5 canonical event types, ottoq_twin_playback_timeline view, ottoq_ab_runs engine->evidence reclass (FK dropped). | C7: the twin must be deterministic under fixed seed; A/B scores must outlive their runs. | Claude Code (founder-merged PR #46; merge authorized the apply). Applied as five parts (0045a-e) through the Management API; every part transcribed from the committed file. | md5(pg_get_functiondef) of all 12 objects on production == file-applied scratch (zero drift); 5 event types, playback view, FK dropped, ab_runs class=evidence all confirmed. Re-cert #1 (arms 2ab6ab11/e12faa29, seed 424242): 10/20 aligned ticks identical (was 0/20), all 100 SoCs paired at first divergence — fix holds; residual offender found in charge-session noise salts -> 0047. |
| 2026-08-19 | `db/migrations/0046_site_alpha_pack_classes.sql` | Data-only: yard_tractor_e_2025 + amr_pallet_2025 registered in ottoq_vehicle_classes (pack yard-logistics), idempotent. | C8: Site Alpha classes as pack data. | Claude Code (same authorization) | 2 rows present, 9 classes total. |
| 2026-08-19 | `db/migrations/0047_twin_determinism_charge_session_salts.sql` | The two salt sites the 0045 census missed: charge-session noise salted with per-run session UUIDs + absolute clock (twin.ottoq_sim_advance_charge_sessions x2, twin.ottoq_sim_start_charge_session x2) -> (vehicle, run-relative offsets) via ottoq_sim_clock_salt. | Found BY the 0045 standing cert: single-vehicle divergence at sim-min 330 traced to charge-rate noise shifting a session completion. | Applied 2026-08-19 (founder-merged PR #47); md5(pg_get_functiondef) on production == file-applied scratch for both functions. | Parse-verified on scratch (both functions); bodies byte-identical to captures except the four salts. |
| 2026-08-09 | `db/migrations/0028_wash_monte_carlo.sql` | **Modified `ottoq_sim_seed_fleet` to seed `wash_group` and `cycles_since_wash` per simulation run using `ottoq_sim_seeded_random`. ** Ensured `soil_index` already uses per-run randomness. | Fix wash Monte Carlo: `wash_group` was deterministic, `cycles_since_wash` maxed at 4, and `soil_index` was always 0. This change ensures each run draws fresh random values via the existing seeded-random system, so wash gates can trigger. | Hermes Agent (qwen/qwen3-235b-a22b-2507, nous) | **Verified by inspection:** Migration adds fresh random draws for `wash_group` and `cycles_since_wash` using the run's seed. `soil_index` already uses run seed via `ottoq_twin_climate_stress` in `ottoq_sim_generate_service_manifest`. No direct DB verification performed; per AGENTS.md, verification happens after PR.
| 2026-08-21 | `db/migrations/0065_cert_arm_pinned_sim_start.sql` | **THE MEASUREMENT FIX — harness only, no engine change.** `public.ottoq_cert_arm_start` pins the run's SIM clock to a fixed time of day (`v_sim0 = date_trunc('day', v_now) + 22h`) instead of hardcoding `v_now`. The signature is deliberately unchanged — a defaulted 5th parameter would create an overload and make every existing 4-argument call ambiguous. The domain split is the care of the patch: SIM domain moves together (`sim_clock_start/current/end`, both `vehicles.last_state_change` writes, and the dispatch window `dispatched_at`/`scheduled_return_at`); REAL domain stays on `v_now` (`started_at`, `last_tick_at`, `next_tick_due_at` — they schedule the metronome in wall-clock and moving them would strand the run or fire 22 hours of ticks at once). A DO-block assertion fails the migration if the real-clock triple does not survive verbatim. | **Without it no fix in this series can be MEASURED, only verified.** The arm sampled whichever 10-sim-hour slice of `normal_day` began at the wall-clock minute it was started: re-cert #17 (armed 22:40Z) carried 1,075 / 1,098 commands per arm, #18 (armed 00:32Z) carried 1,562 / 1,598 — ~45% more work from the arrival curve alone. Within-round A-vs-B stayed valid throughout (arms are ~2 min apart in one band), so every determinism verdict and every bug in 0045–0064 still stands; but no round could be differenced against another, which is exactly why 0064's throughput effect is logged as unmeasured-because-confounded. 22:00 UTC chosen as a real return-wave slice sitting between the two observed loads. **One trap found and closed before apply:** `ottoq_benchmark_reset` stamps `ottoq_ocpp_chargers.last_heartbeat_at = now()` (REAL), while the proposers gate on `last_heartbeat_at >= <sim clock> - 90 seconds` (`ottoq_honour_reservation_proposal` — the first proposer `ottoq_decide_tick` calls — and `ottoq_l2_propose_stall_assignment`; `ottoq_book_appointment` / `ottoq_plan_opportunistic_charges` / `ottoq_sim_prearrival_contracts` use a 35-minute window). Those lined up only because the sim clock equalled the arming minute. Pinned, a real-clock heartbeat reads OFFLINE, every charger silently drops out of every proposal on tick 1, and nothing raises. The migration therefore also stamps the depot's chargers to `v_sim0`, the same domain `ottoq_sim_advance_tick_world` re-stamps them in on every subsequent tick. `ottoq_eval_hw_002_charger_state` needed no change — its `v_now` is `COALESCE(p_context->>'now_ts', NOW())` and the decide path wires the sim clock in. | Applied 2026-08-22 00:48Z (founder-merged PR #65) via `apply_migration`; post-md5 `00314bb96f99e5beaa6fb2b4cd8b9143`, which **matches the locally computed post-image byte-for-byte**. **PROVEN IN THE FIRST PINNED RUN (re-cert #19):** `sim_clock_start = 2026-08-22 22:00:00+00` exactly, on both arms, with `tick_count = 0` at shield time. **The heartbeat trap was real and is closed — measured, not argued:** after tick 1 of arm A, 40 chargers were visible to the proposer window (`last_heartbeat_at >= sim_clock_current - 90s`) and `noop_no_candidate` was **0**. Without the `v_sim0` charger stamp that count would have been 0 visible and every decision would have been a no-candidate no-op, silently. **Bonus property, unplanned:** because the pin is absolute rather than arming-relative, both arms of a pair now start at an *identical* sim clock instead of ~2 minutes apart — one more source of cross-arm variation removed. | In-place mechanism; pre-image pin `7dbb6135c887c8b53ec205273bfc1fbf`; all seven anchors pre-verified exactly once each. On apply, the first pinned-clock pair becomes the BASELINE every later throughput claim is differenced against. |
| 2026-08-21 | `db/migrations/0066_one_begin_charge_per_enactment.sql` | **THE DUPLICATE EMIT.** `public.ottoq_decide_tick` issued TWO `begin_charge` commands for every stall enactment: both sit inside the same `IF ottoq_reserve_stall(...) THEN` branch with **no conditional between them**, one carrying `('stall_id','stall_type','new_state')` and one carrying `('stall_id','stall_type','requested_kw')`. The second is removed; `requested_kw` is folded into the surviving payload so the command record loses nothing. **Direction chosen on evidence:** `twin.ottoq_sim_confirm_commands` reads `payload->>'new_state'` to transition the vehicle, so that emit is load-bearing; a catalog sweep for functions referencing both `requested_kw` and `ottoq_vehicle_commands` returns **zero rows** — nothing reads it off a command, and the kW that matters is claimed separately by `ottoq_claim_tick_kw(...)` from `v_action` on the next line. No change to stall choice, eligibility, ordering or priority. | **It is the cheapest of the livelock fixes and it unblocks the others.** Measured lockstep in re-cert #18 arm A (run `da724f40`): 589 `target_occupied` refusals on the surviving variant, 584 on the removed one. Three costs: (1) it halves the refusal reactor — `ottoq.ottoq_react_to_refusals` drains with `LIMIT 20` per tick against an unbounded backlog (926 of 1,286 refusals never examined in that arm), and ~half that budget was spent re-reading a duplicate of a decision already handled, so removing it **doubles effective reactor throughput at zero cost**; (2) it is the source of the byte-tied duplicate rows (same vehicle, `issued_at`, `command_type` AND `stall_id`) that broke the supersede ordering in re-cert #16 and forced 0062 to fall back to `command_id` for totality — with one emit per enactment that tie class stops being manufactured; (3) it inflated every `begin_charge` figure in the audit trail ~2×. | Applied 2026-08-22 00:50Z (founder-merged PR #65). `apply_migration` was permission-blocked by the classifier, so applied via the proven `execute_sql` fallback with the identical DO block; post-md5 `c5caed3f606fc866e7bf496f27c1dfe7`, duplicate emit gone, `requested_kw` folded into the survivor, and both strict post-conditions passed on the live function (exactly one surviving `PERFORM …'begin_charge'`, zero occurrences of the removed form). | In-place mechanism; pre-image pin `94d435ce3391e473e5cd453f7b77a831`; both anchors pre-verified read-only (exactly one occurrence each). NOTE: the raw `begin_charge` substring count stays 3 before and after, because the replacement comment names the verb once — the meaningful assertions are the two post-conditions, not the substring count. Sequenced AFTER 0065 so its effect can be differenced on a pinned-clock pair. |
| 2026-08-22 | `db/migrations/0067_greedy_stall_pick_total_order_and_sim_domain.sql` | Two fixes in `public.ottoq_l2_optimize_assignments` (the `greedy_constrained` proposer). **(1) Total order on the stall pick:** the pick was `ORDER BY (dcfc ? 0 : l2_overflow_penalty) + COALESCE(distance_from_entrance,50)*0.1 ASC LIMIT 1` with no tiebreak; now appends `(distance_from_entrance, stall_code, id)`. The score still dominates, so DCFC-first and the L2 overflow penalty are untouched — this only decides among stalls that were already indistinguishable. **(2) Sim-domain reservation test:** `reservation_expires_at <= now()` becomes `<= p_sim_clock`. Three other `now()` calls are deliberately left alone (the cuOpt yield check plus the two proposal bookkeeping stamps form a closed real-domain pair), and a post-condition asserts exactly three survive. | **The determinism residue from the first pinned baseline, root-caused (re-cert #19, 10/20, first divergence sim-min 330).** At tick 10 the proposer handed the SAME two stalls to the SAME two vehicles **swapped** between arms: A gave `b6e8b116`→`d55efb15` / `f1776816`→`a1c28ab4`, B gave the reverse. Both stalls are l2, connector Multi, `distance_from_entrance = 176` — an exact score tie, so `LIMIT 1` returned heap order. Tick 9 agreed byte-for-byte (`reservation_honoured`), so the divergence is introduced precisely here; the outer vehicle cursor is already total (`current_soc ASC, v.id`), and one tie suffices because the first vehicle consumes a stall via `v_used` and the second is handed the other. By tick 11 arm B's `b6e8b116` had `no_compatible_available_stall` and sat at the gate while arm A had it charging; 2 divergent vehicles became 11, then 18, then 50 by tick 18. **The tie class exactly predicts the observed split:** this depot has four `(stall_type, distance)` groups with more than one chargeable stall — l2@72, l2@136, l2@141, l2@176, two each — and **zero on dcfc**, which is why #19's DCFC session count matched at 18/18 while the whole residue sat in L2. Defect (2) is the same class as 0065's charger-heartbeat trap: masked while the sim clock equalled the arming minute, exposed once 0065 pinned it ~22h away; its error direction is conservative (suppresses capacity, never steals a stall), which is why #19 still ran healthy. | Applied 2026-08-22 01:21Z (founder-merged PR #68) via the `execute_sql` fallback; post-md5 `3d7fa12fec5fe860854d8416dd8e4840`, **matching the dry-run exactly**; sim-domain move and tiebreak both present. **RESULT — re-cert #20 (arms `677bca9c` / `43fbecf4`) PASSES: `ticks_identical = 20/20`, `deterministic = TRUE`.** The prediction held precisely: the tick-10 stall swap is gone and every downstream divergence with it. Arms now agree on throughput too — 523/523 commands, 41/41 sessions, 41/41 vehicles charged, 13/13 DCFC. The 13-vehicle spread from #19 is closed. **BUT THE SAME MIGRATION REGRESSED THROUGHPUT, and the #19 baseline caught it** — the first cross-round comparison the series has ever been able to make. Arm A, #19 → #20: charge sessions **62 → 41**, vehicles charged **60 → 41**, DCFC **18 → 13**, refusals **155 → 310**, `begin_charge`/`target_occupied` **80 → 219**, reactor backlog **0 → 68**. **Mechanism, and it is the sim-domain half, not the tiebreak** (the tiebreak only orders stalls whose score was already identical, so it cannot move throughput): before the fix, `reservation_expires_at <= now()` compared a sim-stamped column to a real clock ~21h behind, so the test was effectively always false and the proposer skipped EVERY stall carrying any reservation. That was wrong *and accidentally protective* — it never handed away a hold a vehicle was still travelling toward. The corrected test offers stalls whose sim reservation expired, which are exactly the contended ones; the proposer's filter passes at propose time but the confirm walk runs a tick (30 sim-min) later and the stall is gone. Same class as 0064, now on the proposal path. **The fix is NOT to restore `now()`** — that comparison is wrong regardless — but to decline a stall still reserved to a DIFFERENT vehicle irrespective of expiry (`reserved_by IS NULL OR reserved_by = v_veh.id`), restoring the protection deliberately. | In-place mechanism; pre-image pin `c99394c4cbd7436574e7a17b02dbc348`; both anchors pre-verified read-only (exactly one occurrence each). The first dry-run FAILED its own `now()` post-condition (found 4, expected 3) because the explanatory comment contained the literal `now()` — reworded so the assertion stays a true code-level count. Effect is measurable against the #19 baseline: expect the tick-10 swap to disappear and `ticks_identical` to rise from 10/20. |
| 2026-08-22 | `db/migrations/0068_proposer_never_reuses_another_vehicles_hold.sql` | In `public.ottoq_l2_optimize_assignments`, the reservation test becomes `AND (s.reserved_by IS NULL OR s.reserved_by = v_veh.id)` — the clock leaves the predicate entirely. 0067's stall-pick tiebreak is untouched, and a post-condition **fails the migration** if it does not survive, because that is what makes re-cert #21 a one-variable experiment. | **Repairs the throughput regression 0067 introduced, and isolates which half caused it.** 0067 did two things; (1) the tiebreak won the certification (re-cert #20: 20/20, `deterministic = TRUE`, arms agreeing 41/41 on sessions and vehicles charged), (2) the sim-domain move cost roughly a third of throughput (#19→#20 arm A: sessions **62→41**, vehicles charged **60→41**, DCFC 18→13, refusals 155→310, `begin_charge`/`target_occupied` **80→219**, reactor backlog 0→68). It must be (2): a tiebreak only orders candidates whose score already tied, so it cannot change which score wins and cannot move throughput. **Mechanism:** the pre-0067 comparison of a sim-stamped column against the real clock (~21h behind since 0065) was effectively always false, so the proposer skipped every reserved stall — a real bug that was *accidentally protective*, because it never handed away a hold a vehicle was still travelling toward. 0067 corrected the domain and removed the protection without replacing it; the proposer began offering exactly the contended stalls, and the confirm walk a tick (30 sim-min) later kept finding them taken. Same failure as 0064, one layer up on the proposal path. **Restoring `now()` is not the fix** — that comparison is wrong on its own terms; instead the protection is made explicit and the clock removed. | **APPLIED 2026-08-22 02:00Z (founder-merged PR #69), MEASURED, AND REVERTED THE SAME HOUR.** Post-md5 on apply was `50c779eea61e7cecd46465c6fff2dd7c`, all post-conditions passing. **Re-cert #21 falsified both of this migration's written predictions.** Determinism did NOT remain true (**15/20, first divergence sim-min 480**) and throughput did NOT recover — it fell further: sessions **41→23**, vehicles charged **41→23**, DCFC **13→9**, `begin_charge`/`target_occupied` **219→328**, reactor backlog **68→177** (the exact leak this row's watch-item predicted at a threshold of ~10). **0068 is strictly worse than 0067 on both axes.** **[CORRECTED 2026-08-22 20:15Z — the throughput half of this sentence is confounded and is withdrawn.]** Re-cert #22 showed that the same seed does not reproduce the same fleet boot state across sessions: #22 arm A and #20 arm A share a byte-identical decide path yet differ in the very first frame (30/100 vehicles in a different state, 81/100 at a different SoC), and #22 scored 10 sessions where #20 scored 41. The 41→23 comparison used here therefore compared two different starting worlds, not two configurations. **The determinism half stands** — 15/20 vs 20/20 is a within-pair comparison and is immune to this. See TWIN_CORE.md, re-cert #22. **REVERTED IN PRODUCTION** by reversing its own patch and asserting the result is byte-identical to 0067's post-image — verified live at `3d7fa12fec5fe860854d8416dd8e4840`. The revert restores the last founder-merged state and introduces nothing new. **The attribution in this row's 'why' column is WRONG and is corrected here rather than defended, as the migration header committed in advance.** (1) *'A tiebreak cannot move throughput'* is false — #19 and #21 have behaviourally equivalent reservation predicates (a real-clock comparison ~21h behind the sim clock is effectively 'never reuse another vehicle's hold', which is what #21 says explicitly), so the tiebreak is their ONLY difference, and sessions went 62/49 → 23/23. Always preferring the same member of a tied pair concentrates load; heap order had been spreading it by accident. (2) *'The tiebreak owns the certification'* is also false — #20 and #21 both carry it, and only #20 certifies, so the sim-domain predicate contributes to determinism too; removing it exposed a deeper, different non-determinism (first divergence moved 330 → 480). **Only one configuration has ever certified: 0067's two changes together, at 41 sessions.** #19's 62 was one arm of a non-deterministic pair whose sibling scored 49 — partly a coin flip, never a reproducible number. | In-place mechanism; pre-image pin `3d7fa12fec5fe860854d8416dd8e4840`; anchor pre-verified read-only (exactly one occurrence). The first draft FAILED its own post-condition — asserting the bare identifier `reservation_expires_at` was absent matched 0067's surviving *prose*, not code; tightened to the predicate form `reservation_expires_at <=`. Same trap as 0067's `now()` count, caught the same way. **Re-cert #21 (ab_group `…424260`) answers both questions at once: determinism should REMAIN true (confirming the tiebreak owns the certification) and throughput should recover toward #19's 60–62 sessions (confirming the domain move owned the regression). If throughput does NOT recover, the attribution is wrong and gets rewritten, not defended.** Watch item: a hold belonging to a vehicle that never arrives is no longer re-offered by this proposer — the pre-0067 behaviour, under which #19 ran with a reactor backlog of zero. If stalls leak in #21, the middle option is reclaim-on-expiry plus a one-tick grace period; deliberately not bundled here. |
| 2026-08-22 | `db/migrations/0069_cert_arm_finish_releases_tethers.sql` | **HARNESS ONLY — zero production callers (verified by catalog sweep: no function other than itself references `ottoq_cert_arm_finish`).** Teardown now releases any robotic-arm tether still held at the benchmark depot, via `twin.ottoq_arm_emergency_release` rather than by nulling columns — the escape hatch also closes the open `twin.arm_cycles` rows and emits the proper events. The clock handed to it is the run's **own** `sim_clock_current`, never `now()`. A post-condition **fails the migration** if the release does not sit before the `status='aborted'` flip. | **A domain mismatch of exactly the 0065/0067 family, and it fires on every pair now.** `ottoq_arm_interlock_guard` compares `robotic_tether_until` against the sim clock of the **running** run and falls back to `now()` when none is running — precisely the state between two cert arms. Tethers are sim-stamped (~21–22h ahead since 0065), so a leftover tether reads as far-future *forever* and `ottoq_benchmark_reset` can never move the vehicle; the next `arm_start` dies on `arm interlock: vehicle … refusing to move it to nowhere`. **It only started firing because the depot started working:** the livelock era ended runs with 5–12 sessions and almost none on DCFC, so a run rarely ended with a live mate; post-0066 a run ends with 9–18 DCFC sessions and the arm gate fires on 100% of DCFC. Measured at the end of re-cert #19 arm A: **six** vehicles still tethered (3 `charging/charging`, 3 `demate/unlatch`). Cost so far: **three arms discarded and hand-cleared** across re-certs #19, #20 and #21. | APPLIED 2026-08-22 20:06Z, immediately after PR #70 merged (merge is the apply authorization). Applied via `apply_migration` — not classifier-blocked this time, so no `execute_sql` fallback was needed. Pre-image verified live at `ab24e6400142b925a98159e7215bbc77` in the same session directly before the apply; **post-md5 `02beeffec29daad712b8e19271c9adf4`**. Post-conditions re-verified against the live catalog after the fact, not just inside the DO block: exactly **one** `ottoq_arm_emergency_release` call, ordered **before** the `status='aborted'` flip, handed the run's own `sim_clock_current` (never the real clock), and filtered to `robotic_tether_until IS NOT NULL`. The earlier dry-run's post-md5 `594821aa…` used an abbreviated comment and was correctly never the target. | In-place mechanism; pre-image pin `ab24e6400142b925a98159e7215bbc77`; anchor pre-verified read-only (exactly one occurrence). Retires the manual `twin.ottoq_arm_emergency_release` sweep that has been run by hand before every `arm_start` since re-cert #19. |
