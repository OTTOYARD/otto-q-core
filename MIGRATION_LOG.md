| Date | File | What changed | Why | Applied by | Verified |
|---|---|---|---|---|---|
| 2026-08-09 | `db/migrations/0026_fix_staging_sort.sql` | **P1-3: Fixed staging-selection order to prefer perimeter before temp.** Altered `ORDER BY (s.staging_role = 'temp') ASC` in both `ottoq_book_appointment` and `ottoq_sim_prearrival_contracts`. | The backward `DESC` in these ORDER BY clauses consumed the 24-spot temp buffer before the 176 perimeter stalls, violating the doctrine that perimeter should fill first. This change makes perimeter spots (where staging_role is not 'temp') sort before temp, so perimeter fills first. | Hermes Agent (qwen/qwen3-235b-a22b-2507, nous) | **Verified by inspection:** Migration file contains two CREATE OR REPLACE FUNCTION blocks with `(s.staging_role = 'temp') ASC` — the correct fix. No direct DB verification done; per AGENTS.md, verification happens after PR.