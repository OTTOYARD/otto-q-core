# FINDINGS.md — the open findings register

Every finding this build track has convicted but not yet closed, with the file that
convicts it. Written 2026-09-08 because the register existed only in agent session
state: `db/checks/` holds the evidence and `db/migrations/` holds the fixes, but
nothing in the repo said which findings were still **open**. A finding that lives
only in a session dies with it.

**Scope and provenance.** Reconstructed from the working task register on 2026-09-08.
Items are listed with the file that convicts them where one exists, and marked
*not yet written up* where the finding is recorded only as a task line — those
entries state the task line verbatim rather than a reconstruction of it.

Closed items are listed too, in one line each, because "what has already been
fixed" is the question a reader asks second.

---

## Open

| # | Finding | Evidence | Fix | State |
|---|---|---|---|---|
| **G100** *(**INSTRUMENTED, REMEDY PENDING** — [0291](db/checks/0291_where_the_layers_need_translators_answered_from_the_seams_not_from_a_diagram.sql), instrument built by [0392](db/migrations/0392_the_shields_coverage_was_only_ever_counted_from_the_rules_side_so_two_enacting_branches_were_invisible.sql). The remedy is a tick-path change, `forces_recert TRUE`, and it waits for the live run to finish.)* | **Every L1-shield coverage figure this repo has ever published was counted from the RULES' side, which is structurally incapable of finding an enactment that consulted no rule. Counted from the ACTIONS' side, 119 of 859 enacted decisions on run `e8b8eb3e` carry no rule evaluation at all — and 60 of those, on two branches, take a physical action.** `0192`, `0273` and CLAUDE.md 2.5 all answer *of the declared rule codes, how many are evaluated* (20 of 29 at four probes, then 21 of 30 at six, then `0263` §1's optimistic bound), joining `ottoq_rules` to `ottoq_rule_evaluations`. A branch that never calls `ottoq_shield_probe` contributes zero evaluations, so it moves neither numerator nor denominator on that side — **it is not a low score, it is absent from the exam.** G44 has described the nine rules with no caller for eight days; nothing had described the actions with no rules. | `db/checks/0291` §2 and `ottoq_assert_shield_coverage('e8b8eb3e-…')`; five branches each traced to its writer by one `prosrc` query matching both the context string and an INSERT into `ottoq_decisions` | **THE FIVE BRANCHES, AND ONLY TWO ARE A GAP — saying so is the point, because "86.1% shielded" invites the fix where it does harm.** `orchestrator_agent` (nemotron, 48) — **no database writer at all**, because the agent layer writes `ottoq_decisions` over the REST API from an edge function; that is G62's `agent_calls_with_no_l1_rules = 1,120 of 1,120` reproduced on a fresh seed and localised to one context. `itinerary_amended` (30, `twin.ottoq_sim_advance_flow_contract`) and `triage_verdict` (16, `twin.ottoq_sim_advance_visit_atoms`) are the **twin advancing its own world** — shielding them would ask the rules' permission for something the world already did. `bay_reconcile` (5, `ottoq.ottoq_bind_unbooked_bay_occupants`) binds a vehicle **already physically in a bay** — the `space_conflict_ledger` posture rule 6 requires, so gating it would mean refusing to write down a fact. **`gate_intake_no_charge` (12, `public.ottoq_decide_tick`) IS a gap:** its `(3b) GATE INTAKE — NO-CHARGE ARRIVALS` loop calls `ottoq.ottoq_book_stall` for a staging stall, stamps `to_stall_id` on the itinerary leg, writes `'enacted'`, and emits `proceed_to_stall` — **a stall claimed and a vehicle commanded to drive to it, with no `ottoq_shield_probe` anywhere between the loop's `FOR` and its `END LOOP`.** `0263` §1 came within one step of this by finding that four of the five codes at `stall_assignment` are charge-specific and cannot judge a parking hold. **AND `l2_engine` cannot be used to find these:** `trg_ottoq_stamp_l2_engine` sets `COALESCE(NULLIF(enacted_action->>'source',''), l2_engine, 'deterministic_v1')`, and on all 42 unshielded `task_start` rows `enacted_action->>'source'` is NULL — so their `deterministic_v1` label is the trigger's **fallback**, and *"deterministic_v1 did it"* is indistinguishable from *"nobody stamped it"*. Same defect shape as G99. | **open — instrument LANDED, remedy waiting.** `0392` adds `public.ottoq_enactment_branches` (a declared vocabulary for `resolved_action_context`: writer, side of the sim boundary, and whether a shield is **expected**), plus `ottoq_shield_coverage(run)` and `ottoq_assert_shield_coverage(run)`. Declared rather than hardcoded for the reason `ottoq_kpi_touch_actor_types` declares which actors are human — the per-branch judgement is what a reader must be able to audit. **The number to act on is `unshielded_gap`** (unshielded AND either declared `shield_expected` or not declared at all); `unshielded_by_design` is the remainder and is not a defect. P4 asserts both sum identities, the check `0341` had to add to `ottoq_intelligence_ledger` after its first version bucketed 1,161 rows nowhere. **The underlying deficiency this exposes: `ottoq_decisions` does not record which function wrote it** — twelve insert sites in `ottoq_decide_tick` alone, eight database functions plus one edge function, with free text as the only proxy. **Remedy, both halves `forces_recert TRUE` and both deferred until run `e8b8eb3e` completes so the 0390 measurement is not perturbed:** (1) route the agent through `ottoq_external_proposals` as `llm_advisor` — which `ottoq_proposer_precedence` already declares at **rank 20, `holds_tick=true`, `greedy_yields=true`**, and which has submitted **0 proposals ever** (0 live, 0 of 6,681 evidence-ledger rows) — closing the ungated path AND G62's latency defect at once, because a proposal is inherently asynchronous; (2) add the probe to the gate-intake loop. **Not claimed:** that any unshielded enactment has actually violated a rule — no rule was evaluated, so nothing is known either way, and that is the whole finding. **CORRECTED 2026-09-20 BY [0292](db/checks/0292_g100s_agent_claim_is_wrong_the_dials_are_gated_at_policy_write_and_the_gap_is_attribution.sql), FIXED BY [0393](db/migrations/0393_the_branch_i_led_0392_with_is_gated_at_a_probe_point_i_did_not_look_for.sql) — THE `orchestrator_agent` HALF OF THIS ROW WAS WRONG, IN THE DIRECTION RULE 6 CALLS MORE DANGEROUS, AND IT WAS A SAFETY CLAIM.** I wrote that the agent is *"the one path on which an AI changes engine state and the only path with no L1 evaluation"*. **The agent's dials ARE L1-gated.** The chain, four hops, each one query: (1) the edge function writes ONE **depot-scoped** `ottoq_decisions` row per tick whose whole effect set is dial writes and ops actions; (2) `public.ottoq_apply_ops_action`'s every whitelisted branch is a `ottoq_policy_set` call **and nothing else**, each clamped in its own branch, with anything off-whitelist INSERTed into `ottoq_ops_approvals` as `pending` — the human queue — so **the agent's entire physical reach is policy dials: no stall, no vehicle, no booking**; (3) `public.ottoq_policy_set` calls `ottoq_shield_probe` with `policy_write`; (4) **254 `ottoq_rule_evaluations` rows of `AI.001.agent_dial_within_envelope` at `policy_write`/`policy_param` on run `e8b8eb3e` — the very run 0392 measured.** So the edge function's own header (*"L1 shield still gates every physical effect; vehicle-first inviolable"*) is **accurate**, and I contradicted it from the data without reading the path it describes. **What is real is an ATTRIBUTION gap, and it is much smaller:** the evaluation happens at a different `action_context`, on a different `entity_type`, and **nothing joins the two** — so `agent_calls_with_no_l1_rules = 1,120 of 1,120` (CLAUDE.md rule 6, from 0340) is a **correctly computed metric with a misleading name**: it measures *"the agent's row does not carry its own rule results"* and reads as *"the agent is ungated"*. Third instance of the `offerable` shape in two days, and this time the misreader was me. **AND THE REMEDY CHANGES SHAPE, which is why this is a check file and not a quiet edit:** routing the agent through `ottoq_shield_and_log` — 0392's proposal — would have **closed the finding on paper and changed nothing**, because that function hardcodes `entity_type='vehicle'` and `CONTINUE`s on any action with no `vehicle_id`, so it would skip every agent row it was given. What is needed is a **`correlation_id`** (the column already exists on `ottoq_rule_evaluations`) threaded from the agent's decision into `ottoq_policy_set`'s probe call. Additive, and it changes nothing about what the shield decides. **WHAT SURVIVES, AND IT IS STILL WORTH HAVING:** the rules-side blind spot is untouched (§3 of 0292 is itself a rules-side count reporting 23 of 30, while `ottoq_assert_shield_coverage` reports 170 unshielded enacted decisions on the same run — both right, different questions, and only one was being asked before 0392); the instrument stands and **is already earning its keep** (a previously unseen undeclared unshielded branch, `reservation_reopt`, surfaced as the run advanced); and **`gate_intake_no_charge` is still a real gap and is now the only declared one** — it is vehicle-scoped, so `ottoq_shield_and_log` fits it exactly, and unlike the agent there is no second probe point downstream because a staging booking is not a policy write. `0393` sets `orchestrator_agent` to `shield_expected=false` with the reason and the probe point recorded, and its preflight **asserts all four hops** rather than trusting the note — because getting this wrong in either direction is a safety claim. **AND 0292 §3 RETIRES A SECOND STALE SENTENCE, ALSO IN OUR FAVOUR:** CLAUDE.md 2.5 says *"twenty-one of thirty declared rules, at six decision points"*. Measured on this run: **23 of 30, at EIGHT probe points** — the new two are `vehicle_state_change` (628 evaluations, SM.001) and `stall_state_change` (581, SM.003), both of which `0273` listed among its nine unevaluated codes and both of which **`db/migrations/0387` wired hours after `0273` was written**. `0273`'s caveat survives and still governs: evaluated is a WIRING count, not a PROTECTION count, so 23 of 30 is the optimistic bound. Seven remain unevaluated, four `critical` — HW.006, SM.004, SM.005 (G97's population), SM.006 (G96), plus SLA.002, TW.002, TW.004. **The lesson, stated so it generalises:** 0392 was built on `resolved_action_context` — a field that says what a decision was ABOUT — and I read it as though it said what had been CHECKED. **An audit instrument is not evidence about the thing it is pointed at until the path it measures has been read end to end**, and 0392 shipped with four of five branches read that way and the fifth, the one I led with, not read at all. |
| **G99** *(**MEASURED, NOT FIXED** — [0289](db/checks/0289_offerable_requires_a_charger_so_every_staging_stall_reads_unavailable_forever.sql). The remedy changes the frame contract, so it is `forces_recert` territory and a design choice.)* | **The decision frame's `offerable` requires an OCPP charger, so all 113 staging stalls, 3 wash bays and 2 service bays read false forever — 118 of the twin depot's 158 stalls.** `ottoq_build_decision_frame` builds it as `(current_vehicle_id IS NULL AND ocpp_charger_id IS NOT NULL AND station_state='Available' ...)`, and a staging stall's own frame row reads `"ocpp_charger_id": null, "connector_type": "NonCharging"`, so the conjunction is false before any gate is consulted. **AND I ALMOST PUBLISHED A CAPACITY CLAIM FROM IT.** Following `0288`'s capacity thread I counted staging availability from the frame and got *"113 staging stalls, `offerable` 0 in all 611 snapshots, mean 100.0% occupied"* — a dramatic, quotable number about the one depot rule 8 exists to characterise, and **wrong**. | `db/checks/0289` §1-2; the three gates side by side at 07:00 CT tick 910, plus the clause read from `pg_proc` | **WHAT STOPPED IT was asking which GATE says no, rather than accepting one field.** Measured mid-wave: **pointer-free 76**, **calendar-free 77**, `offerable` **0**, of 113. 76 and 77 of 113 cannot intersect in zero — by pigeonhole **at least 40** staging stalls were free on both real gates at that instant. **AND THIS IS NOT A DEFECT IN `offerable`.** `0265`/L-61 added the key for *"the three facts the selector refuses on"*, and that selector is the CHARGE-stall selector; its own comment states the contract exactly (*"offerable=false means no proposal can win it"*), and `0372` deliberately added the charger-health term. For a charge stall it is right. **The defect is in the READER, and tonight the reader was me.** `proposer/forward_proposer.py` is unaffected — it consults `offerable` only for charge-capable stalls, the population the key describes. **The rule this earns, which generalises past this field:** a boolean named for an OUTCOME (*offerable*) rather than its DOMAIN (*charge-stall offerable*) will be read outside its domain, and the reading will look like a measurement. CLAUDE.md's three-gate rule says to name which gate refused; this adds that you must first check the predicate is **defined for the population you are counting**. | **open.** **An open question it leaves, stated as a question:** **792 `twin.staging_overflow` events** fired on this run (422 night tail, 370 wave, continuous 00:42-09:37 CT, against a prior run's 258) while at least 40 staging stalls were provably free on both gates. **I am NOT claiming `offerable` causes the overflow** — the producer is a twin event and may read the pointers or calendar directly, never the frame; establishing the link needs it read end to end and that is the next measurement. What IS established: 792 overflow events and ~40 free staging stalls coexist, so at least one of the two is not measuring what its name says, and **`0288` §3's capacity conclusion must not be extended into a claim about STAGING capacity until that is resolved.** **Three remedies, none small:** (a) rename to `charge_offerable` — honest, and `forward_proposer.py` pins `OFFERABLE_KEY` while the frame is digested into `frame_hash`, so `forces_recert TRUE` invalidating all nine canon columns; (b) add a second fact for non-charge stalls (pointer AND calendar, no charger term) — additive, same recert cost, and it gives future readers the thing they will otherwise fabricate; (c) leave it and document, which is what 0289 does. **(b) is what I would build**, gated on someone actually needing staging availability in the frame — today no consumer does, which is precisely why the field has been wrong for 118 stalls without anything breaking. |
| **G98** *(**MEASURED, NOT FIXED** — [0288](db/checks/0288_cp_sat_was_starved_not_weak_and_feeding_it_did_not_give_it_influence.sql), **TWO NUMBERS CORRECTED BY [0290](db/checks/0290_g98s_two_percent_was_the_wrong_denominator_and_the_churn_is_the_real_defect.sql) — see the end of this row; the direction survives, the arithmetic did not.** The next measurement is named in the Fix column; no migration accompanies this.)* | **CP-SAT was STARVED, not weak — and feeding it did not give it influence. 12 of 610 proposals enacted, 2.0%.** Within ONE run (`c8f678fb`, busy_day, seed **100020** chosen so its 540-sim-minute window spans 00:35-09:37 CT and therefore contains both the night tail AND the morning return wave): **night tail 258 fires, 8 planning (3.1%), 14 vehicles planned; morning wave 106 fires, 68 planning (64.2%), 383 vehicles planned** — 27x the planning volume on 41% as many fires, with `arrived_at_gate` going 0 -> 5 and `staged_awaiting_service` 1 -> 19. Same seed, same engine, same descriptor logic: the only difference is how many vehicles wanted a stall. So `0282` §3's night figures were a TROUGH, not a ceiling. **But the bottleneck moved rather than cleared:** of 610 `forward_lex` proposals across 56 vehicles (**10.89 per vehicle**), **539 (88.4%) superseded `entity_decided_by_other_proposal`**, 20 superseded `newer_proposal_same_entity`, 20 `proposer_abstained`, 16 `stall_reserved`, **12 enacted (2.0%)**. | `db/checks/0288` §1-2; the fire log split on the snapshot clock at 11:00 UTC, and the disposition census joined to `ottoq_decisions` | **WHO SUPERSEDES THEM IS SETTLED, NOT GUESSED.** All other external proposers put **9 rows total** on this run (`greedy_constrained` 4, `ottoq_service_priority` 5), so they cannot account for 539. Tested directly: **538 of the 559 superseded rows (96.2%) have a local decide-path `stall_assignment` decision for the SAME vehicle at the same tick or the next.** So: **CP-SAT is no longer starved, it is out-voted** — the declared rank-0 proposer with `holds_tick=true` and `greedy_yields=true` plans 383 vehicles in three sim-hours and 2% reaches the calendar. This is `0184`'s tie-break finding (*"a forward_lex row is heard but LOSES the tie-break to the local path's regenerated row by created_at"*) and `0255`'s supersession dominance, both at volume for the first time. **10.89 proposals per vehicle is the same mechanism's second half:** a superseded proposal is no longer live, so `vehicle_is_held` admits the vehicle again and the next fire re-plans it — G60/`0292`'s churn, except the overwriting is now mostly done TO the proposer rather than BY it. **AND THE CAPACITY WALL APPEARS HERE FOR THE FIRST TIME**, which is what rule 8's depth goal actually asks for: `p95_time_to_service` **39.6 min** against the night run's **2.7**, and `returns_unserved` **11** against **0**, while turns/point/day rises 2.48 -> 3.56 and asset-hours fall 102.51 -> 57.89. The depot works harder and loses ground — and this only becomes visible when the arrival wave exists, which is why every prior measurement of this depot looked comfortable. **G97 also reproduces on this second seed**: touch_events 610 = operator 78 + override 532, so 87% is the shield again and the human figure is 0.140. | **open — AND THE NEXT MEASUREMENT IS NOW DONE (`0288` §6): IT IS A WIRING GAP, AND THE REMEDY IS A DESIGN DECISION.** Hold coverage PER PROPOSAL, the denominator §5 refused to guess at: of 559 superseded proposals only **14 (2.5%)** had a deferral spanning that vehicle at that tick, though **414 (74.1%)** were for vehicles held at SOME tick. So the hold is armed for three quarters of these vehicles and almost never when the proposal was made. **The cause is one query:** neither door arms a hold — `ottoq_proposer_submit_batch` and `ottoq_submit_external_proposal` both mention `first_refusal` NO and write `ottoq_cuopt_deferrals` NO; only `ottoq_cuopt_first_refusal_arm`, called from `ottoq_decide_tick`, does. **The right-of-first-refusal is armed by the DECIDER, not by the DOOR, so submitting a proposal buys no protection.** That is coherent rather than careless: the mechanism's name says `cuopt`, and the cuOpt path runs the other way round — the decide tick initiates the request, so the decider arms the hold BEFORE dispatching and the proposal returns into a seat already reserved. The CP-SAT bridge inverts it: an external process pushes a proposal in at an arbitrary moment between ticks, gets no seat, and by the next tick the local path has decided the entity. That is the 96.2% mechanically. **Three remedies with different meanings:** (a) arm at the door — strongest, and lets any external proposer stall the kernel; (b) arm at the tick boundary for whatever is pending — keeps arming with the decider, one extra read per tick, cannot be abused by submission timing; (c) leave it and accept CP-SAT as advisory — honest, and then **2% enactment is the designed outcome and every claim about CP-SAT's contribution must be quoted at 2%**. **(b) is what I would build** and it is NOT built: arming from the door changes when the deterministic path is required to defer, on the tick path, affecting throughput — the class CLAUDE.md 2.5 reserves and rule 6 puts on the product side. Chase's call. **I explicitly refused to claim the hold is broken.** `proposer_hold_enabled=1` was armed (7 of 7) and the machinery IS firing — `ottoq_cuopt_deferrals` recorded **63** holds, every one spent AND cleared, across 63 vehicles and ticks 1-1203. 63 against ~610 proposals LOOKS like a tenth of coverage, but a hold may be armed once per vehicle-VISIT rather than once per proposal and I do not have that denominator. **A ratio without its denominator is the unit error `0284` §3 and `0285` §1 both record; three times in one night is enough.** **CORRECTED 2026-09-20 by `0290`, and in the direction that UNDERSTATED our own solver, which rule 6 forbids exactly as firmly as the flattering direction.** **(i) "12 of 610 proposals enacted, 2.0%" is the wrong denominator.** 610 is proposals SUBMITTED, and a vehicle takes at most one enacted assignment however many times it is proposed for. The run made **240** enacted `stall_assignment` decisions across 98 vehicles: **deterministic_v1 130 (54.2%, 77 veh)**, reservation_honoured 47 (19.6%), greedy_constrained 22 (9.2%), cuopt 15 (6.3%), **forward_lex 12 (5.0%, 12 veh)**, reservation_reopt 12 (5.0%), reservation_reassigned 2. **The influence figure is 5.0% of enacted assignments, not 2.0% of anything.** **(ii) "96.2% superseded by the local decide path" was a tautology wearing a percentage** — that query read `ottoq_decisions` with `action_context='stall_assignment'` WITHOUT filtering on source, and that table records every enacted assignment whatever proposed it, including forward_lex's own 12 and cuopt's 15. It means *"the vehicle got decided by someone"*. The defensible version is the table above: `deterministic_v1` is the single largest winner at 54.2%. G98's direction was right; its evidence was not. **(iii) THE HOLD REMEDY IS WITHDRAWN.** `ottoq_cuopt_first_refusal_arm` explicitly EXCLUDES any vehicle that already has a pending `holds_tick` proposal (*"cuOpt has already answered for this vehicle => let the cursor enact it now"*), so the hold is a PRE-proposal seat reservation and 2.5% coverage is the DESIGNED behaviour, not a gap — option (b) would have inverted the function's own intent. The hold is working. **(iv) THE REAL DEFECT IS CHURN: 610 proposals, 56 vehicles, 12 enactments — 50.8 proposals per enactment.** `vehicle_is_held` withholds a vehicle only while it carries a LIVE PENDING proposal, and `ottoq_dispose_external_proposals` runs every tick, so a superseded proposal makes its vehicle admissible again and the next fire re-plans it against a frame that has barely moved. **A churning proposer slanders itself:** fire once per vehicle-decision and the same 12 enactments would read ~21% instead of 2%. **Act in this order — (1) cut the churn (proposer-side, no tick-path change, no recert); (2) THEN ask whether `deterministic_v1` should yield to a pending rank-0 proposal the way `ottoq_l2_optimize_assignments` already yields via `greedy_yields`** — symmetric completion of an existing mechanism, but a tick-path change, `forces_recert TRUE`, and uninterpretable until the instrument is fixed. **Not claimed:** that CP-SAT would fix the capacity wall or that the local path causes it. Pricing the difference needs C5's CRN-paired A/B with the shield held constant (`0146`), which does not exist yet. One run, one seed, one wave. |
| **G97** *(**MEASURED, NOT FIXED** — [0286](db/checks/0286_kpi_four_counts_the_safety_shield_as_human_labour_and_overstates_itself_by_eight_and_a_half_times.sql). The remedy is one line and I have deliberately not applied it; see the Fix column.)* | **KPI 4 — `touch_events_per_turn`, "human interventions per asset-turn" — counts the deterministic shield's own safe-default fallbacks as human labour, and published 1.459 on a run whose human-attributable figure is 0.171. 8.5x.** On run `1efeb1cd` (busy_day/101959, 1,059 ticks): 572 touch events over 392 turns, of which **touch_events_operator 67** and **touch_events_override 505**. **All 505 are the engine** — every one carries `l2_engine='deterministic_v1'` and `outcome_status='overridden_to_default'`, the L1 shield declining a proposed action and taking its safe default (272 `task_start`, 231 `redeployment`, 2 `stall_assignment`; 4.2% of the run's 11,943 decisions). **That is automation refusing to act — the opposite of what KPI 4 exists to count.** **Note the direction: this overstates human labour, so it makes the product look WORSE than it is.** | `db/checks/0286` §1–3; the view's two halves side by side, plus the override-column census over all 12,082 decisions currently held | **THE DEFECT IS AN ASYMMETRY INSIDE ONE VIEW.** `ottoq_kpi_touch_events_per_turn`'s operator half filters `actor_type IN (SELECT actor_type FROM ottoq_kpi_touch_actor_types WHERE human_actor)` — a careful table that marks `ottoq_engine`, `system`, `system_scheduler`, `solar_controller` and `unknown` as non-human, with `unknown` carrying its own note (*"deliberately NOT a touch: counting unknown as human would inflate KPI-4 with every unattributed row"*). Someone thought hard about exactly this failure. **The override half applies no actor filter at all** — `WHERE d.overridden OR d.override_id IS NOT NULL` — so the principle the view spends a whole lookup table enforcing on one side is absent on the other, and that side contributes 88% of the number. **AND THE COLUMNS THAT WOULD EVIDENCE A HUMAN HAVE NEVER BEEN WRITTEN:** across all 12,082 decisions held, `override_id IS NOT NULL` **0**, `override_reason IS NOT NULL` **0**, `override_rule_code IS NOT NULL` **0**, `overridden` true with no `override_id` **522** (all of them), and `ottoq_rule_overrides` holds **0 rows**. No human override has ever been recorded anywhere in this engine, so the `OR override_id IS NOT NULL` clause has never matched a row. **This is simultaneously a measurement of SM.005's population:** that `critical` rule ("audit note on override") is one of the seven with an evaluator and no caller, its subject is these 522 unaudited overrides, and unlike SM.006 in `0285` the input it needs does exist — it is simply, uniformly, absent. | **open.** The narrow fix is to give the override half the filter the other half has, counting only overrides that evidence a human: `WHERE d.override_id IS NOT NULL`, which on today's data makes KPI 4 read **0.171**. **I have not applied it, for two reasons and the first is the real one.** (a) It changes a published headline KPI **in the direction that flatters us** — a change that improves the automation claim 8.5x must not be made by the person who found it on their own reading of what `overridden` means; rule 6's "forbidden in both directions" applies to fixes as much as to sentences. (b) `overridden` may carry a meaning I have not found: every instance today is `deterministic_v1`/`overridden_to_default`, but the column is old, `ottoq_decisions` is `class='engine'` and purged, so "all 12,082" is the current contents rather than the engine's life — if some caller sets `overridden` for a genuine human action without writing `override_id`, the proposed predicate would UNDER-count. **Recommended: apply the predicate change together with wiring SM.005**, because they are one finding seen twice — fixing the KPI alone leaves the engine free to keep producing unaudited overrides that no longer show up anywhere. **Not claimed:** that KPI 4 has always been wrong by 8.5x (the factor is this run's mix; what is run-independent is the missing filter and the empty columns), that the other four KPIs are affected (they are not — this is the only one reading `overridden`, and all eight published fields still report `not_reproducible: []`), or that 0.171 is the true figure rather than the figure under the recommended predicate. |
| **G96** *(**MEASURED, NOT FIXED** — [0285](db/checks/0285_sm006_forbids_exactly_one_thing_and_the_battery_does_it_3426_times.sql). No migration accompanies it, and the reason is in the Fix column: two of the three decisions underneath it are not mine.)* | **SM.006 forbids exactly one thing — reversing between charge and discharge without dwelling — and the twin depot's battery does it 3,426 times, 14.4% of every transition it has ever made.** The rule is `critical`, `enforcement='block'`, and has **never had a caller**, so nothing has ever counted it. Evaluated by hand from `bess_snapshots`: **legal** `standby→charging` 6,901, `charging→standby` 6,890, `discharging→standby` 3,262, `standby→discharging` 3,251 (20,304); **undeclared** `charging→discharging` **1,719** and `discharging→charging` **1,707** — precisely the transition the rule's own description names (*"dwell-through-standby between charge and discharge"*). Of the six declared BESS states only three ever occur; `fault`, `offline` and `online` are declared and unobserved. | `db/checks/0285` §2; consecutive `bess_snapshots.status` per `system_id` joined to `ottoq_state_transitions` where `entity_kind='bess'` | **THIS IS WHY I DID NOT WIRE IT, and the reasons are not the ones SM.001 had.** **(a) There is no trigger to hang it on** — `0387` added one call to a trigger that already existed; `ottoq_bess_units` has **no trigger of any kind**, so a new AFTER trigger on a table the tick writes every beat is a different risk class. **(b) The unit-level column speaks a different dialect:** `twin.ottoq_sim_bess_step` writes `current_state` as `charging`/`discharging`/`'idle'` with its own comment *"'idle' is the unit-level vocabulary"* — and `idle` is an undeclared synonym for `standby`, so a probe there would fail the MAJORITY of transitions on a vocabulary mismatch and report it as a state-machine violation. That is `0387`'s preflight lesson exactly: the input must exist before the rule can be measured. **AND A CORRECTION OF MY OWN, the third unit error of the night after `0284` §3:** I first reported "declared vs live vocabulary, overlap **zero**" from `SELECT DISTINCT current_state FROM ottoq_bess_units` — which returned one row, `idle`, across 3 units. **That is a census of three pointers at one instant, not a vocabulary.** The column takes `charging` and `discharging` too; they were simply not in force. A vocabulary is a property of the WRITER and had to be read from its source. | **open, and it cannot carry a run id — structurally.** `bess_snapshots` has **no `sim_run_id` column**, so it is not run-scoped, never purged, and mixes every run into one series: the 23,730 transitions span 2026-05-12→2026-09-20 at this depot, the engine's whole life. CLAUDE.md's *"cite the run, never the table"* cannot be honoured here and saying so is the honest move. **Three decisions, only the first mechanical:** (i) `idle` vs `standby` — declaring the synonym is smaller, renaming the written value is cleaner, and eight functions read the column; (ii) **is a direct reversal actually illegal?** — Chase's call: a modern grid inverter can cross zero without dwelling, so either the matrix is wrong and 3,426 findings evaporate, or the dwell is real and the twin has modelled a duty cycle the hardware would refuse, which every `bess_setpoint_kw` command and the whole `energy_mpc_follow` path inherits. **The G94 shape: two readings, opposite remedies, measurement cannot choose.** (iii) the probe belongs on the `bess_snapshots` INSERT comparing to the previous row per `system_id` — 91k rows at this depot, so its cost needs measuring before it is added. **Promoting SM.006 to `block` today would refuse 14.4% of the battery's transitions**, which is why 2.9a measures before enforcing. |
| **G89** *(**NOW INSTRUMENTED — `0374`/`0375`, `db/checks/0269`. Still NOT enforced**, and enforcement remains an energy-policy decision because a site-sum rule changes what the BESS is allowed to do, and so changes `ottoq-energy-mpc`'s setpoints and the `energy_mpc_follow` path. `db/checks/0265`, `0269`.)* **UPDATE 2026-09-20 12:45 UTC — the finding holds, the mechanism is sharper, and one of its numbers wanted context.** (1) **Nothing needed reconstructing: `grid_import_kw` IS the site total**, equal to the component sum in **8,050 of 8,050** snapshots on this depot at a worst residual of **0.10 kW**. (2) **The mechanism is an incomplete meter, not merely an unchecked sum.** EN.001 *does* compare against `service_max_kw`, but its load term is `ottoq_depot_current_demand_kw` → `twin.ottoq_sim_compute_charger_load_kw` = `SUM(ocpp_sessions power)` **and nothing else** — so it asks a true question, correctly answered, about a quantity that is not the site's load. (3) **The rate is 1 in 8,050 snapshots across 9 runs, and the distribution is why that is not comfort:** buckets covering **1,800–2,700 kW are empty** — the site has never operated between 1,714 and 2,739 kW, so it does not ride the cap and tip over, it takes one ~1,000 kW step (the BESS charge magnitude) clean over. The rarity is the rarity of the *coincidence*; nothing prevents it. **What `0374` installs:** `ottoq_site_power_excursion_ledger` (`class='evidence'`, append-only, no FK) plus a per-tick detector above the policy branch, and `ottoq_site_power_ledger` as the reader — so "how often has this site exceeded its service contract" is answerable across purges for the first time. **THE LIMITATION, STATED PLAINLY:** G89's own recommendation was a projected-sum check *at `bess_dispatch`*, i.e. **before** the decision. What was built is a **meter, not a projection** — it witnesses the excursion after the fact and cannot prevent or predict one. That is deliberate (it catches an excursion however it arises, not only via a BESS decision, and it is what makes the historical rate answerable) but it does **not** discharge the pre-decision check, which remains unbuilt. **And `0375` corrects `0374`:** `0374` shipped `usual_dominant_component` claiming to answer "what pushed us over"; it answers "what is the biggest load", and the two disagree here — it reads `ev_charging` (1,691.8) while deferring the 968 kW BESS charge alone brings 2,738.8 under the cap with **729 kW to spare**, so a reader following it would throttle vehicles for nothing. Renamed `largest_component`, joined by `excursions_bess_deferral_clears` / `excursions_needing_ev_action`, which read **1 / 1 / 0** — *every excursion on record would have been cleared by deferring the battery, and none required refusing a vehicle.* **CORRECTION 2026-09-20 13:45 UTC (`db/checks/0271`, captured by `0378`) — G89'S SENTENCE "NOTHING ASSERTS THE SUM" IS FALSE, AND I REPEATED IT IN `0374`'S HEADER, ITS TABLE COMMENT, ITS REGISTRY NOTE AND ITS CERT-LINEAGE NOTE.** I went to build the projected-sum check G89 asked for and rule 5 stopped me: **it already exists.** `ottoq_energy_orchestrate` computes `v_charge_cap := GREATEST(50, v_demand_target − v_base_load + v_solar + v_bess_dispatch)` every tick — the battery's own draw subtracted kW for kW, which is the cumulative reasoning G89 said was absent — and publishes it as a `charge_cap_kw` command. **What survives is narrower and still real: no RULE asserts it.** The orchestrator publishes, it does not refuse, and **all 1,260** of the run's caps carry `"advisory": true` in their own payload. Against what actually flowed the advisory cap is mostly respected: **26 of 1,260 snapshots (2.1%) drew more EV load than the cap in force, worst overshoot 205.0 kW.** **THE EXCURSION HAD A SPECIFIC CAUSE, AND IT IS A PLANNING DEFECT, NOT A MISSING CONSTRAINT.** `v_demand_target` starts as `service_max_kw × factor` (2,500 × **0.9** = 2,250 — the policy value, not the 0.50 default in the source; I computed a first version against 1,250 and had to redo it), then is **replaced outright** by `ottoq_bess_reserve_target(...)` with **no `LEAST(v_service_max, …)`**. That function binary-searches for the lowest grid-import ceiling the battery can hold and **returns the forecast peak when it can hold nothing** — a *capability* answer used as a *permission*. At 13.81% SoC it returned **2,802** against a 2,500 contract, giving a **1,762 kW** cap where the contract-derived arithmetic (2,250 − 72 + 0 − 968) gives **1,210**. Measured: **16 of 1,260 targets exceeded `service_max_kw`**, worst **2,959** (459 over), worst published cap **2,804.4** — itself above the contract — while the **median target was 1,048**, so the override normally tightens by more than half and is a good mechanism that fails only when the battery is empty and demand is high. **THE FIX IS ONE LINE AND IS NOT MINE:** `LEAST(v_service_max, COALESCE(ottoq_bess_reserve_target(...), v_demand_target))`. It lowers the EV charge cap in exactly those 16 ticks — trading charging throughput at the worst moment for staying inside the contract. Right on the evidence, but a throughput call on the one depot whose capacity is the open question (rule 8). **`0378` captures the 16 rows into `class='evidence'` before the next demo run deletes `ottoq_energy_commands` and with it every number above.** **AND IT WEAKENS A cuOpt CLAIM I MADE TWICE:** `0374` and `db/checks/0269` both call this "the first measured instance of the cumulative-resource construct cuOpt cannot express." Overreach — a cumulative site-power resource *is* reasoned about here, in SQL, per tick. Truer: **the site power cap is currently held by an advisory publication plus an unclamped heuristic, where a scheduler with a first-class cumulative resource would carry it as a hard constraint.** Still a point for CP-SAT, a weaker and more honest one. **AND THE STRONG VERSION OF THAT CLAIM WAS ALREADY TRUE ELSEWHERE — `db/checks/0274`.** It needs no excursion at all: every one of the four constructs R-12 places outside cuOpt 26.08 is modelled in `solvers/cpsat/` **with a test that can fail** — **T3b** proves the site power cap BINDS (free peak 550 kW → 320 kW under a 330 kW cap), **T2** holds point exclusivity *and* the 18-minute DCFC cooldown on the service point, **T5** holds concurrency-within-point plus moves as scheduled operations. Plus 2.5's four determinism pins, all present and asserted in CI: `ortools==` pinned in `requirements.txt` alone (9.15.6755), `max_deterministic_time` always set with the wall clock only on explicit request and then stamped `reproducible: false`, `num_search_workers = 1`, and a canary that is **byte-identical across two solves, across a fresh process, and on a truncated budget against five contending cores**. So the sentence to use is about our own instrument rather than a competitor's limits: *"every construct the vendor documentation places outside cuOpt is modelled in our CP-SAT prototype, each covered by a test that can fail, and the plan is byte-identical across solves, across processes, and under CPU contention on a truncated budget."* | **Two independently legal decisions summed to 239 kW over the depot's declared service maximum, and no rule asserts the sum.** At the highest-import snapshot of run `5b37ee46`: EV charging **1,691.8 kW** (under the 1,800 kW `dcfc_max_concurrent_kw` nameplate), BESS **−968.0 kW** (*charging*, drawing from the grid), solar 0.0, building+lighting 79.0 — **grid import 2,738.8 kW against `service_max_kw` 2,500, a 9.6% overshoot, and the decomposition reconstructs the import to 0.1 kW.** Neither decision broke its own limit; their sum was never checked. **`bess_dispatch` evaluates exactly ONE rule** — `EN.003.bess_limits`, 234 evaluations, 0 failures — which checks the battery's own envelope; `EN.001.grid_capacity_ceiling` caps *aggregate charging* at `stall_assignment` and `charge_session_start`. **Nothing asserts `charging + BESS_charging + base_load ≤ service_max_kw`.** The shield gates each consumer against its own limit and never against the shared one. It happened **eight minutes into the run** (sim 06:56:09 against a 06:48 start) — the cold-start surge — and the second and third highest imports, within seconds, carry `bess_output_kw = 0`, so the battery draw is what took this one over rather than the charging wave alone. | `db/checks/0265` §1–2: the depot's declared caps, 1 of 1,139 snapshots over `service_max_kw`, 0 of 1,139 over the charging nameplate, deepest BESS charge −989.4 kW, and the per-component reconstruction | **Deliberately not fixed.** The cheap interim if one is wanted is a **MEASURED-only** rule at `bess_dispatch` logging the projected sum without blocking — 2.9a's blind-spot promotion doctrine, the shape 0370 used. | **open, and it is the most useful finding of the night for the CP-SAT decision.** This is **2.3's "shared site power cap"**, which 2.3 names as one of the four load-bearing constructs of the domain model and which 2.5 — citing `docs/research/answers/R-12` — records that **cuOpt cannot express at all**, as one of the four reasons the CP-SAT decomposition is *forced rather than chosen*. So it is the first measured instance on the twin depot of the exact constraint class the solver argument rests on, and the sharpest available answer to what the always-on CP-SAT host actually buys: **a cumulative site-power constraint that a per-consumer rule structurally cannot hold, because a per-consumer rule cannot see a sum.** |
| **G90** *(named, not fixed — nothing ships wrong today. `db/checks/0265` §3.)* | **`site_energy_snapshots.peak_demand_kw_15min` is not a 15-minute quantity — it is a running peak of instantaneous import, and reading it for demand billing overstates by 79%.** Measured over 1,139 snapshots on run `5b37ee46`: it is `>= grid_import_kw` in **1,139 of 1,139**, equal in 54, **never less**, and its maximum (**2,738.8**) equals the all-time instantaneous maximum import exactly. The 15-minute rolling **average** — which is what utilities bill on and what CLAUDE.md 2.9 means by *"15-min rolling, matches demand billing"* — is **1,529.2**. | `db/checks/0265` §3; and the KPI's own definition, `ottoq_kpi_peak_site_kw`, which computes `max()` over `avg(grid_import_kw) OVER (… RANGE '00:15:00' PRECEDING …)` | **Not fixed.** Renaming a stored column is a migration plus a sweep of every reader, and there is no urgency because the KPI does not use it. | **open, low severity.** **And the reason it was found is worth more than the finding:** the investigation began as "KPI 3 reports 1,529.2 while max grid import is 2,738.8, so the KPI understates the peak by 44%" — a serious accusation against a number we quote. **The KPI is correct**; it derives its own rolling mean and deliberately ignores the conveniently-named column. Third instance of G75's family tonight after `released_at`/`booked_at` and `stranded_recharges`: a name that describes a quantity the column does not hold. |
| **G88** *(**FIXED** by [0372](db/migrations/0372_the_shared_candidate_source_offered_charge_stalls_whose_charger_was_faulted.sql) (applied `20260920045844`, `forces_recert TRUE`). Found by two instruments built the same hour, not by reading code: `db/checks/0264`'s matched-frame comparison and `scripts/coverage-guard.sql`.)* | **`ottoq_stall_free_between` — the shared candidate source every proposer and the reroute walk go through — offered charge stalls whose OCPP charger was `Faulted`.** **CP-SAT noticed first:** on four consecutive frames it declined with *"40 charge-capable stall(s) and not one is offerable this tick (N charger_faulted, M occupied, K reserved)"* while our own pointer census in the same capture reported **4 charge stalls free** — and the four pointer-free stalls were exactly the faulted ones. Measured across five placement routines, **only `ottoq_l2_optimize_assignments` consulted charger health**; `ottoq_stall_free_between`, `ottoq_react_to_refusals` and `ottoq_replan_stranded_undercharge` did not. **And the one purpose-built routine that does — `ottoq.ottoq_replan_after_charger_fault`, which checks `station_state='Available'`, goes through `ottoq_indepot_reassignment_guard` and temp-stages rather than stranding — is reachable only from `twin.ottoq_report_charger_fault`, whose `p_actor` defaults to `'depot_tech'` and which `scenarios/mid_session_charger_fault.json` lists as a manual injection form.** So it is an operator entry point, correctly off the tick path, and the twin's own **18 charger faults** on this run never reached it; the generic stranded-undercharge sweep carried them instead, and that sweep does not check charger health either. | `db/checks/0264` §1–2 (the four declines with their faulted/occupied/reserved breakdown summing to 40 every pass, against `charge_free = 4`); the five-routine `station_state` audit; `scripts/coverage-guard.sql` §1b, which flagged `ottoq_replan_after_charger_fault` on its first run | **Fixed by 0372:** one predicate in the shared source, scoped to `dcfc`/`l2`, excluding a stall whose charger reports `Faulted` — matching what the two charger-aware routines already do. P9 asserts the calendar state set, the occupancy guard and the ordering all survived, and tests the **comment-stripped** body per 0371's lesson. **Heartbeat freshness is deliberately not checked** although `HW.002` requires it: that needs a staleness threshold this function should not choose, and every charger on the depot currently reports a heartbeat, so the omission changes nothing today and is named rather than hidden. | **fixed.** **Not a safety incident, and that was established before the fix rather than after:** `HW.002.charger_state_precondition` (`critical`) was evaluated **113 times on this run with 0 failures**, consistent with the local optimizer pre-filtering — nothing charged through a faulted charger. The real exposure is narrower: **the reroute books through `ottoq_book_stall` directly rather than through `ottoq_decide_tick`, so a reroute onto a faulted-charger stall faced neither the pre-filter nor the probe.** Reroutes this run were overwhelmingly `staging` (8 `l2` and 1 `dcfc` of 90), so the live exposure was small — which is the argument for fixing it while it is small, not for leaving it. |
| **G87** *(named, not fixed — a compatibility question, see the Fix column. `db/checks/0263` §3.)* | **`reliability.stranded_recharges`, a number `ottoq_twin_events_window` publishes, counts neither recharges nor vehicles.** `twin.ottoq_sim_advance_service_flow` emits `twin.recharge_stranded` with payload `{"recharged": N, "floor": 80}` — **239 events on run `5b37ee46`, carrying 1,333 vehicle-requeues between them, up to 16 in a single tick** — and the report surfaces it as `count(*)` of those events. Read at the writer, **nothing is recharged**: the loop sets `current_state='staged_awaiting_service'` and `svc_step='need_charge'` and places the vehicle in a temp stall chosen by `ottoq_replan_stranded_undercharge`. No SoC is written anywhere. Its own comment gives the reason — *"DOCTRINE (Chase 2026-07-28): never bounce a vehicle to the gate"* — and the split is correct: OTTO-Q chooses, the twin executes the returned plan. **It is a re-queue, not a rescue.** So the published field is wrong twice, in opposite directions: it **names an action that did not happen** (a reader takes 239 as 239 vehicles topped up by the simulator, which would mean the twin was quietly covering for the orchestrator — it is not), and it **under-counts the action that did** by 5.6x, because each event stands for `N` vehicles and it counts events. | `db/checks/0263` §3 and Q4: `requeue_events` 239 against `vehicles_requeued` 1,333, `soc_floor` 80, `max_in_one_tick` 16; the writer's own body, which contains no SoC write | **Not fixed, and the reason is not laziness.** Renaming a field in a live report is a compatibility question for whatever consumes `ottoq_twin_events_window` (it is the only consumer in the database, but the API/UI side is not established here), and changing `count()` to `sum()` changes a published number — both need to happen together, with the old name kept or aliased for one release. **Interim rule: do not quote `stranded_recharges`.** Q4 gives both figures under names that say what they are. | **open, low severity, high embarrassment.** Same family as G75 — a name that reads as a pair with something it is not — except this one is in a number someone would put on a slide rather than in a column someone might query. The twin is behaving well here; only the label lies. |
| **G86** *(**FIXED** by [0383](db/migrations/0383_the_whole_of_g86_is_one_concurrency_class_that_no_executor_admits_and_the_walkaround_is_its_only_member.sql) (applied `20260920161631`, `forces_recert TRUE`); retraction and end-to-end proof in [0277](db/checks/0277_g86_was_one_orphan_concurrency_class_and_the_staging_stalls_it_was_blamed_for_were_never_its.sql). [0261](db/checks/0261_a_mandatory_service_with_no_executor_holds_eighty_seven_percent_of_the_staging_capacity.sql) carries the original trace — **its executor conclusion is superseded and its staging-stall attribution is retracted**.)* | **`perimeter_walkaround` was a mandatory service no code could perform — because it was derived into a seventh concurrency class, `hold`, that no starter admits and of which it is the only member. THE "HOLDING 98 OF 113 STAGING STALLS" HALF IS RETRACTED: those are `perimeter_hold` PARKING bookings, named for the depot's perimeter RING, and they are not this service's.** Measured on run `5b37ee46` at tick 501: staging **113 total, 59 free by the pointer, 12 free by the calendar, 11 free on BOTH gates**, and **98 held by `perimeter_hold`**, while **258 `twin.staging_overflow`** events fired on the same run. Traced end to end: the **producer** is `ottoq.ottoq_derive_visit_needs` line 231, which emits the atom `must_do=true, deferrable=false, est_min=12, concurrency='hold', at_perimeter=true` when `v_is_night` — and the CT gating is *correct* (`v_hour` is `EXTRACT(HOUR FROM (v_clock AT TIME ZONE 'America/Chicago'))`, and this run's sim clock starts 06:48 UTC = 01:48 CT, which is night). The **observers** both say yes: `ottoq.ottoq_observe_asset` sets it unconditionally with the comment *"ASSUMPTION G3-3 (the kernel gates it to night)"*, and `twin.ottoq_sim_observe_asset` draws it at `night_walkaround_p` default **0.90**. The **executor is NONE**: every completion path takes a fixed service list — `advance_flow_contract` `ARRAY['charge']`, `advance_service_flow` the wash/detail/service-bay lists, `advance_visit_atoms` seven services that do not include it and which reference neither `at_perimeter` nor concurrency `'hold'` — and a search of every function body in the database for the literal returns exactly **three**: the producer and the two observers. `at_perimeter` appears in **one** function, the producer. So the hold cannot clear on completion; it clears on expiry, which is what `0260`'s measured **248-minute mean window** is. | `db/checks/0261` §1–2; the two-gate census, and the executor trace with `functions_naming_it = 3` against 10+ for every other service | **Fixed by 0383, and option (b) turned out not to exist.** The three candidates I offered assumed the walkaround held a staging stall; it never did, so "stop it holding a stall" had no referent. Option (a) was right for a reason I had not found: **no completer needed writing.** `twin.ottoq_sim_advance_visit_atoms` already completes ANY atom whose `ends_at` has passed, names no service and needs no bay — the gate is the START side, `ottoq_start_concurrent_atoms`, which admits `concurrency IN ('cabin','exterior','digital')`. 0383 derives the atom as **`exterior`** beside `sensor_clean` (event-raised, performed at the vehicle, `lane_stalls=NULL`, `at_perimeter:true` already declared on the atom), adds it to `ottoq_atom_retirable_set()` and declares it in `service_cadence_policy` — **one transaction, and never the retirable-set change first, or `ottoq_atoms_guard` stops demoting and 63 harmless atoms become permanent `must_do` blockers.** **`exterior` and not `digital` is the integrity of the change:** the starter meters `concurrency IN ('cabin','exterior')` against `general_tech` (**10** at the twin depot, one vehicle per tech), while `digital` is explicitly exempt from that pool — so the cheaper word would have had the twin complete 63 walkarounds with nobody walking, which is exactly the fabricated work completion I refused to write. Proven on a rolled-back probe (`0277` §4): derive → `cls=exterior must_do=true` with **no** `guard_demoted`, start → `in_progress` with `ends_at` at exactly the declared 12 minutes, advance → `done`. **No backfill:** the 63 stored `hold` atoms keep their class, so the new detector still reports `hold` as an ORPHAN_CLASS and should. | **closed 2026-09-20.** `ottoq_assert_service_vocabulary()` returns an empty `undeclared` for the first time, and a new evidence-based detector, `public.ottoq_atom_class_coverage()`, makes the defect class visible rather than merely fixed: an orphan class is exactly `atoms > 0 AND ever_started = 0`. It is deliberately NOT a probe of any starter's source — `bay`, `anchor` and `gate` are executed by three other seams, so a text probe would have reported three false orphans and made the real one look like a fourth. **What it does not catch, stated so nobody over-trusts it:** a class that completes without the work happening (the `credited`/`satisfied` shapes `ottoq_kpi_service_completion` separates). **Still open and now correctly separated from G86:** the 258 `twin.staging_overflow` events and the 82–98 long `perimeter_hold` parking claims are a staging-capacity-under-dwell question, and closing G86 frees not one stall. G76's "155 of 158 reserved, 21 occupied" is explained from the calendar side by those parking holds, not by this service. **The general rule it forces, third instance after G84 and 0260's wash bays: a free-stall count is only meaningful as the INTERSECTION of the pointer gate and the calendar gate.** A census over either alone is a different question — the same defect shape `0250` established for depot scope. |
| **G95** *(**MEASURED, NOT FIXED** — cause pinned in [0281](db/checks/0281_thirty_vehicles_entered_the_emergency_state_and_the_only_way_out_needs_work_they_never_had.sql); no migration accompanies it on purpose.)* | **Thirty vehicles entered `emergency_staged` on one run and none left it — 26% of the fleet, mean 243 of 620 ticks, max 379.** The state also appears in **no** admission list in `twin.ottoq_sim_advance_visit_atoms`, so no in-place atom can start while a vehicle is there: a full stop, not a slow lane. **The exit exists and runs — it is conditioned on interrupted work.** `ottoq.ottoq_readmit_resumed_visits` requires an OPEN visit carrying `meta ? 'reopen'` (*"proves this visit was actually cut short"*), and **1 of the 30 ever had one.** Its doctrine is *"a vehicle resuming ITS OWN interrupted work"*; 29 had no work to resume, so the exit does not apply rather than failing. A vehicle reaching `emergency_staged` without work in flight has no route back. | `db/checks/0281` §1–2; the snapshot-derived cohort joined to `meta ? 'reopen'`, run-scoped | **TWO HYPOTHESES FORMED AND DISPROVED FIRST, both recorded because each looked solid.** **(a)** *"human-only exit in a twin with no humans"* — the declared machine really does permit `emergency_staged →` exit only to `command_center_operator`/`depot_supervisor` while entry is permitted to `ottoq_engine`, a genuine asymmetry, but not the cause. **(b)** *"the approval gate is permanently closed"* — `ottoq_ops_approvals` shows 1,792 declined / 203 expired / **0 approved**, which reads as a 0% grant rate. **It is not: 81 approvals were granted**, and every one carries `expired_from='approved'` with `expired_reason='run_ended'` because `ottoq_sim_release_depot` deliberately expires them (0097: *"no approval outlives its run… a surviving 'approved' row is readable by the NEXT run's gates"* — the 0046 divergence channel). Reading `status` alone and stopping is the same error as reading a roster as an occupancy census. The 1,717 dominant declines are `automated_reassignment` meeting the zone-C allowlist, which is deliberate policy. | **open.** Three routes back, with different meanings: widen readmission to admit a resolved exception with no cut-short visit (changes what "resume" means); simulate the `command_center_operator` the declared machine names (the twin already simulates technicians); or send them to `out_of_service`, the other declared exit (honest, but removes them from the fleet). Depends on what `emergency_staged` is meant to mean. **Not established:** how the 30 entered — `tow_requested → emergency_staged` is undeclared and likely another G94 instance, but the SM.001 probe has only seen short recert pairs so far, so it is unverified; and whether 26% is typical, since this is one run/scenario/seed. |
| **G94** *(**MEASURED, NOT DECIDED** — surfaced by [0387](db/migrations/0387_two_critical_state_machine_rules_had_a_caller_shaped_hole_and_the_input_they_need_did_not_exist.sql)/[0388](db/migrations/0388_my_own_new_probe_writes_blocked_into_a_log_where_that_word_means_something_it_did_not_do.sql); measurement in [0280](db/checks/0280_the_engine_takes_six_transitions_its_own_declared_state_machine_does_not_contain.sql).)* | **The engine performs six vehicle transitions its own declared state machine does not contain — and until SM.001 had a caller this was unmeasurable.** Post-`0388` window: SM.001 **32 shadow_pass / 22 shadow_fail**, SM.003 **32 / 0**; the pre-0388 window agrees on the rate (88/60). **Every failure is the LEGALITY half, none is a role-gate failure** — which is the proof 0387's attribution fix worked. The 22 are two unlike things: **16 are harness** (`offline → deployed` 8, plus `en_route_to_depot`/`charging_dcfc`/`staged_for_departure → offline` 8) — fleet prime and run teardown, not depot operations, and the operational machine has no reason to declare them. **6 are the engine shortcutting its own declared flow:** `charging_l2 → staged_for_departure` (2), `charge_complete_holding → staged_for_departure` (2), `staged_awaiting_service → charge_complete_holding` (2). `staged_for_departure` is declared reachable from exactly three states (`offline`, `service_complete_holding`, `staged_awaiting_service`), so the designed path out of a charge is `charging_l2 → charge_complete_holding → staged_awaiting_service → staged_for_departure` and the engine skips it. | `db/checks/0280` §3; the shadow_fail reason breakdown and the declared-transition census | **Not decided, deliberately.** Two non-equivalent readings: **(a)** the declared machine is incomplete and the shortcuts are legitimate — a vehicle that finishes charging with nothing outstanding is ready to leave, and routing it through `staged_awaiting_service` would model a wait that does not happen; **(b)** the engine is skipping states consumers rely on — note `service_complete_holding` exists in the declared machine specifically as a route to `staged_for_departure` and the engine appears not to use it, so anything reading for a "service complete" state would never see one. It changes what "ready for work" means and which intermediates a consumer may depend on observing, so it is Chase's call. | **open.** Both rules are `enforcement='shadow'`, so neither reading is enforced — and **promoting SM.001 to `block` today would refuse ~40% of the engine's transitions**, which is precisely why 2.9a measures before enforcing and precisely what could not be known before 0387. |
| **G93** *(**FIXED** by [0386](db/migrations/0386_the_fourteen_atom_verdict_is_the_one_thing_the_permanent_run_record_does_not_keep.sql) (applied `20260920173700`, `forces_recert FALSE`); first durable verdict and the transport finding in [0279](db/checks/0279_the_first_durable_determinism_verdict_and_the_transport_that_made_it_impossible.sql).)* | **Every determinism verdict this engine had ever produced was gone. 1,166 arms are archived permanently and not one carried its pass/fail.** CLAUDE.md 2.9a treats the fourteen-atom byte-identical verdict as a *product property* — the claim "most of the field cannot make". `ottoq_run_archives` is class `run_ledger`, registered as *"the permanent per-run record; its whole job is to outlive the run"*, and holds **1,166** rows with `reason='determinism_arm_complete'` — of which **0** carry `validation_notes`. The verdict was written only to `ottoq_sim_runs.validation_notes`, and `ottoq_purge_prior_runs` ends with `DELETE FROM ottoq_sim_runs`: **0 surviving cert-harness runs, 0 recoverable verdicts.** The comment that made it look handled — *"The verdict survives a dropped client: it lives on both run rows"* — is true of a dropped HTTP client and false of the next demo run. **Third instance after 0231 and 0340: a durability comment is not a durability mechanism.** The archive could not have held it either — `ottoq_archive_run` copies `v_run.payload`, and each arm is archived by `ottoq_sim_stop_and_reset` *inside the arm loop*, before the verdict exists. | `db/checks/0279` §1–2; `ottoq_determinism_verdict_ledger` verdict_id 3 and the two timeout probes | **0386 adds an append-only `class='evidence'` ledger, one row per PAIR, no FK to `ottoq_sim_runs`** (check (b2) demands an FK only of engine/stamp; on evidence it could only block the purge or CASCADE-erase what check (c) protects). **Not backfillable — no verdict existed anywhere**, so row 1 is the first durable verdict this engine has held; the arms' *inputs* survive, so every historical certification is re-runnable even though its answer is not. **The per-atom diagnostics are deliberately additive:** `v_equal` is an AND chain of `=`, so one NULL hash makes the chain NULL and the outcome fall through to `failed` — rebuilding it with `IS DISTINCT FROM` would have treated NULL=NULL as agreement and could silently turn a present `failed` into a `passed`, so the chain is untouched and a new `null_atoms` column makes the trap visible instead. | **closed 2026-09-20.** First verdict: busy_day/314159/12t, **passed, 14 atoms, 0 disagreeing, 0 null**, in **192.7 s** — which is also the proof that the Supabase Management API path could never have produced one: it cancels at 120 s twice over (`SQLSTATE 57014` at 2m00s; **HTTP 524** at 2m05s even with `SET statement_timeout = 0`, so the `SET` lifts Postgres's limit and the edge proxy cuts it anyway). The `postgres` role carries no `statement_timeout`, which is why detached pg_cron execution works. **Recert now has an executor at all** — `ottoq_cert_columns` declared nine canons, `ottoq_cert_matrix` reported them, `ottoq_cert_recert_floor` expired them, and nothing ran them; the same declared-with-no-executor shape as G86. Runner is pg_cron job 741 with an advisory lock, scoped to the twin depot per rule 8 — **which leaves the two `grid_smoke` fixture columns uncertified, a judgement flagged for Chase rather than assumed.** |
| **G91** *(**FIXED** by [0385](db/migrations/0385_the_catch_up_that_stopped_one_state_short_strands_a_vehicle_for_the_whole_run.sql) (applied `20260920172340`, `forces_recert TRUE`); measurement in [0278](db/checks/0278_a_vehicle_that_reaches_staged_for_departure_holding_cabin_work_can_never_have_it_started.sql).)* | **A vehicle that reaches `staged_for_departure` still holding a mandatory CABIN atom can never have it started, and SLA.004 then blocks its redeployment for the rest of the run.** `twin.ottoq_sim_advance_visit_atoms`'s admission cursor admits six vehicle states, then sub-gates: `exterior`/`digital` atoms are startable in all six, **`cabin` atoms only in `charging_dcfc`/`charging_l2`/`charge_complete_holding`**. A vehicle in `staged_for_departure` fails the inner EXISTS, so it is **never a candidate at any value of `v_free`** — stronger than starvation, which at least implies a queue. Measured on run `562bf027` (busy_day, seed 777777, 1,241 ticks, 542.6 sim-min): **3,055 vehicle-ticks** blocked, **every one `cabin`** (3,049 `interior_inspection` + 6 `interior_tidy`), **zero `exterior`** — and 28 vehicles ended the run holding a pending non-deferrable `must_do` cabin atom (21 + 7). **AND THE FUNCTION'S OWN COMMENT ALREADY DIAGNOSES IT FOR THE NEIGHBOURING STATE:** *"charge_complete_holding is kept ONLY as a catch-up: without it a vehicle whose charge finished before the tech arrived would never be cleaned and SLA.004 would block its deploy forever."* Correct mechanism, one state short. | `db/checks/0278` §1–3; the gate verbatim from `prosrc`, the 28-vehicle end state, and the snapshot-frame reconstruction | **0385 adds `'staged_for_departure'` to that one list and nothing else.** **The fix I first planned was wrong and measurement killed it:** I was going to re-rank the cursor's ORDER BY so a dispatch-blocked vehicle outranked charging vehicles for the `general_tech` pool. But the pool averaged **1.66 of 10** busy during those blocked ticks and was full in **0.6%** of them — re-ranking a queue nobody was waiting in. ORDER BY was also the wrong lever on its own terms: it orders *candidates*, and these vehicles are not candidates. A reserved pool slice was rejected for the opposite reason — the pool genuinely binds at **9.91 of 10** during DCFC waits, so a reserve would be paid for by the contended population. | **closed 2026-09-20.** Two things ruled out rather than fixed: SLA.004 not exempting `readiness_check` (22 of 25 blocks) is **not** a defect — the advancer has a dedicated branch completing it once a vehicle is `staged_for_departure`, so those blocks self-resolve; and the `M3_cabin_at_charger` doctrine the gate defends is already **84% bypassed** (only 35 of 221 cabin starts began in a charging state, because `ottoq_start_concurrent_atoms` re-checks no state and `ottoq_decide_tick` calls it from two enactment sites), so loosening a selection filter cannot weaken an invariant the executing path never enforced. **That is G92, open.** |
| **G85** *(**MEASURED** by [0370](db/migrations/0370_fifty_nine_calendar_claims_were_already_overruled_and_the_ledger_held_none_of_them.sql) (applied `20260920042835`, `forces_recert TRUE`) — deliberately **not** acted on, per 2.9a's blind-spot promotion doctrine. After-measurement in [0260](db/checks/0260_the_engine_sat_on_fifty_nine_known_false_claims_for_two_hours_each.sql).)* | **Fifty-nine live calendar claims were already overruled by a vehicle sitting in the stall, and `space_conflict_ledger` — the table CLAUDE.md rule 6 says "records every calendar claim overruled by physical reality" — held none of them.** The ledger records conflicts discovered **at assignment time**: 25 `assignment_refused_occupied` / `command_refused_preflight` rows on run `5b37ee46` (l2 10, staging 8, dcfc 7) plus one `stale_claim_displaced`. It had no concept of a claim that is contradicted **while it stands**. Measured at the sim clock over bookings whose window *contains* it: of **97 live `perimeter_hold` bookings on staging stalls**, 83 `held` had **zero** with the holder in the stall, 26 stood empty and **57 had a different vehicle physically in them**; 59 contradicted claims in total, **0 present in the ledger by `displaced_booking_id`**, only 4 of the 59 stalls appearing in it at all. The detector's own first sweep gives the number that could never have been produced before: **average claim age 114.6 minutes, average time still to run 140.5 minutes.** So the engine could have known for nearly two hours and instead learned at the worst moment — when the holder finally travelled and preflight refused it. **The 25 refusals are the late symptom of the 59 standing contradictions nothing counted.** And the scale: **98 of the depot's 113 staging stalls held by `perimeter_hold`**, average window **248 minutes** on a run whose whole life is 540 sim-minutes. | `db/checks/0260` §1; the detector `ottoq.ottoq_detect_contradicted_claims` and its 59 rows with `conflict_kind='standing_claim_contradicted'`, `resolution='recorded_not_acted'` | **0370 counts it and changes nothing else.** One row per booking per run, deduped, capped at 200 a tick, `SECURITY DEFINER`, error-swallowing so it can never abort a tick; wired into `ottoq_sim_decide_and_dispatch` after 0367's reclaimer, and P9 asserts the function declares itself `recorded_not_acted`. **Acting on it — rebooking a holder whose stall is already taken, before it travels — is an assignment change and belongs to Chase.** | **measured, open for a decision.** **The walkaround note that used to close this cell is retracted (`db/checks/0277`): the 98 holds are `perimeter_hold` PARKING claims with `need_atom IS NULL`, and `perimeter_walkaround` never took a staging stall — "a mandatory service that never completes cannot release the stall its hold takes" linked two mechanisms that share a prefix and no code path.** G86 is fixed by `0383` and it frees none of these stalls. **G85's own measurement is unaffected and is the part that matters here:** 59 standing claims contradicted by a vehicle physically in the stall, average age 114.6 min with 140.5 min still to run, 0 in the ledger — and the 248-minute mean window is the dwell these parking holds are written for, not a symptom of an unperformed service. |
| **G84** *(**FIXED** by [0369](db/migrations/0369_a_vehicle_parked_in_one_stall_was_still_holding_another.sql) (applied `20260920041750`, `forces_recert TRUE`). Third and last layer of the G82/G83 investigation, and the one only the first two could expose.)* | **A vehicle physically sitting in one stall was still holding an unexpired reservation on another, and the reclaimer had no right to take it back — so the reroute saw calendar-free stalls and was refused every one by the pointer.** Made legible by 0368's enriched escalation payload on run `5b37ee46`: every remaining `no_capacity` carries `candidates_seen` of **2 or 4** with `stall_type_source='payload'` — i.e. the walk examined 2–4 **calendar-free** stalls and `ottoq_reserve_stall` refused all of them. The calendar and the reservation pointer disagree about the same stall. (The reactor's own comment always said the pointer is "the scarcer gate"; the size of the gap had never been counted.) Measured at 38 sim-minutes: **50 reserved-empty unexpired stalls, of which 36 were held by a vehicle sitting in a different stall** — and **21 of those 36 had no held/active booking** for that holder on that stall, against **15 that did**. Reservation census in the same moment: dcfc 10/10 reserved with 8 occupied, l2 27/30 reserved with 24 occupied, staging 101/113 reserved with 60 occupied and TTLs out to **7h14m**, wash_bay 3 stalls never touched. | `db/checks/0259` §1–2; the split query joining `stalls.reserved_by` to both `stalls.current_vehicle_id` (is the holder elsewhere?) and `ottoq_stall_bookings` (does the calendar back it?) | **Fixed by 0369** as a third release class, bucketed first-true so `by_unusable_holder + by_sim_expiry + by_unbacked_orphan = released` exactly. **The booking test is the whole safety of it:** a held/active booking for that holder on that stall is a real forward plan — charge now, staging place when done — and releasing on "holder is elsewhere" alone **would have destroyed fifteen legitimate plans**. The hierarchy it encodes: the calendar is the authority on intent, the pointer is a lock; a lock with no intent behind it is garbage, a lock with intent behind it is untouchable. **And one guard that is not optional:** `ottoq_stall_bookings.sim_run_id` is `NOT NULL`, so on a production call (`p_sim_run_id IS NULL`) the booking test can never match and the reclaimer would strip the entire site — the class is gated on `p_sim_run_id IS NOT NULL` and P9 refuses to ship without that gate. **Effect, measured one tick later:** staging reserved **101 → 88**, unclaimed staging **12 → 25**, total unclaimed stalls **~7 → 32**. | **fixed** — and what it leaves is a genuine capacity wall rather than a defect: **all 10 DCFC stalls claimed with 8 occupied, and 28 of 30 L2 claimed with 25 occupied.** That is the answer forming to "how many vehicles can this depot orchestrate", and it is a number, not a bug. |
| **G83** *(**FIXED** by [0368](db/migrations/0368_the_reroute_hunted_the_scarcest_stall_type_for_vehicles_that_wanted_the_most_plentiful.sql) (applied `20260920041236`, `forces_recert TRUE`). The second half of G82's answer, not a new subject: 0367 gave the reroute real stalls to offer, and this is what it did with them.)* | **The reroute searched for `dcfc` — ten stalls on this site — whenever the refused command carried no `stall_type`, including for vehicles whose actual target was a staging stall, of which there are 113.** `ottoq.ottoq_react_to_refusals` derived its search type as `COALESCE(payload->>'stall_type', CASE command_type WHEN 'stage' THEN 'staging' ELSE 'dcfc' END)`, so a `proceed_to_stall` without that key fell through to the scarcest resource on the depot. Measured on run `5b37ee46` (busy_day, seed 777777, twin depot, speed 8.0) at tick 38, with 0367 live from tick 1: of **31** refused commands, **14 `superseded`** and **10 `vehicle_state_incompatible`** are not reroutable and were correctly escalated; of the **7** reroutable, **2 rerouted** and **5 escalated `no_capacity`**. Split by what the walk looked for — **4 carried `stall_type='l2'` against an `l2` target** (2 of those rerouted), and **4 carried no `stall_type` at all against a STAGING target, for which the walk searched `dcfc`, found none, and escalated `no_capacity` while 41 stalls stood unclaimed.** The command already records where it was sent, in `payload->>'stall_id'`. | `db/checks/0258` §3 and Q3; the join from `ottoq_vehicle_commands.payload->>'stall_id'` to `stalls.stall_type` is the whole proof — payload type NULL, actual target `staging`, assumed `dcfc`, 4 rows, all escalated | **Fixed by 0368.** The wanted type is now read from `payload->>'stall_type'`, then from the stall the command was actually sent to, and only then from the command-type default; `stall_type_source` records which of the three answered. Part B is the observability half: `no_capacity` alone could not distinguish "the site is full" from "the walk looked in the wrong place", which is the ambiguity that hid this for a whole run, so escalations now carry `wanted_stall_type`, `stall_type_source` and `candidates_seen` in both the command payload and the `ottoq.refusal_escalated` event. P9 asserts every prior behaviour survived the patch — the `arrival_inspection` exclusion, the last-resort pass, `reroute_after`, `reacted_at`. | **fixed** — **and it carries a measurement mistake of mine that would have mispriced it:** the first reading of the reroute's success rate divided by ALL 31 refusals and reported **6.5%**, when 24 of those 31 are not reroutable by design. The honest figure is **2 of 7, 29%**. That is G78's defect — an honest refusal scored as a miss — committed by me on the instrument measuring the fix. `0258` Q3 now splits the classes and computes only the reroutable denominator. |
| **G82** *(**FIXED** by [0367](db/migrations/0367_the_reclaimer_0360_installed_has_never_run_on_the_live_metronome_path.sql) (applied `20260920041500`, `forces_recert TRUE`); after-measurement in [0258](db/checks/0258_the_reroute_loop_never_needed_building_it_needed_a_world_that_tells_it_the_truth.sql).)* | **The reservation reclaimer 0360 installed had never run once, because the live metronome does not call the function it was wired into — and that is why 296 of 346 refusals escalated to `no_capacity` while the reroute loop was working correctly.** `ottoq_demo_metronome` (cron job 12, the live simulation engine) calls `ottoq_sim_advance_tick_world` and `ottoq_sim_decide_and_dispatch` **directly**, at its own lines 100 and 108, and never calls `ottoq_sim_advance_tick` — which is where 0360 put `ottoq_release_unusable_reservations`. **G77's pattern reproduced inside the migration that was fixing G77:** the right routine, no live caller. Measured on run `3fb415d8` (cuOpt arm, seed 777777, busy_day, twin depot): **393 `ottoq.refusal_escalated` events over 1,245 ticks (0.316/tick), 296 of the first 346 `no_capacity` after `target_occupied`**; **38 release-eligible reservations standing and unchanged across a tick boundary** (tick 1123 → 1124); and all **4** DCFC stalls the reroute walk found calendar-free carried a reservation **36–45 sim-minutes past its own expiry**, **two of them holding no vehicle at all**. **And the architectural point, which is the opposite of what was expected:** the rejection → re-solve loop **already exists and already runs** — `ottoq.ottoq_react_to_refusals`, called from `decide_and_dispatch` line 121, walks up to 25 calendar-free stalls, reserves, books, and re-emits the command with `reroute_after`, escalating only when every candidate fails `ottoq_reserve_stall`. It had four legal moves and was refused all four by expired pointers. Nothing needed building; the loop needed a world that tells it the truth. **A second defect, found while proving the first, which would have hidden it again:** invoked directly the reclaimer returned `{"ok": false, "msg": "deadlock detected", "sqlstate": "40P01", "released": 0}`, and its call site is a bare `PERFORM`, so that JSON is discarded — **a reclaimer that releases nothing was indistinguishable from one with nothing to release.** **And the wiring check that missed all of this was fooled by a prefix:** `position('ottoq_sim_advance_tick' in prosrc)` returned 4179, which is the offset of `ottoq_sim_advance_tick_**world**`. Third instance of that mistake class here, after the `_`-is-a-LIKE-wildcard trap and the comment-vs-executable-SQL trap. A caller check must match the call syntax — `ottoq_sim_advance_tick(` — not the bare name. | `db/checks/0258` §1–3; the metronome's own source at lines 100/108; the 40P01 return quoted verbatim from a direct invocation | **Fixed by 0367.** Part A takes rows `FOR UPDATE ... SKIP LOCKED` so the reclaimer can never be a deadlock participant (contended rows are left for the next tick and counted in `skipped_locked`), counts `eligible` without locking, and emits `ottoq.reservation_reclaim_blocked` (registered in `ottoq_event_types_catalog`) when `eligible > 0 AND released = 0`. Part B was generated verbatim from `pg_get_functiondef` with one block inserted — not retyped, because 0360 Part C nearly broke every tick from a signature typed from memory. The call sits **above** the policy branch so every arm of an A/B gets the same world: reservation hygiene is part of the problem definition, not the policy (0146's argument about the L1 shield, applied to the calendar). **Immediate effect, measured 4 minutes after applying:** release-eligible reservations **38 → 1**, unclaimed stalls **11 → 41**, charge-type unclaimed **0 → 1**, `reservation_reclaim_blocked` **0**. | **fixed** — and it retracts an attribution, see the note on G76/G77 |
| **G8** | Outbound command lifecycle has no lease, ack, retry, TTL or dead-letter. | *not yet written up* | — | open |
| **G9** | No producer for the forward power schedule (`ServiceProfile`). The publication boundary in CLAUDE.md 2.5 is described but nothing emits it. | *not yet written up* | — | open |
| **G10** | The SDR terminus does not bind to the operation ledger; SDRs are mutable; negative durations exist. Also: `ottoq_emit_sdr` signs a payload containing `leg_id` (run-scoped) and omitting `booking_id`/`visit_id` (the attribution) — the signature covers what cannot survive a replay and omits what must. | [0123](db/checks/0123_the_settlement_record_was_outside_the_certification_and_the_scan_that_wrote_it_grew_with_history.sql), [0218](db/migrations/0218_h_sdr_hashed_a_signature_computed_over_a_run_scoped_id.sql) (names it, does not fix it) | — | open — full six-column recert when done |
| **G12** | **Re-scoped in both directions by [0142](db/checks/0142_the_lint_g12_rejected_needed_a_tokenizer_not_a_database.sql); the original entry was wrong on one count and too strong on one conclusion.** CI still does not run the SQL: `verify.yml` runs Python batteries and no migration or check has ever executed against a database in CI. **But the file-only lint was rejected for a fixable reason.** It said telling code from literal "needs a real plpgsql parser because the bodies are dollar-quoted" — it needs a **tokenizer**, which is a hundred lines and no grammar. `scripts/sqlstruct.py` is that tokenizer (nesting block comments, `''` and `E''` strings, quoted identifiers, `$tag$` bodies closing only on their own tag). Measured on the very file that killed the lint: **0220 matches `LIMIT 1` fourteen times in raw text and three times in code.** The entry's own "all five hits" was an undercount. **And the ask is too weak where it matters most:** a database in CI would have caught **none** of the three defects found in the 2026-09-08 apply window — 0225's P3 (dry-run at the wrong floor; needs the live catalog *and* the state a predecessor creates), 0226's A1 (needs the live 92-row lineage table), and 0225's A2 contradicting its own A5 (needs reading comprehension). All three were caught by `APPLYING.md` step 3b(ii), which is a **process, not a gate** — and "we'll catch it in CI" is how a process step gets dropped. | [0142](db/checks/0142_the_lint_g12_rejected_needed_a_tokenizer_not_a_database.sql), `scripts/sqlstruct.py`, `tests/test_sqlstruct.py` | **partly built.** The separator ships with 356 tests, and `test_every_sql_file_segments_and_balances` now asserts dollar-quote balance over every committed migration and check — 300+ files, under a second. That replaces the by-eye check done on 0225 after five hand splices in one afternoon. Its first run found a defect **in itself**: it recursed into `0221`'s `$$COALESCE(b.sim_run_id,'00000000$$` — a dollar-quoted *string* with an unbalanced apostrophe, valid PostgreSQL — and raised on correct input, which is precisely what 0221's own comment three lines above warns against. Now fail-safe: recurse only if the body tokenizes. **The `LIMIT 1` rule itself is still not shipped** — building the separator was the blocked part; the rule on top needs its ORDER-BY half and its own noise budget. | open — separator built, database-in-CI still open and re-scoped |
| **G13** | **Written up at last, and two of its five claims do not survive re-measurement.** The one that matters is bigger than recorded: **64 of the Benchmark depot's 160 stalls are seated by vehicles that belong to no run, and its 154 live reservations expired 2026-08-22/23** — sixteen days ago. **40% of the depot is unreservable**, because `ottoq_reserve_stall`'s CAS refuses any stall with a `current_vehicle_id`. Nothing will ever clean it: both teardowns *do* clear seats but are depot-scoped and run-triggered; the run governor **explicitly exempts `cert_harness` in both its loops**; and the nightly purge covers three event tables and no state tables. **There is no janitor** — cleanup rides on the next run, so a depot nobody runs again stays as the last thing left it, forever. And no run explains the residue: `ottoq_sim_runs` is not purged, holds 811 runs back to 2026-06-19, and **zero** on that depot — so "orphan occupancies" attributes them to a parent that never existed. Also: `idx_stalls_one_vehicle_per_stall` is `UNIQUE (current_vehicle_id)`, which enforces one *stall per vehicle* — **the name is inverted** on a constraint guarding the assignment-plus-verification invariant. NOT reproduced: the 96 double-seat attempts (the CAS reads as correct; the counter that would show them is not attributable to a statement). NOT investigated: the run-keyed grant, the seven 0129 leg cursors, per-seat subtransactions — recorded as untouched rather than implied covered. | [0138](db/checks/0138_the_benchmark_depot_is_forty_percent_held_by_nothing.sql) | **answers an open question**: the Benchmark depot is not a viable second certification lane until cleared. Clearing is a data operation, not a migration — `ottoq_tick_invariance_reset_fleet` already does exactly this and returns 64 stalls; it must not run while a pair is in flight. Whether a periodic janitor should exist is a product decision, because it changes what a depot looks like between runs. | open |
| **G14** | Calibration priors sit outside the reproducibility key, and the weekly ingest refit them mid-round (round 19). | [0115](db/checks/0115_round_nineteen_the_priors_moved_under_the_engine.sql) | **partial — `h_cal` is in the verdict, so two arms on different priors now FAIL rather than pass quietly. Two facts measured 2026-09-08 15:08 UTC that sharpen what is left.** (1) `ottoq-twin-ingest-weekly` has fired five Sundays running (08-09, 08-16, 08-23, 08-30, 09-06) and **every run took 0 seconds** — a refit that costs nothing across five weeks is either doing nothing or short-circuiting, and which it is has not been established. (2) `h_cal` is single-valued (`11a24626`) across all 57 certification pairs that carry it — but the earliest is **2026-09-06 15:56**, and that Sunday's ingest ran at **04:05**, so **no pair has ever run on both sides of an ingest.** The instrument that would catch a mid-round refit exists; it has never been given the chance to. Decisive experiment, cheap and dated: a pair shortly before and shortly after the next ingest (Sunday 2026-09-13 04:00 UTC). Until then "the priors are stable" is an absence of evidence, not evidence of absence. | open |
| **G16** | **CONVICTED, and larger than the task line.** Arm B's boot writes are stamped with arm A's run id — `ottoq_sim_stop_and_reset` sets the transaction-local `ottoq.sim_run_id` GUC to the run it tears down and nothing re-points it before the next arm's fleet reset. Measured: arm A's run carries **exactly 116 more** `vehicle.state_changed` trigger events than arm B's, on the same pair. The larger half is arm A, where the GUC is not yet set at all: `ottoq_active_sim_run_id()` returns NULL, and the state-change trigger writes `data_source = CASE WHEN v_run IS NULL THEN 'production' ELSE 'twin' END` — so the harness emits **~109 HMAC-signed events per pair labelled `production`**, 26,740 of them live in the current retention window. No round can fail on it: both arms exclude their own reset events from `h_evt` symmetrically, and arm B's contamination of arm A's run lands after arm A was hashed. Invisible to the verdict by construction. | [0131](db/checks/0131_the_certification_writes_twenty_six_thousand_production_labelled_events.sql) | **fix drafted as [0228](db/migrations/0228_provenance_asks_the_depot_not_the_run_id.sql) — a THIRD option, and it is 0073's, unpropagated.** 0131 Q7 costed two: suppress the reset's events (hash-neutral, but the suppression flag is read by the triggers on every write to `vehicles` and `stalls`, production included, so a stuck flag silences the whole state-change stream — a strictly worse failure) or re-point the run id (correct, but moves every column's `h_evt`). 0073 already settled this class for the SDR emitter: derive `data_source` from `depots.feed_mode` — `external` is the real world, everything else is the twin — rather than from whether a run id happened to be set. The triggers never got that treatment. No suppression flag, so the hazard does not exist; no change to any event's run scope, so no canon moves. Blast radius measured over 1,823,428 state-change events: every row with a run is already `twin` and stays `twin`; the rows that change are exactly the null-run ones on a non-external depot. **Part B in the same file:** 27,440 of the 27,460 come from the vehicles trigger, which passes `p_fleet_operator_id` and never `p_depot_id` — so "what happened at the flagship depot" is unanswerable for vehicle events. The fix has to resolve the depot anyway to read its feed mode. | **CLOSED AT THE SOURCE** — [0228](db/migrations/0228_provenance_asks_the_depot_not_the_run_id.sql) applied 2026-09-08 11:21 AM CT. Both triggers derive `data_source` from `depots.feed_mode`, and the vehicles trigger passes `p_depot_id` at both sites for the first time. P1 proved `forces_recert FALSE` on the `h_evt` expression isolated out of the live verdict function rather than asserting it. **What it cannot do:** the 27,460 already-mislabelled events are signed and the signature covers the mislabel, so relabelling invalidates it and deleting is a deletion from an audit ledger. They stay, and they stay wrong. |

## Open, but diagnosed this session

| # | Finding | Evidence | Fix | State |
|---|---|---|---|---|
| **G81** *(**MEASUREMENT AND WIRING BOTH FIXED**; consumption deliberately left. [0365](db/migrations/0365_the_overstay_has_a_name_and_no_reader_so_give_it_one.sql) (applied `20260920033500`) added `ottoq_stall_overstays(uuid)`, a run-scoped instrument — its first live read on `3fb415d8` found **8 concurrent overstays**, including **NASH-DCFC-STALL-09 held 188 minutes past its window** (one of only ten DCFC) and NASH-L2-STALL-31 at **241 minutes**. [0366](db/migrations/0366_the_frame_now_says_which_vehicles_are_overstaying_a_charge_stall.sql) (applied `20260920034500`) publishes `occupies_charge_stall_unbooked` on every frame vehicle, in the existing `g.facts >= 1` block beside `holds_charge_place`, reusing the `proposer_frame_facts` gate 0265/0287/0292 used. **Both `forces_recert FALSE`, and P4 asserts the grounds in each: no `ottoq_hash_*` atom reads the frame builder.** **Verified by two independent implementations agreeing:** the frame flags **7** vehicles and 0365s standalone instrument reports **7** charge-stall overstays on the same run, with all 116 vehicles carrying the key. A published fact that no separate instrument can confirm is how a key that is always false looks installed. **WHAT REMAINS, AND IT IS DELIBERATE: nothing consumes it yet.** Teaching `vehicle_is_held` to treat an overstaying vehicle as placed-in-fact is the change that actually stops the redundant offers, and it changes what the engine proposes — so it wants its own file and its own paired-seed before/after. Publishing and consuming were split on purpose after this entrys neighbour G80 needed three revisions for moving faster than its evidence.)* | **The engine detects an overstay, names it precisely, and publishes the name to nobody: `window_elapsed_occupied` has one writer and zero readers, 162 times on a single run.** This is the ROOT CAUSE under G80. `ottoq.ottoq_release_expired_bookings` releases a booking whose window has elapsed **while the vehicle is still occupying the stall**, and stamps `release_reason = 'window_elapsed_occupied'` — a deliberate, specific name for exactly the condition G80 chased. So the overstay is not unnoticed drift; it is a recognised state. **Measured on Arm A `3fb415d8`, and the ratio is the part that matters: `window_elapsed_occupied` = 162 against plain `window_elapsed` = 143.** More bookings expire with the asset still plugged in than expire cleanly — 53% of window expiries are overstays, so this is the normal case, not an edge case. **And nothing consumes it.** Searched exhaustively: the string occurs in **one** object in the whole database (the function that writes it) — no other function, no view, no matview — and nowhere in `edge-functions/`, `bridge/`, `proposer/` or `metrics/`. **A named signal with one writer and no reader.** The release itself is not wrong: it does not touch `stalls.current_vehicle_id`, so it never lies about physical occupancy — verified in G80, where 7 of 7 overstaying vehicles had the stall agreeing they were present. What is missing is any **reconciliation**: the calendar forgets the stall while the world still holds it, and every downstream reader (the decision frame, the proposer, the assignment path) then sees a free stall that is not free. G80 measures the price — **41** `assignment_refused_occupied` preflight refusals on this run, at least 8 naming a current overstayer. | the one-writer/zero-reader search across `pg_proc`, `pg_views`, `pg_matviews` and the repo's TypeScript and Python; the `release_reason` census on Arm A (162 / 143 / 22 / …); and the worked booking row for `b97789b5` on `NASH-L2-STALL-31` showing `state='done'`, `release_reason='window_elapsed_occupied'`, `source='reservation_honoured'`, window 06:57:26–07:14:32, `released_at` 07:14:44; **and one NEGATIVE check worth recording so it is not re-chased: the vehicle and stall records do NOT disagree with each other.** Of 57 vehicles claiming a stall on the twin depot, `stalls.current_vehicle_id` agrees with all **57**, with zero empty and zero naming a different vehicle. A conflict row appearing to show two claimants for one stall was two observations ~54 sim-minutes apart (conflict at sim 08:32, frame at 09:26), not a structural inconsistency — the same time-gap artifact that produced two of this register's corrections. So the divergence is **calendar versus world only**, which is exactly what makes `window_elapsed_occupied` the right signal to publish | **Not fixed here.** The cheapest honest fix is to make the signal readable rather than to change the release: publish the overstay so the frame carries it (a vehicle occupying a stall whose booking has been released), which lets `vehicle_is_held` and the proposer stop treating that stall as available, and lets the assignment path stop issuing commands preflight will refuse. `space_conflict_ledger` is the existing home for *"every calendar claim overruled by physical reality"* and already records the downstream refusals, so the overstay belongs beside them. **Do NOT fix it by re-extending the booking** — that would let a vehicle hold a stall indefinitely by not leaving, which is the thing the window exists to bound. | open — measured on live Arm A `3fb415d8`, twin depot only. Same family as G44 (rules declared with no caller) and `0349` (an attestation nothing checked): the system knows, says so, and no one is listening. It is the third time tonight that what looked like a solver problem turned out to be bookkeeping drift between the calendar and the physical world — after the reservation leak (G76) and G80. |
| **G80** | **Seven of twenty vehicles charging in a stall hold no live booking on it — an overstay — and the proposer, which correctly trusts the calendar, therefore offers them a different charge stall.** Found by the matched-frame comparison (`scripts/compare-plans-on-one-frame.sh`), which judges both solvers on the **identical** decision frame and so carries none of the run-level A/B's cadence confound. **The chain, every link measured on Arm A `3fb415d8`:** of **20** vehicles whose `current_state` is `charging_l2`/`charging_dcfc` with a `current_stall_id`, **13** have a covering live booking on that stall and **7 do not**. For all **7 of 7**, `stalls.current_vehicle_id = vehicle.id` — so the vehicle record and physical stall occupancy agree, and **the calendar is the outlier**. That is an overstay: the booking finished and the asset never left. `space_conflict_ledger` exists for exactly this (*"every calendar claim overruled by physical reality"*) and C7's failure library names `overstay` as one of the canonical nine. **Worked example.** `b97789b5` sits in `NASH-L2-STALL-31`, whose L2 booking is state **`done`** for the window 06:57:26–07:14:32 while the sim clock reads **09:26:53** — expired **2h12m** earlier. Its only *covering* booking is a **staging** stall, `NASH-STG-S031` (07:40–12:00). So the frame's `holds_charge_booking = false`, `holds_charge_reservation = false`, `holds_charge_place = false`, `has_live_booking = true` are **all correct**. `vehicle_is_held` short-circuits on `holds_charge_place` — its docstring says that answer *"wins outright"* — so `has_live_booking` is never consulted, the vehicle is judged plannable, and CP-SAT offers it `NASH-L2-STALL-19` at 19 kW against `soc 70 / target_soc 100`. **Given the calendar, that offer is defensible.** Given the physical world it is wasteful, because the vehicle is already drawing power in a charge stall. **AMENDMENT — the cost is measured, and the engine is SAFE.** `space_conflict_ledger` holds **41** rows for this run, every one `conflict_kind='assignment_refused_occupied'` with `resolution='command_refused_preflight'`: a `begin_charge` command stopped because another vehicle was physically present, `detail.code='target_occupied'`. Of those 41, **8** name a *currently*-overstaying vehicle as the present occupant — a **lower bound**, since the overstay set is a snapshot at one clock and the other 33 involve stalls whose occupant has since moved. So the chain completes: a booking reaches `done` while the asset stays plugged in → the calendar shows the stall free while it is physically occupied → an assignment is made to it → preflight refuses the command. **Nothing is double-booked and nothing unsafe happens** — this is precisely the assignment-plus-verification pair CLAUDE.md rule 6 says never to remove, doing its job 41 times. **The cost is wasted work, not risk:** 41 refused commands, plus proposer effort spent offering charge stalls to vehicles already drawing power. **CORRECTION — IT DOES NOT ACCUMULATE, AND I CLAIMED IT DID FROM TWO POINTS.** This amendment first read *"it accumulates, which is what makes it a leak rather than a regime… 7 of 20 (35%) to 10 of 18 (56%)"* and called it the same signature as the reservation leak `0360` fixed. Sampled properly across Arm A the share **fluctuates and if anything declines**: tick 455 → 10 of 18 (**55%**), tick 489 → 6 of 15 (40%), tick 521 → 5 of 22 (**22%**), tick 556 → 7 of 23 (30%). **What is true is persistence, not growth:** an overstay is present at every sample, at **22–55%** of vehicles charging in a stall. That is a standing regime, and a regime at that level is worth fixing on its own terms — but it is not a leak and the reservation-leak analogy is withdrawn. The rising `conflicts_logged` (41 → 45 → 58 → 66 across those same ticks) is **not** evidence of worsening either: `space_conflict_ledger` is cumulative, so it can only rise. Its *rate* is roughly steady at ~0.25 refusals per tick. **Instant-consistent pool figure**, measured from the frame rather than across two moments: of 116 frame vehicles the serviceable pool is **25, all 25 already in a stall**, and the frame holds zero `staged_awaiting_service` and zero `arrived_at_gate`. | the matched-frame log (`matched_3fb415d8.jsonl`, contamination asserted 0 on every sample, so the observation was passive); the 20 / 13 / 7 booking-coverage census; the 7-of-7 physical-agreement check that identifies the calendar as the outlier; this vehicle's booking rows read directly with stall codes, types, states and windows; and the frame's four hold-flags read against `vehicle_is_held`'s actual short-circuit order | **Not fixed here, and the fix is NOT the one this finding first proposed.** The real defect is the overstay: a booking reaching `done` while its vehicle still occupies the stall, with nothing reconciling the two. Fixing the proposer instead would be treating the symptom. When it is addressed, the honest options are to release-and-rebook the overstaying vehicle so the calendar matches the world, or to record the overstay in `space_conflict_ledger` and let the proposer see it. **A move-cost term in the objective is still worth having** so a lateral move can never look free, but it is a mitigation, not the cause. | open — measured on live Arm A `3fb415d8`, twin depot only. **THIS ENTRY HAS BEEN WRONG TWICE AND BOTH ARE RECORDED RATHER THAN OVERWRITTEN.** (1) It first claimed *31 of 50 (62%)* of the pool was already stalled; that census came from `vehicles` ~85 sim-minutes from the frame that produced the offer, the clock-domain error class of G75 and `0251` §5. (2) It then claimed the offer was a **gratuitous lateral move to a mid-charge vehicle with no declared need**. The vehicle is physically mid-charge, but it holds **no booked charge place** and is **30 points below target**, so the offer is reasonable on the evidence the proposer is given — and the `svc_step: None` I cited is not a statement that nothing is needed. (3) Consequently the recommendation I drew from it — *narrow the pool, exclude the charging states* — **would be wrong**: it would discard exactly these 7 genuine cases. The `--states` test that returned `serviceable = 0` proves only that this frame held no gate arrivals at that instant, not that CP-SAT had no real work. |
| **G79** *(**FIXED** by [0362](db/migrations/0362_a_worse_ranked_proposer_removes_a_better_one_from_the_pool_before_rank_is_read.sql) (applied `20260920024530`) + [0363](db/migrations/0363_the_gate_0362_installed_could_never_be_opened.sql) (applied `20260920025230`), both `forces_recert FALSE`, gated by `proposer_rank_protects_supersede`. **Measured with the gate ON on run `f14d5620`, fire_id 118 / tick 598: `forward_lex` rows lost to `newer_proposal_same_entity` went to ZERO**, while cuOpt's 16 self-supersessions continued — equal ranks still supersede on recency, which is the intended behaviour. **AND THE REMAINING 9 SUPERSESSIONS TURNED OUT TO BE CORRECT, which is worth more than the fix:** they carry `entity_decided_by_other_proposal`, written by `ottoq_decide_tick`, which also does not read precedence — so they looked like a second instance of the same defect. They are not: **9 of 9 of those vehicles are physically in a stall.** They were served, so the `forward_lex` proposal for them was genuinely moot. `ottoq_decide_tick` needs no rank guard. **No starvation, which was the whole reason for the gate:** the failure mode feared was a protected proposal holding a vehicle forever, and every affected vehicle was placed. That measurement is what would justify promoting the gate from measured to enforced. One residual naming imprecision, not filed as its own finding because nothing is lost by it: 0 of those 9 entities had an *enacted proposal*, so `entity_decided_by_other_proposal` names a proposal where the local decide path did the placing — G78's family, cosmetic here.)* | **Proposer rank IS enforced where pending proposals are SELECTED and was not consulted where they are SUPERSEDED — so a later-submitting worse-ranked proposer removed a better-ranked proposal from the pool before the rank-ordered selection could prefer it.** `ottoq_l2_external_proposal` sorts on declared data first: `ORDER BY COALESCE((SELECT pp.rank FROM ottoq_proposer_precedence pp WHERE pp.source = p.source), 2147483647) ASC, p.created_at DESC, …` — rank first, unlisted at the maximum, recency only as a tie-break. **That half always worked.** But `ottoq_submit_external_proposal` superseded unconditionally — no rank term, no source term, and the string `ottoq_proposer_precedence` did not occur in it — and supersession moves rows out of `pending`, the only status that `ORDER BY` can see. So cuOpt (**rank 10**, *"service-failure fallback. Lower priority than CP-SAT"*), firing every 30-second tick, removed `forward_lex` (**rank 0**, the declared primary CP-SAT proposer) from the candidate pool before ranking happened. **A pool filtered by recency before a correct ranking is applied.** Measured before the fix on run `f14d5620`: of 26 superseded `forward_lex` rows, 12 had a later `cuopt` proposal for the same entity; the other 14 were not claimed as cuOpt's doing. | the two `ORDER BY` / `UPDATE` bodies read directly; `position()` returning 0 for `ottoq_proposer_precedence` in both supersede writers; the 12-of-26 entity-matched count before; and after the fix, zero `newer_proposal_same_entity` on `forward_lex` with 9 of 9 remaining supersessions resolving to vehicles physically in stalls | **Fixed, and the fix reuses the ordering that already works (rule 5):** the guard does not rank anything, it declines to DELETE a strictly better-ranked candidate and lets the existing sort judge it. Strictly better only, so a proposer refreshing its own plan still replaces it — a stale plan outliving its world is G62/G71. **Remaining work is promotion, not repair:** the gate is set on one run; enabling it by default wants a second run's worth of the same no-starvation measurement, and per 0362's own note **the run where the key is first set to 1 is the run that needs a fresh canon.** | **fixed — measured-not-enforced.** Also note the defect *inside* the fix: 0362 shipped a gate that could not be opened, because `ottoq_policy_params.param_key` has an FK to `ottoq_policy_param_catalog` and the key was unregistered. Caught within minutes by trying to use it (23503); 0363 registers it. Credit to the FK — it refused the write rather than letting a permanently-closed gate read as installed. |
| **G78** | **The disposer has no concept of an abstention, so a proposer that correctly declines is recorded as a refusal for a malformed stall id — and the more honest the proposer, the worse it scores.** Measured on the first `forward_lex` fire this engine has ever had (fire_id 116, tick 442, run `f14d5620`): of 16 submitted rows, **11 were deliberate abstentions** carrying `abstain: true`, no `stall_id`, and an explicit rationale — *"planned to start at +111 min on l2 95fe1b50…, beyond this tick's 30-min window; re-offered when due"*. That is CP-SAT planning a horizon and offering only what is due now, which is the correct behaviour for a rolling re-solve. The disposer scored one of them **`refused` / `invalid_stall_id`** and superseded the rest. Established rather than inferred: `ottoq_dispose_external_proposals` contains `invalid_stall_id` at char 1,958 and **does not contain the string `'abstain'` anywhere (position 0)** — it has no abstention branch at all, so a row with `abstain: true` falls through to the malformed-stall-id path. **Why it is worth filing rather than shrugging at:** nothing unsafe happens — an abstention is correctly not enacted — but the refusal tally is the instrument by which proposer quality gets judged, and it now charges a proposer for declining. cuOpt largely does not abstain, so it never paid this; CP-SAT abstains **11 of 16 rows by design**, so it pays it constantly. Any A/B that scores refusals would conclude the more careful solver is the worse one. **Same defect class as G74 (a rule reporting a pass for a check it never ran) and G73 (a verdict whose only possible word was the wrong one): an instrument mislabelling correct behaviour, which is the failure mode most likely to be believed.** | the disposition census on run `f14d5620` after fire_id 116, the refused row inspected directly (`abstain = "true"`, `stall_id` NULL, `verb = assign_stall`), and the comment-stripped `position()` scan of `ottoq_dispose_external_proposals` showing `invalid_stall_id` present and `'abstain'` absent | **Not fixed here.** Give the disposer an abstention branch before the stall-id validation: a row with `abstain: true` is neither enacted nor refused — it is a recorded declination, ideally its own `disposition_reason` (`proposer_abstained`) so the refusal tally stops absorbing it. Cheap, and it must land before any A/B scores refusals across proposers, or the comparison is rigged against the better solver. | open — measured on live run `f14d5620`, twin depot only. Found by firing CP-SAT for the first time, which is the general point: the abstention path had never been exercised because the only proposer that had ever run barely abstains. |
| **G76** *(**AND THE FIX CREDITED HERE WAS MIS-ATTRIBUTED — corrected by G82.** The improvement recorded when 0360 applied — free stalls 2 → 30, charge-type 0 → 11, occupancy 21 → 43 — was real but **did not come from 0360's tick wiring**, which G82 proves had never executed on a metronome-driven run. It came from the one-off direct invocation of `ottoq_release_unusable_reservations` made while verifying that migration. A single manual call is not a fix; it looked like one because the numbers moved. 0367 is what actually puts the reclaimer on the live path.)* | **The twin depot is 98% reserved and 13% occupied: the binding constraint is reservation hygiene, not capacity, and not solver quality.** Measured on live run `f14d5620` (busy_day, twin depot, rule 8), depot-wide: **155 of 158 stalls carry a reservation, 21 hold a vehicle, and 135 are reserved-and-empty.** Only **2 of 158** stalls are unclaimed by anything — so a proposer's legal move set is ~1% of the site while 87% of the site is physically free. The sharpest slice is the one Chase named as the pressure valve: **113 staging stalls, all 113 reserved, 4 occupied, 109 reserved-and-empty** — and 29 of those staging reservations are ALREADY EXPIRED against the sim clock and not reclaimed. On the 40 charge stalls the picture is the same (40/40 reserved, 0 truly free) and **8 of them, all L2, are held by vehicles that cannot drive to them — 6 `tow_requested`, 2 `staged_for_departure`**; **zero routines in the database mention both `tow_requested` and `reserved_by`**, so nothing releases a reservation when a vehicle is immobilised. **This is the upstream cause of three symptoms that were being read as solver problems.** The CP-SAT bridge abstains with *"40 charge-capable stall(s) and not one is offerable this tick"* — correctly. cuOpt's proposals land as 4 enacted / 7 refused / 60 superseded because it is proposing into a fully pre-committed site. And Nemotron's objective directive (`readiness_first` / `throughput_first` / `energy_balanced`) is moot when no solver has a legal move. **No optimiser can outperform another when neither has an admissible action.** **Confirmed across ticks, not one instant:** sampled at ticks 337 and 344, `truly_free` = 0 of 40 and CP-SAT `status='empty'` both times, dead-weight holders 7 then 9. | the CP-SAT fire record via the offline bridge route (`bridge/proposer_bridge --frame/--classes/--emit-sql`, which needs no DSN, no edge secret and no host), plus the depot-scoped stall census and the holder-state census joined through `stalls.reserved_by` | **Not fixed here, and the obvious fix is wrong — see G77.** Two separable pieces: (a) release a stall reservation when its holder enters a state that cannot consume it (`tow_requested`, `staged_for_departure`), which is the one-function change and returns ~20% of charge capacity; (b) reclaim reservations that have expired on the SIM clock, which is 29 staging stalls on this run. **Do NOT implement (b) by scheduling `ottoq_gc_stale_reservations` — G77 is why.** | open — measured on live run `f14d5620`, twin depot only. **Directly on Chase's stated goal** (how many vehicles this depot can stage, sort and orchestrate at once): the answer today is limited by reservations that outlive their purpose, not by stalls. |
| **G77** *(**see the mis-attribution note on G76, corrected by G82.** 0360 fixed this routine's clock defect, which stands; what it did not do is put a reclaimer on the path the live engine uses.)*  | **The only routine that reclaims stall reservations compares a SIM-clock column to `now()` (WALL), has zero callers and zero cron jobs — and scheduling it, which is the obvious fix for G76, would erase every live reservation on the depot.** `ottoq_gc_stale_reservations` clears `reserved_by` where `reservation_expires_at < now()`. `reservation_expires_at` is written on the **sim** clock; `now()` is **wall**. Measured on run `f14d5620`: wall is **4h46m AHEAD** of the sim clock, so of **156 reserved stalls, 127 are live against the sim clock and 0 are live against the wall clock — the function would clear all 156**, including every one of the 127 valid ones. Vehicles `staged_awaiting_service` would lose the stall they are queued for. **Why it is dormant rather than damaging: it is dead code.** Zero `cron.job` rows reference it and **no routine anywhere in the database calls it** (`position()` over every `prosrc`, not `LIKE` — `_` is a wildcard). So it has never run and no number is wrong today. **The reason this is filed at all, and why it is more than hygiene:** G76 says reservations are not being reclaimed, and a reader who greps for a reaper finds exactly this function and schedules it. **The obvious fix for G76 is the trigger for G77.** Same defect class as G75 and `0251` §5, but unlike G75 the bad arithmetic is already written into a function rather than merely invited. | the clock-domain measurement (`live_vs_SIM` = 127, `live_vs_WALL` = 0, `gc_would_clear` = 156 of 156 on one depot-scoped query), the empty `cron.job` scan, and the empty caller scan | **Fix before use, not after.** Change the predicate to the run's `sim_clock_current` (the pattern `0357` applied to `ottoq_sim_release_depot`: `COALESCE(r.sim_clock_current, r.sim_clock_start)`, never `now()`), and only then give it a caller. Its second arm — clearing reservations when no run is `running` for the depot — is wall-clock-free and already correct. | open — dead code today, so latent; but it is the landmine directly under G76's fix, which is why the two are filed together |
| **G75** | **`ottoq_stall_bookings` carries `booked_at` on the WALL clock and `released_at` on the SIM clock, in the same row, under names that read as a pair.** Measured on run `92a6ac36`: **1,202 of 1,202 released bookings have `released_at < booked_at`** — a stall released before it was booked, in **100%** of rows, because `booked_at` sits at 2026-09-20 00:23 (wall) and `released_at` at 2026-09-19 13:01–16:15 (sim). Anyone computing hold duration the obvious way gets a **negative** number every time. The table also carries `booked_at_sim` (sim) and `during` (sim), so it holds **three** timestamps on two clocks. **Severity calibrated rather than asserted: latent, not live.** Measured, **no** routine or view anywhere does `released_at - booked_at` arithmetic — the two functions touching both columns only SET `released_at`; duration is taken from the `during` range, whose bounds are both sim and therefore internally consistent. So no number ships wrong today. It is a landmine for the next person to write the obvious query — which is exactly what `0255` was about to do — and it sits directly under 2.9's KPI #2 (`service_point_turns_per_point_per_day`) and KPI #5 (`p95_time_to_service`). | [0255](db/checks/0255_three_quarters_of_the_refusals_are_the_proposer_being_late_not_the_depot_being_busy.sql) Q4 (the impossible ordering, asserted as 100% of rows) and Q5 (the severity gate — if it ever returns a row, this has gone from latent to live) | **Not fixed here.** Stamp `released_at` on the wall clock to match `booked_at` and add `released_at_sim` beside `booked_at_sim`, so the pair that reads like a pair is one. That is a migration and a backfill decision (existing rows are sim-stamped), not a comment. | open — measured, twin depot only |
| **G71** *(**ANSWERED** by [0255](db/checks/0255_three_quarters_of_the_refusals_are_the_proposer_being_late_not_the_depot_being_busy.sql) — and the answer inverts the charitable reading. Of **39** refused proposals, **29 (74%) asked for a stall that was ALREADY HELD at the moment the proposal was created**; only **10** are genuine contention. On `stall_occupied` specifically it is **11 of 13**. So three quarters of the refusals are **the proposer being late, not the depot being busy** — which is G62 biting: a 30,394 ms mean chain against a 30-second beat produces proposals about a world that has already moved. **Two independent methods**, one purely wall-clock (when was the stall claimed) and one on the `during` range (was it held when proposed), give **28 and 29 of 39** — agreeing in aggregate. **Stated honestly: only 24 rows are called stale by both**, so the proportion is solid and a per-row verdict from either method alone is not. **This changes what to fix:** not more stalls and not a smarter objective, but a fresher frame, or a RANKED SET the kernel can pick from — a single-stall proposal gives the disposer nothing to dispose over, which is the propose/dispose pattern CLAUDE.md already describes. Choosing among those is a design call and is not made here.)* | **A refusal for `stall_occupied` does not separate contention from a proposer reasoning over a stale frame.**
| **G74** *(**CLOSED** by [0355](db/migrations/0355_the_energy_shield_declared_five_block_rules_at_the_moment_power_flows_and_none_had_ever_run.sql) (applied `20260919235525`) + [0356](db/migrations/0356_the_shield_finally_sees_the_moment_power_begins.sql) (applied `20260919235738`), both forces_recert TRUE; after-measurement in [0254](db/checks/0254_the_energy_shield_now_runs_at_the_moment_power_begins.sql). **And it was bigger than filed: `charge_session_start` is declared by FIVE active rules, every one `enforcement='block'`** — EN.001 grid capacity and EN.005 grid-event hardstop both `safety_critical`, plus EN.002 stall power ceiling, EN.004 demand-response compliance and HW.002 charger-state precondition. **None had ever been evaluated**, so the whole energy-safety cluster between the orchestrator and the switchgear was dark; four more of G44's nine are now called. **The engineering call: `stall_assignment` is the calendar claim and `task_start` mostly carries no load — `charge_session_start` is where electrons actually flow, so that is the gate, and the fix supplies the rule's input there rather than teaching `task_start` to fake one.** Measured on live run `92a6ac36`: **56 sessions, 280 evaluations, 280 = 56 × 5 asserted — every session gated by every rule**, EN.001 **56 of 56 really evaluated** with a measured headroom (min 95.6 kW) and zero abstentions at that context. **MEASURED, NOT ENFORCED** per 2.9a: 280 of 280 passed, 0 `would_block`, nothing refused; promotion reads `ottoq_shield_probe.would_block`. **A latent blocking bug found by the pre-flight and fixed:** HW.002's `COALESCE(ARRAY(SELECT jsonb_array_elements_text(…)), ARRAY['Available'])` can never reach its fallback because `ARRAY(SELECT …)` returns an empty array rather than NULL — so with empty parameters that `block` rule **rejected every charger, healthy ones included.** Dormant only because its registered defaults supply `allowed_states`, and dormant *behind* the other defect. **Honesty correction to this finding as filed:** it claimed abstentions were "indistinguishable from a real check by any count that does not parse the reason string" — **wrong**; `result_payload = '{}'` identified them exactly (12,831 of 12,831 measured). The real defect was an undocumented convention nothing asserted; `en001_evaluated` now labels it.)* | **The grid-capacity rule reports a PASS for 8,219 of its 8,663 evaluations without checking anything, and the depot demonstrably runs to within 1.6% of its engineering cap.** `ottoq_eval_en_001_grid_capacity` opens with `IF v_depot_id IS NULL OR v_request_kw <= 0 THEN RETURN ROW(TRUE, 'no depot/request context', …)` — **fail-open on a rule whose own code calls a violation `safety_critical`.** Measured over every EN.001 evaluation the engine has logged: at **`task_start`, 8,219 evaluations, 0 carrying `requested_kw`, 8,219 abstentions recorded as passes, never once evaluated in the engine's life**; at `stall_assignment`, 444 evaluations with 443 carrying real load, so there the wiring works. **And the regime makes it matter:** 494 evaluations carry a measured headroom figure and the **minimum is 26.1 kW against a 1,620 kW engineering cap** (1,800 kW nameplate less a 10% margin) — the depot ran to **1.6%** of its limit, so a guard blind at one of its two probe points is not academic. **Worse than G44 rather than the same:** G44 is nine rules with no caller, visibly absent and countable; this rule *is* called 8,219 times and every call lands in `ottoq_rule_evaluations` as `passed = true`, indistinguishable from a real check by any count that does not parse the reason string — **it inflates the shield's apparent coverage.** Honest sentence: *"evaluated 444 times at stall_assignment, 494 carrying a measured headroom, never failed, minimum margin 26.1 kW — and abstained 8,219 times at task_start, where it cannot see a load."* Of 68 context-less passes on the test run, 41 targets are not stalls at all, 16 wash_bay and 10 staging are correctly waved through, and **1 was an `l2` charging stall**. Side effect on the criterion Chase named: `ottoq_activity_feed` is polled by `TwinDecisionLogTab` every 4s and renders that reason verbatim, so **68 of 326 rows (21%) show the internal string "no depot/request context"** to whoever is watching — never blank, but engineering jargon on the decision type an auditor clicks first. | [0253](db/checks/0253_start_stop_restart_works_and_the_grid_guard_is_blind_at_one_of_its_two_probe_points.sql) Q1 (the per-probe-point census), Q2 (asserts every abstention is recorded as a pass — 8,355 of 8,355), Q3 (the 26.1 kW minimum headroom), Q4 + Q4b (the target split, with the arithmetic guard that caught a missing depot predicate), Q6 (what the UI renders) | **Not fixed here, and scoped rather than hand-waved.** The hash-safe half: `ottoq_hash_rule_evaluations` digests `passed`, `result_payload` and `parameters_used` and **not** `reason` (measured), so rewording the abstention costs no recert and clears the UI string. The half that forces recert: making the abstention *countable* needs a marker in `result_payload`, which **is** digested — a migration classified `forces_recert: TRUE`. The real question underneath is whether EN.001 should be registered at `task_start` at all, or whether that probe point should supply `requested_kw` (a charging task_start does imply load) — a wiring decision, and the one worth making. Either way **the rule must not report a pass for a check it did not run.** | open — measured on live runs `fa7c2782` / `1fd7bfbb`, twin depot only |
| **G73** *(CLOSED by [0354](db/migrations/0354_the_review_judged_the_whole_run_from_three_chains_and_a_ledger_that_never_held_cuopt.sql), applied `20260919224417`, forces_recert TRUE; measured after in [0252](db/checks/0252_the_verdict_that_could_only_say_one_word_now_says_three_true_things.sql). **And the diagnosis below was incomplete — the window was only half of it.** The verdict's "nothing came back" branch tested ONLY `ottoq_proposer_fire_log.n_submitted`, a table holding **112 of 112 rows `declared_source='forward_lex'`, never one cuOpt row**, so on any cuOpt-served run `solver_returned_nothing` was **the only word the function could emit** — unfalsifiable, not merely mis-scoped. 0354 adds `v_tot_landed` from `ottoq_external_proposals` (where cuOpt's proposals actually land) and requires both ledgers silent before claiming silence; the live window immediately re-classified to **`kernel_refused_all`** — a different and correct diagnosis. It also names the scope (`verdict_scope: 'window'`) and adds a run-scope block with its own `run_verdict`, which reads **`proposals_enacted`**. Both scopes now disagree and both are true. It reaches the agent: `agent_review_enabled` is 1 on the live run, so `ottoq_agent_board` — the only thing `ottoq-orchestrator-agent` reads before deciding — now carries `run_verdict: proposals_enacted` where it used to carry the false word, and `ottoq_intelligence_stack` carries both for the Twin panel. Every pre-existing key preserved (A5); the UI reads `verdict` as a free string so the rename broke nothing. **Three defects of mine caught before applying, all invisible to compile-check because plpgsql plans lazily:** a precondition selecting `FROM public.ottoq_determinism_pair` (no such table — it is a FUNCTION; 0340 uses `pg_stat_activity`), a signature check against `pg_get_function_identity_arguments = 'uuid, integer'` that returned zero rows because this server includes parameter names, and READ COMMITTED making A3/A4 flaky on a ticking run. The read-only dry-run `scripts/APPLYING.md` mandates caught the first two.)* | **The return leg of the loop works, and its verdict says the opposite.** Chase's governing requirement is agent → deterministic → review → back to the agent. `ottoq_agent_review` (0342) delivers it, and on live run `a51acc84` the loop is traceable end to end on the producer's own key: **17 agent chains reached a proposal, 17 of 17 resolve to a real agent decision, 3 of 17 had a proposal enacted**, against **17 NVIDIA calls returning 24 proposals** — and the 24 stamped rows agree with the 24 returned exactly. **But the review returns `verdict: "solver_returned_nothing"`, `totals.enacted: 0`, `linkage: none` ×3.** Every per-chain reading is *correct*: it examines the last 3 nemotron chains (ticks 423/425/427) and the newest stamped proposal is at tick **339**, so those three genuinely have none, and `linkage: 'none'` is 0342 refusing to report zeros that read as refusals. **The defect is the verdict**: it is one of six machine-actionable words with no window qualifier, so a 3-chain sample publishes in run-level vocabulary. An auditor would read the solver as dead on a run where it placed 17 calls and got 3 chains enacted. | [0251](db/checks/0251_the_loop_closes_end_to_end_and_the_review_calls_it_nothing.sql) Q1 (the per-chain loop), Q2 (every stamp resolves to an agent decision — a stamp resolving to nothing would be worse than no stamp), Q3 (the review's own answer beside Q1's) | **Not fixed here.** Two options of different size: qualify the word (`solver_returned_nothing_in_last_N_chains`), or compute the run and the window separately and report both. Prefer the second — the run-level number is the one an auditor wants and it is already derivable. | open — and note **what it is not**: the linkage is sound, the loop closes. This is a reporting defect over working machinery, which is the failure mode most likely to be believed. |
| **G72** *(**FIXED** by [0364](db/migrations/0364_a_proposers_hit_rate_is_deleted_by_the_next_run_that_starts.sql) (applied `20260920035200`, `forces_recert FALSE`) — `public.ottoq_proposal_disposition_ledger`, append-only, registered `class='evidence'`, **no FKs** (same reasoning as 0340: an enforcing FK on evidence can only block the purge or, as CASCADE, erase what the registry's check (c) forbids erasing), filled by `ottoq_capture_proposal_disposition()` on `AFTER UPDATE OF status`, with the error swallowed so a capture failure can never abort `decide_tick`. **The decisive witness, measured live on run `3fb415d8` at 03:51 UTC:** proposal `caa2159e-39cd-4180-9091-46df11ae3771` (`greedy_constrained`, `enacted`, tick 930) is **1 row in the ledger and 0 rows in `ottoq_external_proposals`** — the working set has already deleted the row the ledger kept, which is exactly the loss this finding described. A second capture followed unprompted (`ottoq_service_priority`, `superseded`, 03:51:47), so the trigger is live and not a one-off. `ottoq_proposer_scorecard` reads the answer per source and divides `enacted_pct_of_committed` by **non-abstained** rows, so G78's defect — an honest abstention scored as a miss — cannot recur in the scorecard. **Honest limit: across-run survival is structural, not yet witnessed.** No demo run has started since the ledger was created, so the claim "it survives `ottoq_purge_prior_runs`" rests on the registry class and the absent FK, not on an observation; the first new run is what converts it. Also note `captured_at` defaults to `now()`, which is **transaction start** — the witnessed row's `captured_at` equals its `created_at` to the microsecond because that proposal was created and enacted inside one `decide_tick` transaction. For elapsed time use `disposed_at − proposal_created_at`, never `captured_at`. Item (a) of the old fix column — why `ottoq_proposer_fire_log` went dark after 2026-09-17 — is **not** addressed and is no longer on the critical path, because this ledger does not depend on it.)* *(previously amended by [0251](db/checks/0251_the_loop_closes_end_to_end_and_the_review_calls_it_nothing.sql) §4 — the scope below was too strong, in the direction that understates us: **within a run enactment IS measurable**, via the `agent_handoff.chain_id` stamped into the proposal JSON, and G73 measures it. What survives: across runs it is still lost to the `class='engine'` purge, and `ottoq_proposer_fire_log` is still not the instrument — 0 columns matching `%enact%` and **112 of 112 rows `forward_lex`, never one cuOpt row**, so "it went dark" was also wrong; it never logged this path at all. Honest scope: **traceable within a live run, not after it.**)* | **cuOpt's proposal-to-enactment rate is not measurable from any durable table, and the one evidence-class table that could measure it stopped writing two days ago.** Rule 6 requires cuOpt claims to be ledger-backed; `ottoq_model_call_ledger` (0340) delivers that for **calls** and **proposals returned** and for nothing downstream. **`ottoq_external_proposals` — where disposition lives — is a per-tick working set, not a ledger:** `ottoq_l2_optimize_assignments` and `ottoq_l2_propose_seat` each **open with a `DELETE FROM ottoq_external_proposals WHERE sim_run_id = … AND source = <their own>`** before writing the tick's rows, so a proposer erases its own history every tick. Measured: **45 rows across 2 runs** for the engine's life, and the two `greedy_constrained` refusals visible at tick 284 were **gone** at tick 305. It is also class `engine`, so the demo-run purge takes what survives the tick. **`ottoq_proposer_fire_log` IS class `evidence` and does survive its run — but it has 0 columns matching `%enact%`** (it counts `n_submitted`/`n_abstained`/`n_deferred`), holds 112 rows over 10 runs, and its most recent fire is **2026-09-17 00:29 UTC**: **0 rows for the live run** that started 2026-09-19 21:55. So the durable log is both structurally unable to answer the question and currently dark. | [0250](db/checks/0250_i_counted_five_depots_to_tell_chase_one_was_not_full.sql) Q6 (row/run counts plus a comment-stripped anchored census naming both deleters) and Q7 (`enactment_columns = 0`, `rows_for_live_run = 0`, `latest_fire = 2026-09-17`) | **Not fixed here.** Two separable pieces: (a) find why `ottoq_proposer_fire_log` stopped capturing after 2026-09-17 — it is the existing evidence-class hook and the cheapest place to land this; (b) add a disposition outcome to it, or capture disposition into `ottoq_model_call_ledger` the way 0340 captures calls. Until one lands, **say "not quotable"** rather than picking a number off a working set. | open — and it is the 0231 pattern again: a table that reads like a ledger, is named like a ledger, and is deleted from. |
| **G71** | **A proposal refused for `stall_occupied` does not distinguish contention from a proposer reasoning over a stale snapshot, and nothing currently tells them apart.** All 7 refusals on the live run carry `stall_occupied` or `stall_reserved` and all 7 target `l2`/`dcfc` at 87%/80% occupancy — which is consistent with genuine contention, and **equally consistent** with cuOpt proposing against a frame that was already out of date when it answered. G62 makes the second reading live rather than theoretical: the agent chain's mean latency is 30,394 ms against a 30-second beat, so a proposal can be born stale. The two are distinguishable — compare the target stall's occupancy at `created_at` against `disposed_at` — and are **not** distinguished today. | [0250](db/checks/0250_i_counted_five_depots_to_tell_chase_one_was_not_full.sql) §5 states the limit explicitly rather than leaving the refusal reason to be read as a verdict on the proposer | **Not fixed here**, and it is blocked on G72: the comparison needs `created_at` and `disposed_at` for a proposal that still exists, and the working set deletes them. Fix the durable record first, then this is one query. | open — named so the refusal tally is not misread as a quality judgement in either direction |
| **G70** | **The asset pushes eight channels; everything else the agent reasons over, OTTO-Q derives itself — and seven declared telemetry channels have never been emitted, so no code path that consumes them from telemetry has ever run.** Chase asked whether the agent should see all variables or only what is pushed. Measured on run `dde654cc` rather than answered in prose: **`ottoq_telemetry_packets`, 30 columns — 8 carry physics** (`soc_pct`, `battery_temp_c`, `ambient_temp_c`, `instant_power_kw`, `speed_kmh`, `current_lat`, `current_lng`, `tire_pressures_psi`), each 16,248 of 16,771, and the 523-row shortfall equals the dropped-packet count **exactly**, so the feed is narrow rather than broken. **Seven columns are 0 of 16,771**: `active_warnings`, `range_remaining_km`, `odometer_km`, `motor_temp_c`, `cabin_temp_c`, `heading_deg`, `power_15min_avg_kw`. `dtc_codes` carries **5**. Against that, **`vehicle_need_profile` has 48 columns and zero always-null** — 40 always populated, 8 conditional and each conditional for a reason (`dcfc_block_reason` on exactly the 16% blocked, the `rider_flag_*` pair together at 2%, and so on). **So the answer is that the depth is real but lopsided: OTTO-Q knows a great deal and is told very little.** And it carries a risk, by the twin repo's own law: a real OEM feed would push `active_warnings` on day one, and no OTTO-Q path has ever consumed it, because the twin has never sent it — `ottoq_agent_asset_depth` already surfaces `min_range_remaining_km` and it reads NULL for exactly this reason. **The swap test holds for eight channels and is untested for seven; untested is not working.** | [0249](db/checks/0249_the_asset_pushes_eight_channels_and_the_agent_reasons_over_forty_eight_it_derives_itself.sql) — per-column NOT-NULL census via `jsonb_each(to_jsonb(t))` so a column added later appears without editing the query; the dropped-packet identity (523 = 523); and a comment-stripped anchored census confirming **exactly one** inserter, `twin.ottoq_sim_emit_telemetry`, of the twelve routines that mention the table | **Not fixed here — the fix is twin-side.** `twin.ottoq_sim_emit_telemetry` is the single writer, so emitting the seven dark channels is one function's change and it belongs to whoever owns the twin's emitter. Q5 names them as rows so it is a work list rather than a paragraph. **Nothing should be deleted**: the columns are the right contract, they are simply unexercised. | open — measured, and it **corrects a claim of mine**: 0350's commit message named `active_warnings` and `dtc_codes` among the "populated" per-asset variables. They are 0 and 5 of 16,771. The finding 0350 was built on (the agent could not see per-asset detail) is unaffected; my sentence about the feed's depth was the reassuring version written without counting. |
| **G69** 🔴 | **A credential this repo removed twelve days ago is still live in production, in two places, and the finding that named it was never closed.** On **2026-09-07** `edge-functions/ottoq-energy-mpc/index.ts` had three hardcoded fallbacks removed and a fail-closed guard added, with its own comment saying the shared secret "is in history and must be treated as compromised regardless of what this file says now." **The fix was never deployed.** Deployed **v4, dated 2026-07-15**, still reads `Deno.env.get("OTTOQ_INTEL_URL") ?? "http://<internal host>:8080"`, `OTTOQ_INTEL_TOKEN ?? "<27-char secret>"`, `OTTOQ_BRIDGE_TOKEN ?? "<the same secret>"`, and has **no** `if (!BRIDGE_TOKEN …) return 500` guard. **And it is not only the edge function:** three live overloads of `public.ottoq_energy_mpc_replan` carry that same token as a **`DEFAULT` argument value**, dumped into the tracked `db/baseline/functions_public.sql`. **This is not a new finding** — `docs/MAGENTA_AUDIT.md` §198–228 named the secret, the plain-HTTP host, the dual use and the rotation step. What is new is the measurement that it is **still in force**, which nothing had checked, and which every drift signal this repo used reported as "unchanged". | the deployed body, pulled by `supabase functions download --use-api` and diffed against the repo (18 lines, 1,192 bytes); `pg_get_function_arguments` over `pg_proc` (3 of 3 `ottoq_energy_mpc_replan` overloads carry the token as a default); `has_function_privilege` (**`anon` cannot execute, `authenticated` can**); `ottoq_policy_params` for `energy_mpc_follow` (set once, 2026-07-15, one run scope, long purged) | **Not fixed here, and not fixable by me alone.** Two founder actions, in this order: **(1) rotate** — the 27-character secret is burned because it is in git history; set a new `OTTOQ_BRIDGE_TOKEN` **and** update the twin's `pg_net` caller in the same change, or the bridge starts refusing its own caller, and drop the `DEFAULT` from all three `ottoq_energy_mpc_replan` overloads. **(2) deploy the repo's version**, which fails closed: with the intel variables unset it returns `500 bridge not configured` instead of proxying to a hardcoded host. **The repo copy is left untouched on purpose** — syncing the deployed body in would re-commit the credential and undo the 09-07 fix, which is why `scripts/check-edge-drift.sh` lists this function as an acknowledged exception with the reason written beside it rather than muting it. | open — **founder action.** Stated at its real size: the edge function is a **fixed-target proxy**. It forwards to one hardcoded path, returns the response, writes **no** database state, takes no caller-controlled destination, and cannot reach the engine; `anon` cannot call the replan function; and nothing currently follows this bridge. The exposure is that the token is public (git history), that it is genuinely the authentication because the function is deployed **`verify_jwt: false`**, that the same string is also the outbound `Bearer` over **plain `http://`**, and that the probe and 502 paths echo the internal host. |
| **G21** | The site load meter re-derives *which run is running* once per candidate charge-session row. `twin.ottoq_sim_compute_charger_load_kw` calls `ottoq_depot_running_run(p_depot_id)` on the right of its WHERE-clause run-scope comparison: **8,966,506 evaluations in one 12-tick pair, 108.8 s, 8,756 per call**, of a value constant for the call. The call sits opposite a Var (so it is a per-row filter expression) and the function carries a `SET` clause (so it is not inlinable). The whole meter is 195.4 s of the 700.7 s pair `0129` profiled — **28%**, and ~36% of the 537 s pair after 0222. Cross-checked from the other side: the self-times of everything that calls it sum to **197.3 s** against the meter statement's **195.4 s** — so after the fingerprint, the two largest self-times in the engine profile (`ottoq_l2_propose_stall_assignment` 124.9 s, `ottoq_eval_en_001_grid_capacity` 62.9 s) are this meter seen from its callers, and those callers have almost no cost of their own. Found by reading the `pg_stat_user_functions` half of the r25_g capture that 0129 never opened, and by ranking statements by **calls** rather than seconds. | [0130](db/checks/0130_the_load_meter_asks_which_run_is_running_nine_million_times.sql) | **FIX 1 = [0223](db/migrations/0223_the_load_meter_asks_which_run_is_running_once_per_row.sql), APPLIED** 2026-09-08 08:39 AM CT — the hoist; A3's plan proves `CTE r -> Result`, one evaluation where 8,756 were. **FIX 2 = [0227](db/migrations/0227_the_load_meter_scans_forty_five_thousand_rows_to_sum_three_hundred.sql), drafted** — 0223 did not touch the scan underneath, which is still a Seq Scan reading **45,379 irrelevant rows to sum 303**, the run scope applied afterwards as a Join Filter: measured **17.5 ms / 2,751 buffers per call**, ~1,024 calls a pair, about **4.5%** of a pair. Fixed with an expression index leading on `COALESCE(sim_run_id, zero)`, because **`depot_id` holds one value in 99.87% of the table** while `sim_run_id` holds 798 — so the obvious index leads on a constant. Index only, so no result can change; chosen over 0221's sargable rewrite because that rewrite is equivalent only while zero rows carry a NULL run, which is a fact about data and not a constraint. | **BOTH FIXES CLOSED.** FIX 1 = [0223](db/migrations/0223_the_load_meter_asks_which_run_is_running_once_per_row.sql), applied 08:39 AM CT — and r27_g **proved it exactly**: `ottoq_depot_running_run` and the meter both show **1,128 calls** in one 24-tick pair, identical to the unit, where 0130 measured **8,756 evaluations per call** (`db/checks/0141`). FIX 2 = [0227](db/migrations/0227_the_load_meter_scans_forty_five_thousand_rows_to_sum_three_hundred.sql), applied 11:18 AM CT — the plan now reaches the rows through `ocpp_sessions_runscope_load_idx` with no Seq Scan surviving and no Join Filter on the run scope. |
| **G22** | The refusal reactor says `production` and hands you a sim run id. `ottoq.ottoq_react_to_refusals` hardcodes `p_data_source:='production'` at both escalation call sites while passing `p_sim_run_id:=p_sim_run_id` on the next line: **71,944 signed events across 494 sim runs** assert production provenance and carry a simulation run. It is the largest production-labelled event type in the database. Together with G16's 26,856 fleet-reset events, **98,800 of the 98,834 production-labelled rows in a 2.28M-row signed stream are the certification harness — the `production` half of the event stream is 99.97% harness.** | [0132](db/checks/0132_the_refusal_reactor_hardcodes_production_and_passes_a_sim_run_id.sql) | **[0224](db/migrations/0224_the_refusal_reactor_says_production_and_hands_you_a_sim_run_id.sql) drafted**, `forces_recert` FALSE *proven* rather than predicted — its P1 asserts against the live verdict function that `h_evt` never reads `data_source` | **CLOSED AND VERIFIED** — [0224](db/migrations/0224_the_refusal_reactor_says_production_and_hands_you_a_sim_run_id.sql) applied 08:41 AM CT, and round 27 supplied the evidence its footer demanded: **1,960 `ottoq.refusal_escalated` events since the recert floor, every one `data_source='twin'`, zero `production`.** |
| **G23** | The retention purge covers the event stream and barely touches the calendar. Lifetime insert/delete: `ottoq_events` 97% purged, `ottoq_rule_evaluations` 45%, **`ottoq_stall_bookings` 14%**, `ottoq_service_detail_records` 6%. The calendar is an **accumulation, not a rolling window** — it grows ~800 rows per certification pair, forever, and [0127](db/checks/0127_the_run_scope_predicate_that_no_index_can_read.sql) measured it at 53% of every disk block this database has ever read. Same shape as G19/G21 from the storage side: a fixed workload whose cost grows because the table it crosses grows. | [0133](db/checks/0133_two_clocks_the_batch_proof_and_the_live_request.sql) Q4 | two questions, and the first is not the build track's: (1) are a finished twin run's bookings evidence worth keeping? (2) if yes, the hot path must stop crossing them — 0221 did that for one function, 0130 Q9 proposes doing it broadly | open |
| **G25** | **Four enforced atoms are outside the canon comparison.** The pair enforces fourteen equalities; `ottoq_cert_matrix`'s `on_canon` — which feeds `consecutive_passes`, which feeds `green` — compares **nine**. `h_rule` (enforced 0205), `h_rcl` (0217), `h_sdr` (0219) and `endst` (0139) are not among them. Worse: `canon_rule` and `canon_rcl` are *returned as columns* — carried, printed, never judged, so a reader sees them beside `canon_cmd` with no way to know only one can break a streak. A change moving one of those four **identically on both arms** passes the pair, keeps `on_canon` true, advances the streak and leaves `green` true while the canon silently moves. Round 26 caught it only because two markdown files were diffed by hand. | [0134](db/checks/0134_four_enforced_atoms_are_outside_the_canon_comparison.sql), [0140](db/checks/0140_p3_refuses_for_a_disagreement_its_own_fix_already_explains.sql) | **[0225](db/migrations/0225_the_canon_comparison_sees_every_atom_the_pair_enforces.sql) drafted, and its P3 rewritten before it was ever applied.** The first P3 asserted the newly compared atoms were single-valued above the recert floor. Dry-run against the floor **0226** installs — the combination the apply window actually creates, and one nothing had evaluated — **it failed on two of seven columns**, and all three faults were in P3, not the data: it selected `depot_id` then dropped it at `GROUP BY` while the matrix keys `(depot, seed, ticks, scenario)`; it judged `grid_smoke`, a 6-tick fixture; and its bar was stricter than the property it names — the one real disagreement (`busy_day/314159/12t`, the 08:25 pair, whose two arms hashed `h_sdr` differently **eighteen minutes before 0218 fixed exactly that** and while 0219 still had `h_sdr` measured rather than enforced) costs three streak rows and leaves the column green. P3 now asks the question that matters: **does any column green under nine atoms stop being green under fourteen?** A fixture that was never green cannot lose green, so `grid_smoke` drops out without special-casing. The floor is deliberately *not* raised past 08:25 and that pair is not touched — it is correct history and the evidence that motivated 0218. **The replacement P3 has not yet been dry-run**; the file's own footer says so and says it must return no rows first. | **CLOSED** — [0225](db/migrations/0225_the_canon_comparison_sees_every_atom_the_pair_enforces.sql) applied 2026-09-08 11:14 AM CT. The matrix now compares all fourteen atoms; `canon_sdr` and `canon_endst` are populated on every current column. Two defects caught in pre-flight, both mine: **P3 had been dry-run against the live floor rather than the one 0226 installs** (it passed for the wrong reason), and **A2 contradicted A5** and would have aborted — A2 forbade any streak change while A5 existed to report exactly that. A2 now checks P3's prediction against the outcome instead. Result matched the committed table exactly: `busy_day/314159/12t` 6 → 3 as the only movement, no column losing green. |
| **G24** | **There is no load test.** What is measured is a 34–40 day *lifetime* mean — 24–47 ms per request over 637,473 calls at ~5.6 calls/min — and the counters did not move across 75 idle seconds. That is evidence the code path is fast and none at all that the system is fast under concurrency. The attempt to settle the 8–20 s tail by differencing latency inside and outside a certification-pair window is recorded *as a failure*: at 5.6 calls/min a 19-minute window holds ~100 calls against a 19-second tail. | [0133](db/checks/0133_two_clocks_the_batch_proof_and_the_live_request.sql) | **HARNESS BUILT AND CALIBRATED, NOT YET RUN** — `load/` (see `load/README.md`). pgbench-driven, declared targets, nearest-rank percentiles, a `SELECT 1` floor measured at the same concurrency so `engine_share = target - floor`, `pg_stat_activity` probes before/between/after so a pair overlap is labelled rather than discarded, achieved-vs-requested rate, and a refusal to pass off a tail from under 200 transactions. Calibrated against a known 20 ms sleep: reads 20.325 ms, floor 0.000 ms — `tests/test_load_harness.py`. Also found that pgbench reports two latencies differing 29x on a fast path (throughput-derived vs per-transaction log); the harness keeps both and flags the gap. **`load/results/` is empty and that is the honest state**: the harness needs `OTTOQ_DB_URL` and this container has none, and it refuses to fall back to the management API because that path cannot do concurrency. The HTTP edge-function path is deliberately unimplemented rather than measured through an agent proxy. | open — needs a host |
| **G28** | **Every `forces_recert: FALSE` declared since 2026-09-04 15:00 has been inert, and no certification column is green.** `ottoq_cert_recert_floor` joins `ottoq_cert_lineage` to `schema_migrations` on `l.name = m.name`. Since that date every lineage row has been written with the `NNNN_` file prefix while the apply path writes the unprefixed name, so **33 of 92 classifications join nothing** and fall through the deliberately conservative `COALESCE(l.forces_recert, true)`. Twenty-one of the 33 say FALSE. The floor therefore jumps to the newest migration after every apply — it sat on **0224's** timestamp, a migration that declares FALSE. Measured cost: **not one column green, six of seven stale, and `busy_day/424242/24t` carrying twenty-six consecutive passing pairs against a `consecutive_passes` of zero.** Normalising the key on BOTH sides (one-sided breaks the case where `schema_migrations` itself carries a prefix) puts the floor at **2026-09-07 21:36:53** — 0208, the newest migration that actually declares TRUE — and **zero** migrations since the boundary are genuinely unclassified, so this is not a loosening, it is the floor the classifications already specify. Invalidates no pair verdict and no hand-diffed round canon; invalidates every 'green'/'streak' statement since 09-04 and the *effect* (never the reasoning) of my own FALSE footers. | [0135](db/checks/0135_the_recert_floor_reads_a_name_the_ledger_never_writes.sql) | **fix drafted as [0226](db/migrations/0226_the_recert_floor_reads_a_name_the_ledger_never_writes.sql)**, dry-run read-only against the live catalog — normalises both sides and adds `ottoq_cert_lineage_orphans()` so an unreachable classification is one statement away from being seen, with A3 asserting the conservative default survives. Applied after round 27 and **before 0225**, deliberately: a lower floor makes 0225's P3 a much harder test. | **CLOSED** — [0226](db/migrations/0226_the_recert_floor_reads_a_name_the_ledger_never_writes.sql) applied 2026-09-08 11:11 AM CT. Floor `2026-09-08 13:41:09` → **`2026-09-07 21:36:53.363037`**; **six flagship columns green, from zero.** Its A1 was rewritten before applying and would otherwise have aborted the whole window: it demanded `ottoq_cert_lineage_orphans()` be empty and it returns **22** — the SQL-endpoint block 0192–0215, which has no `schema_migrations` row to key against and whose classifications 0199's second floor branch already consults. **The proof is not inside the migration:** 0225, 0227 and 0228 all landed afterwards as `forces_recert FALSE` and the floor did not move once. |
| **G26** | **Production and the proof harness share one database.** A pair holds the flagship depot in one transaction for 9–18 minutes, six pairs a round, several rounds a day, on the database the API serves. Leading candidate for G24's tail, and the structural reason G16 and G22 can put harness rows into the production-labelled event stream at all. | [0133](db/checks/0133_two_clocks_the_batch_proof_and_the_live_request.sql) Q3 | **costed with numbers in [0137](db/checks/0137_there_is_no_production_to_separate_from_the_harness.sql), and the finding's own title is the wrong framing.** Measured: **799 of 809 sim runs are `cert_harness`**, the only two non-sim depots are a hardware lab bench and a retired fixture with **one stall each**, and with G16+G22 the production-labelled signed stream is 99.97% harness. This is not two tenants sharing a database; it is one tenant and a guest. Three options costed — a separate project (buys all three costs), a separate schema (**named so it can be rejected on purpose**: same pool, same cache, same 6–13 minute transaction, so it buys nothing for the one that matters), and measure-first. **Recommendation, stated: run the `load/` harness with and without a pair in flight, publish both with run IDs, then take the separate project** — and take it anyway if G12 stays blocked, because "CI has no database" is a cost being paid daily and only that option ends it. | open — decision is Chase's |
| **G27** | **Two hoisting fixes each miss their own per-call arithmetic, and now each has been measured on TWO 24-tick columns, so it is a property of the fix and not of a seed.** 0222 (boot fingerprint, called **4 times per pair regardless of tick count**, so its saving should not scale at all) removed 228 s from the 12-tick mean and **511 s and 602 s** from the two 24-tick columns — ratios **2.24 and 2.64** against a predicted **1.0**. 0223 (load meter, called **per tick**, so its saving should scale by **2.0**) removed 155 s from the 12-tick mean and **201 s and 183 s** from the same two columns — ratios **1.30 and 1.18**. **Both columns of each fix agree with each other and miss the prediction the same way, in opposite directions between the fixes.** That kills the hypothesis recorded earlier — 'hoisting removes more than the call because it also removes what the planner did around it per row' predicts *over*-scaling for 0223, and 0223 under-scales on both columns. A single mechanism covering both is now harder to construct, not easier. And the number is only ever wrong in our favour, which is the reason to chase it rather than enjoy it. | [round26.md](db/canons/round26.md), [round27.md](db/canons/round27.md) | **all of the above is arithmetic about wall-clock, the weakest instrument in the building.** `r27_g` fires 15:52 UTC — a seventh column at 24 ticks with `track_functions='all'` (not `'pl'`, which does not count SQL functions and is exactly what blinded `0129`), against a `pg_stat_user_functions` baseline captured 13:45 and re-verified row-by-row at 15:16 with not one counter moved. It measures the two quantities this entire table assumes and **nothing has ever counted**: how many times the boot fingerprint is called at 24 ticks, and how many times the load meter is. `db/checks/0136` Q8 establishes the same diff answers G23(b), and `db/checks/0139` Q6 that it answers G21b. One measurement, three findings. **PARTLY DELIVERED, and the shortfall is named rather than glossed:** r27_g counted the load meter at **1,128 calls / 17,886 ms in one 24-tick pair** (`db/checks/0141`), which closed G27's live half — the meter is called **~1.10x**, not the 2.0x the per-tick assumption predicted, and the observed savings ratios 1.30 and 1.18 bracket that counted figure. What it could NOT do is name the caller of the 13.2M `ottoq_policy_get` calls, because its baseline held fourteen rows. **`r28_g` at 18:25 UTC is a 12-tick column against the full 304-row baseline**, giving the first honest 12-tick count and the caller attribution both. | **CLOSED by `r28_g`.** The load meter runs **1,004 calls at 12 ticks** against **1,128 at 24** — a ratio of **1.12x, not 2.0x**. 0223's predicted scaling assumed one call per tick; the meter is very nearly independent of tick count, so the per-call arithmetic did not under-deliver and the assumption underneath it was wrong. And `db/checks/0130` **derived** ~1,024 calls at 12 ticks without ever counting them: measured **1,004, right to within 2.0%** — a figure treated with suspicion for two rounds that did not deserve it. **What replaces G27 as the open duration question** is round 28's finding that the 24t:12t cost ratio moved from **1.53 to 2.34–2.59**, which 0144 explicitly does *not* claim to explain. | CLOSED |
| **G21b** | **SOLVED by `r28_g` (`db/checks/0144`). It is a view, and the static census could never have found it.** `ottoq_policy_get` ran **4,894,867 calls / 199,648 ms self in one 12-tick pair** — **60.5% of the pair's entire wall clock** and 85.96% of all function calls. **The caller is not among the 64 in the census**: every one of them at its theoretical maximum (measured invocations × static mentions) sums to **42,461**, which is **115x short**. The source is `public.ottoq_approach_band`, whose first CTE is `MATERIALIZED` and resolves three policy parameters **for every sim run that has ever existed** — **831 rows scanned, 1 needed, 2,493 calls per evaluation where 3 suffice, 99.88% waste** — to obtain three constants for the run actually executing. The one real consumer already filters by run *and* vehicle and takes one row; `MATERIALIZED` is precisely the keyword that forbids pushing that predicate into the CTE, so **96,120 of 96,121 rows are built and discarded**. Same defect class as `0145` and the energy orchestrator: an unscoped read of a run-scoped table. `0139`'s refusal to guess was correct — it called the shape "a function called from SQL the profiler cannot attribute" and that is now measured rather than suspected. (`ottoq_readmit_resumed_visits` is **not** a second consumer: its only mention is inside a comment.) | [0144](db/checks/0144_sixty_percent_of_the_pair_is_a_view_resolving_policy_for_every_run_that_ever_ran.sql), [0139](db/checks/0139_the_three_busiest_indexes_in_the_database_are_one_functions_misses.sql) | **no fix drafted, deliberately.** Three candidate shapes (drop `MATERIALIZED`; scope the CTE; convert to a function taking the run id) and their pre-flight requirements are in 0144. The view is read on the decide path, so any change must prove the four 12-tick canons unmoved by EXPLAIN and by a recertification round before it ships. **A whole-run memo of `ottoq_policy_get` remains unsafe** for the reason 0139 gave — the MPC lookahead inserts and deletes policy params mid-tick. | **SOLVED — caller named, fix specified, not written** |
| **G62** | **The agent is on average exactly one tick late.** Nemotron's mean latency over 1,120 calls is **30,394 ms**, max **180,743 ms**, against a 30-second beat; **433 of 1,120 (39%) exceeded one tick.** That is the mechanism behind 721 `deterministic_fallback` decisions sitting beside 729 `nemotron` ones. An advisory agent cannot be a synchronous dependency of the beat it advises. | see Fix column | open — quantified and durable via `ottoq_intelligence_ledger.calls_over_one_tick` ([0340](db/migrations/0340_every_external_model_call_gets_a_ledger_that_outlives_its_run.sql)). The fix is architectural, not a timeout bump: the agent must advise the NEXT beat, not block this one. | open |
| **G64** | **Every Nemotron call changes engine state with no L1 rule evaluated — and the wording above overstated the hole, which is worth correcting in the table rather than in a footnote.** The count is right: `agent_calls_with_no_l1_rules` = **1,120 of 1,120**, and run `dde654cc` carried 177,179 rule evaluations at four probe points with **not one at a policy write**. What is wrong is the implication that the write was *unguarded*. Read end to end it passes four guards: a six-key `KNOBS` allowlist in `ottoq-orchestrator-agent` (a key outside it is not written, it is queued to `ottoq_ops_approvals` as `nemotron_policy_out_of_whitelist` for a human), a move cap of 3 per chain, a ±30% per-tick drift limiter, and `ottoq_policy_set`'s own five returned refusal reasons plus the catalog clamp — with the enacted value read back from the database. The AI is bounded; it is not bounded **by the shield**, and it does not appear in the shield's ledger. **Two things that are genuinely wrong, both measured:** (a) three of those four guards live only in edge-function TypeScript, and the catalog is **WIDER** than the agent envelope on **3 of 6** dials — `deploy_peak_fraction` 0.5–1.0 vs 0.30–1.00, `energy_demand_factor_peak` 0.3–0.9 vs 0.25–0.95, `energy_demand_factor_expensive` 0.2–0.8 vs 0.20–0.90 — so nothing in the engine could state what the model is permitted to do; `ottoq_policy_set`'s own comment that "the catalog is the one that actually wrote the row" is mechanically true and descriptively wrong, because the edge clamps first and the catalog is the OUTER bound. (b) "Show me, in one ledger, every deterministic check that admitted an AI action" had no answer. | `ottoq_rule_evaluations` grouped by `action_context` (4 contexts, 177,179 rows, none a policy write); `ottoq_policy_param_catalog` joined against `KNOBS` (3 of 6 disagree); `ottoq_policy_params` by `updated_by` (`ottoq_prime`, 1,269 rows, exactly the six declared keys, every value ever written inside the edge envelope) | **[0352](db/migrations/0352_the_agents_dial_limits_live_in_edge_typescript_and_not_in_this_database.sql)** puts the envelope into the engine — `agent_writable` / `agent_min_value` / `agent_max_value` / `agent_max_drift_pct` on `ottoq_policy_param_catalog`, seeded verbatim from `KNOBS`, published as `ottoq_agent_dial_envelope()` returning **both** the agent and the engine bound so a reader can see which is binding; its A1 refuses any agent bound **looser** than the engine's. **[0353](db/migrations/0353_the_shield_gains_a_fifth_probe_point_and_the_ai_write_path_finally_lands_in_its_ledger.sql)** adds `action_context = 'policy_write'` — the fifth probe point that is actually **called** — the rule `AI.001.agent_dial_within_envelope`, and a probe in `ottoq_policy_set` through `ottoq_shield_probe` (chosen by census: all four existing probe points use it, and only it returns `would_block`). Non-vacuity is proven both ways: 0.93 on `energy_demand_factor_peak` is legal to the setter (catalog max 0.95) and must **fail** the agent ceiling of 0.9, while the same value from a non-agent actor must **pass**. Verified in production, not only in a rolled-back assertion: a value-preserving write by `0353_verify` produced one ledger row in **4 ms**, `entity_id` NULL, run recoverable from `context.scope_id`. | open — **measurement closed, refusal not wired.** The rule is `enforcement = 'log_only'` on purpose (CLAUDE.md 2.9a: measured first, enforced after a flagship round; promotion must read `ottoq_shield_probe.would_block`). And the envelope is a declaration plus a detector until `ottoq-orchestrator-agent` is redeployed to read `ottoq_agent_dial_envelope()` instead of its own `KNOBS` — which is gated on **G67**, since the repo's copy of that function is stale and deploying from it would regress production. |
| **G65** | **Starting a simulation synchronously deletes all history, and one missing index made that quadratic.** `ottoq_start_demo_run` calls `ottoq_purge_prior_runs` inline before it creates a run. Measured 2026-09-19: **~6.4 M rows across 35 engine tables** over 5,000 rows each (2,385,592 in `ottoq_decisions` alone), three of them firing a per-row DELETE trigger. On top of that, `ottoq_ops_approvals.visit_id` — the referencing column of an FK to `ottoq_visit_needs` — had **no index**, so every one of 122,734 deleted parent rows cost a 30.4 ms seq scan: **62 minutes for that one table.** Observed, not modelled: cron jobid 727 spent 16m48s inside that FK probe and was still going. | cron `job_run_details` for jobids 726/727, `EXPLAIN (ANALYZE, BUFFERS)` before and after, `pg_constraint` × `pg_index` census of unindexed FK children on the purge path | [0347](db/migrations/0347_one_missing_index_made_the_twin_start_door_cost_six_thousand_gigabytes.sql) fixes the quadratic term — 30.447 ms → **0.159 ms** (191×), 6,604 buffers → 2, so that table goes 62 min → ~20 s. It does **not** fix the shape. | open — the index shipped; bounding the start-path purge has not. A start whose cost grows with all history is a landmine that regrows. Bound it against the measured cost of the first successful purge, not a guess. |
| **G66** | **The declared rank-0 proposer does not fire at all on a fully armed run, which is a different defect from G60 and must not be filed as its recurrence.** Run `dde654cc` (2026-09-19 17:09, `ottoq_agentic_arming` verdict **armed, 7 of 7**, `prearrival_charge_yields_to_solver=1`) ticked past 40 ticks with cuOpt calling NVIDIA live and enacting 21 `stall_assignment` rows — and `ottoq_proposer_fire_log` recorded **zero fires during the run**. Its whole content is `forward_lex`: 75 empty fires (last 2026-09-17 00:29:33) and 37 submitted fires / 208 proposals (last 2026-09-14 09:23:34), both predating the run. So `ottoq_agent_review`'s `empty_fires: 0` is not "fired and found nothing", it is **never fired**, and the verdict is `solver_returned_nothing` rather than G60's `solver_saw_empty_frame`. | `ottoq_proposer_fire_log` grouped by (declared_source, status) with `max(fired_at)` read against the run's `started_at`; `ottoq_agent_review(run,3)`; `ottoq_activity_feed(run,400)` | **unfixed.** 0339 opened the gate that was taking the scarce resource before the fire; the fire itself is not happening, one stage earlier. A fix aimed at G60's mechanism cannot move this, and the A/B that proved G60 says nothing about it. | open — this is PR #194's outstanding item, now localised: `fires_with_headroom` cannot become non-zero while `fires` is zero. Evidence: [0248](db/checks/0248_the_twin_start_door_opens_and_the_whole_loop_is_visible_on_a_live_armed_run.sql) |
| **G68** | **CP-SAT is absent, not broken, and the fallback hid it so well that the attestation reported "armed".** `ottoq-cpsat-propose` v5 requires `OTTOQ_INTEL_URL` and `OTTOQ_INTEL_TOKEN`; both unset, so it throws `"CP-SAT service is not configured"` before building a request, and its catch returns **HTTP 200** with the cuOpt fallback. `ottoq_proposer_submit_batch(p_source:='forward_lex')` — the only writer of `ottoq_proposer_fire_log` — is therefore never reached, which is the mechanism behind G66. Measured on run `dde654cc`: **60 of 60** agent chains, ticks 1→221, identical reason. `cpsat_service`'s last ledger call is 2026-09-14 09:23:39. | `ottoq_decisions.enacted_action->'solver_handoff'` grouped over the run; deployed bodies of both edge functions; `ottoq_intelligence_ledger` | [0349](db/migrations/0349_a_run_reads_armed_while_its_rank_zero_proposer_is_unreachable.sql) makes it **visible** — `ottoq_agentic_arming` gains a `primary_proposer` block and a new verdict `armed_primary_unreachable`, proven both ways (A2 requires the verdict on the live defect; A3 plants one fire row in a rolled-back subtransaction and requires `reachable` to return to `true`, so the signal is a measurement and not a constant). | open — **founder action, not mine.** The two secrets must be set and the `ottoq-intelligence` host (FastAPI + `ortools==9.15.6755`) must be running. The bridge's 5-second `AbortSignal.timeout` and its comment that "the solver host may be intentionally stopped between development sessions" are correct and were left alone. |
| **G67** | **The repo is stale on the six edge functions that own the loop, and nothing detects it.** Traced while diagnosing G66: the repo's `ottoq-orchestrator-agent/index.ts` ends its handoff with `EdgeRuntime.waitUntil(fetch(...))` and can emit only `queued`/`disabled`/`gate_error`; the **deployed v26** awaits the bridge and emits `completed`/`fallback`/`failed` with `engine` and `fallback_reason`. Every live agent decision records `{status: fallback, engine: cuopt, fallback_reason: "CP-SAT service is not configured"}` — **a shape the repo's copy cannot produce.** Six functions changed since the 2026-08-03 manifest snapshot and **every one is on the agent/solver/twin loop**: `ottoq-cpsat-propose`, `ottoq-orchestrator-agent`, `otto-twin-control`, `ottoq-cuopt-propose`, `ottoq-orchestrate-tick`, `ottoq-assign-optimize`. Anyone auditing the loop from the repo reads code that is not running. **And the old manifest's drift signal was worse than none:** its `version` column is not the API's — `ottoq-wave-admit` reads v2 there and v5 live with an identical `updated_at`, so diffing it flags ~26 of 28 functions when 6 changed, a 77% false-positive rate. | `list_edge_functions` `updated_at`/`sha256` against the 2026-08-03 snapshot; deployed bodies of `ottoq-orchestrator-agent` v26 and `ottoq-cpsat-propose` v5 read in full and compared to the on-disk copies | `edge-functions/_MANIFEST.md` refreshed 2026-09-19: the 6 stale copies named with their deployed `sha256`, the withdrawn version-column claim, and `updated_at` adopted as the signal. **The six were deliberately NOT hand-synced** — transcribing ~400 lines of deployed TypeScript out of an API response risks a silent one-character divergence, and a copy that looks synced is worse than one labelled stale. They need a real Management-API pull verified against the recorded hashes. **Then done properly, 2026-09-19:** with a Supabase access token the founder supplied, all 28 ACTIVE functions were pulled with `supabase functions download --use-api` (no Docker, no transcription — the CLI unbundles server-side and writes source straight to disk) and compared by SHA-256. Four were synced into the repo; `scripts/check-edge-drift.sh` makes the comparison repeatable, refuses to report a pass when it could not compare (no token/CLI → **exit 2**), and is mutation-proven in both directions (a one-line change to a repo copy reports `DRIFT` and exits 1). | **CLOSED — and the pull corrected the drift list it was based on.** Measured by content hash, **five** functions differed, not six: `ottoq-cuopt-propose` and `ottoq-assign-optimize` were flagged by `updated_at` and are **byte-identical** (false positives), and **`ottoq-energy-mpc` was NOT flagged and differs** (false negative — its `updated_at` of 2026-07-15 predates the snapshot). So **both** drift signals this repo has used were wrong: `version` at a 77% false-positive rate, and `updated_at` at 2 false positives + 1 false negative out of 28. The false negative is the one whose drift contains a live credential — see **G69**. Repo is now **27 of 28** in sync, the 28th deliberately. |

---

## Closed

| # | Finding | Closed by |
|---|---|---|
| G2 | Anon `EXECUTE` on privileged mutations; proposals and overrides took identity from the client. | server-derived identity |
| G3 | Two need-derivation paths — twin and real feed — that could disagree. | one shared path reading asset state |
| G4 | The verdict could not see the proposal stream. | `h_prop` + `h_defr` |
| G5 | The L1 rule-evaluation ledger was not run-scoped. | run-scoping migration |
| G6 | The recall decision left no ledger row. | [0206](db/migrations/0206_every_recall_decision_is_a_ledger_row_and_the_implementation_is_a_name.sql) |
| G7 | The work side had no way to refuse a recall. | [0211](db/migrations/0211_the_work_side_can_refuse_a_recall_and_the_refusal_is_a_ledger_row.sql), [0212](db/migrations/0212_the_tick_asks_the_work_side_before_it_books_the_stall.sql) |
| G11 | The event stream carried no sim time. | sim-clock column on `ottoq_events` |
| G15 | The L1 shield read the wall clock inside the twin — 12 of 29 active evaluators. | [0204](db/migrations/0204_the_run_has_one_clock_and_the_shield_and_the_events_read_it.sql) |
| G18 | The repo had stopped following the change-control discipline `scripts/APPLYING.md` documents: 93 migration files carried no version header, 98 carried invented ones, and CI never looked at `db/migrations` at all. | `tests/test_migration_hygiene.py`, `scripts/gen-migration-index.py`, `db/migrations/UNFILED.md` |
| G17 | KPI-4's touch vocabulary was seven-eighths unsatisfiable — seven of its eight actor types were rejected by the `ottoq_events` CHECK — and it missed every real human actor the constraint does permit. | [0213](db/migrations/0213_kpi_four_counted_seven_actor_types_that_cannot_exist.sql) — vocabulary is now a table pinned to the live CHECK; 20 classified, 8 human, none uninsertable |
| G19 | The certification pair got ~2x slower in seven days on a fixed workload. `ottoq_boot_state_fingerprint` — the enforced `endst` atom — serialized and MD5'd **1,360,915 rows per call**, four calls per pair, to characterise **13**. | [0222](db/migrations/0222_the_fingerprint_hashed_a_million_rows_to_report_thirteen.sql) — the fingerprint measured **260.6 ms / 218.2 ms** against ~64,000 ms after; round 26 a and b landed **537 s and 533 s** against a 16-pair baseline of min 643 / mean 755. Evidence: [0129](db/checks/0129_the_fingerprint_hashes_five_million_rows_to_report_thirteen.sql), [0126](db/checks/0126_where_the_tick_actually_spends_its_time.sql), [0128](db/checks/0128_the_pair_reads_nine_gigabytes_and_seven_of_them_are_five_scans_of_one_table.sql) (conclusion superseded) |
| G20 | The second SDR emitter would bill a DCFC charge at the L2 tariff: `charge` is the one `svc_code` mapping to two operations and the lookup picked with an unordered `LIMIT 1`. Dormant since 2026-06-18, so nothing burned. | [0220](db/migrations/0220_the_schedule_task_emitter_asks_the_stall_what_kind_of_charge_it_was.sql) — the trigger asks `stalls.stall_type`, not the heap. Evidence: [0124](db/checks/0124_the_other_sdr_emitter_would_bill_a_dcfc_charge_at_the_l2_tariff.sql) |
| G63 | **The Twin start door had never opened: `ottoq_start_demo_run` could not complete in this engine's recorded life.** Four defects in sequence, each hidden behind the one before it. | [0344](db/migrations/0344_the_purge_cannot_complete_and_the_twin_start_door_has_no_handler.sql) (registry check (b) narrowed to run-key columns, blocking FK dropped) → [0345](db/migrations/0345_the_purge_deletes_engine_tables_alphabetically_not_by_dependency.sql) (step (4) ordered by real FK depth, not alphabetically) → [0347](db/migrations/0347_one_missing_index_made_the_twin_start_door_cost_six_thousand_gigabytes.sql) (unindexed FK child column made the purge quadratic: 30.447 ms → 0.159 ms per probe) → [0348](db/migrations/0348_a_six_row_table_held_the_twin_start_door_shut_because_its_guard_never_learned_the_purge_protocol.sql) (a **six-row** table whose append-only guard never learned the purge's arming protocol, which also made `ottoq_recall_decisions`' 283k rows undeletable via FK). **Measured end to end:** run `dde654cc`, 7,032,541 rows purged in 210,880 ms, `retry_passes: 0`, armed 7 of 7, ticking. Evidence: [0248](db/checks/0248_the_twin_start_door_opens_and_the_whole_loop_is_visible_on_a_live_armed_run.sql) |
| G60 | **The rank-0 primary proposer was starved by a contract signed forty minutes before the vehicle arrived.** 0335 made CP-SAT (`forward_lex`) the primary assignment proposer; it submitted **nothing** across 75 fires, and in 74 of them `n_in_serviceable_state` equalled `n_vehicles_held` exactly. `ottoq_sim_prearrival_contracts` reserves a dcfc/l2 stall for every `en_route_to_depot` vehicle from the WORLD beat, so by `arrived_at_gate` — the only state the fire and `ottoq_decide_tick` act on — the choice was already made. 0337 could not fix it: a reservation taken forty minutes ahead is not a race. | [0339](db/migrations/0339_the_charge_stall_was_reserved_before_any_solver_could_see_it.sql) — run-scoped `prearrival_charge_yields_to_solver`, default 0 = unchanged, granted by `ottoq_agentic_arm`; attestation went six keys to seven. **Proven by controlled A/B on the live function, both arms in one aborted transaction: 23 en-route vehicles, dial=0 took 23 of 23 charge stalls, dial=1 took 0 and granted 23 staging.** Evidence: [0247](db/checks/0247_the_primary_solver_was_starved_by_a_contract_written_forty_minutes_early.sql) |

---

## Measured leads — not yet adversarially refuted

Stated separately because the discipline of this page is that a finding names
the guard that closes it. These are measured but the diagnosis is not finished,
and each says exactly what would falsify it.

| # | Lead | What is measured, and what is not |
|---|---|---|
| G61 | **The run-scope registry is per-column; its FK guard is per-table, so a mixed-class table is required to block the purge it is registered to survive.** `ottoq_proposer_fire_log` has two rows — `sim_run_id` class `evidence` and `tick_seq` class `stamp` — and `ottoq_check_run_scope_registry` check (b) asks "does this TABLE have an FK to `ottoq_sim_runs`". So the stamp row demands the FK that defeats the evidence row. | **Measured:** dropping that FK made the guard report a blocking defect, which refused migration 0340 outright (assertion A3b). 112 fire-log rows name runs in the purge's doomed set. **Not established:** whether the right fix is narrowing check (b) to the registered column, reclassifying `tick_seq`, or giving evidence tables a nullable FK. 0340 sidesteps it by registering exactly one column and pins that with A3d. |

---

## Not ours to close — founder actions

These are open, they are the highest-severity items on this page, and no
migration can close them.

| # | Finding | State |
|---|---|---|
| **S-01 / S-02** | A shared secret was committed to the repository. The **code half is fixed**: the literal is gone and the bridge fails closed rather than open, pinned by `ottoq-intelligence/tests/test_auth_fails_closed.py`. A committed secret stays compromised until it is **rotated**, which is an operator action on EC2 and in Supabase. | open — awaiting rotation |

Everything else in [`docs/MAGENTA_AUDIT.md`](docs/MAGENTA_AUDIT.md) — 85 of 87
findings — is closed, each `FIXED` naming the guard that fails without it.

## The other thing on this page that is not ours to close

`db/migrations/UNFILED.md` lists **67 migrations, 734,007 characters**, applied to the
database with no committed file. They are recoverable from `ottoq_schema_snapshots`
and the function catalog. Whether to spend the time recovering them is Chase's
call, not the build track's, and the drift check reports **INVESTIGATE** rather
than CLEAN until it is made.
